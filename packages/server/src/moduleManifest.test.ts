/**
 * СТОРОЖ МАНИФЕСТА — `DEV_MODULES` против `MODULE_MANIFEST_VERSION`.
 *
 * Инвариант #6: порядок выполнения модулей = порядок массива в `createKernel`, и он
 * зафиксирован ПОМАТЧЕВО (`state.version.manifest`). Загрузчик (`serverWiring.ts`)
 * отказывается поднимать матч, чей закреплённый манифест не совпал с задеплоенным, —
 * иначе живой матч молча доигрывался бы по другим правилам, а его реплей перестал бы
 * воспроизводиться.
 *
 * Но вся эта защита держится на РУЧНОЙ дисциплине: «поправил `DEV_MODULES` — не забудь
 * поднять `MODULE_MANIFEST_VERSION`». Забыл — и проверка на загрузке пропускает матч,
 * потому что версия-то не изменилась. Проверять нечего: строка сама себя не знает.
 *
 * Этот файл замыкает контур. Список ниже — граф модулей, за который отвечает ТЕКУЩАЯ
 * версия манифеста. Любая правка `DEV_MODULES` (добавили модуль, убрали, переставили)
 * роняет тест, и починить его можно ровно одним способом: поднять
 * `MODULE_MANIFEST_VERSION` и перезакрепить список — то есть сделать осознанно то, что
 * иначе забылось бы.
 *
 * Область — ЧЛЕНСТВО И ПОРЯДОК, ровно как формулирует правило рядом с самой константой.
 * `version` отдельного модуля намеренно не закрепляется: это другой сигнал и другое
 * решение, и молча расширять контракт тестом было бы неправильно.
 */
import { describe, expect, it } from 'vitest';
import { createKernel } from '@void/shared-core';
import { createDevMatch, DEV_MODULES, MODULE_MANIFEST_VERSION, loadShippedData } from './scenario';

/** Версия манифеста, для которой закреплён список ниже. Разъехалась с
 *  `MODULE_MANIFEST_VERSION` → список устарел, его надо перезакрепить.
 *  (12 — CARGO-1: членство и порядок НЕ менялись, версию подняла смена ФОРМЫ
 *  состояния — у флота появились заявки на подъём десанта. Правило рядом с самой
 *  константой требует поднимать её и в этом случае, поэтому список просто
 *  перезакреплён за новой версией, без правок.
 *  21 — VET-2: тот же случай. Членство и порядок не тронуты, форму изменило
 *  ДОБАВЛЕНИЕ двух полей заслуги ветерана на `UnitStack`; список перезакреплён
 *  без единой правки.
 *  22 — PVR-1.4: снова тот же случай. Членство и порядок не тронуты, форму изменило
 *  ДОБАВЛЕНИЕ поля `boons` на `PveState` — долга по усилениям между волнами; список
 *  перезакреплён без единой правки.
 *  28 — ROADS-2/3: членство и порядок не тронуты, сменились ПРАВИЛА движения для партий
 *  с сетью дорог (`Planet.roads`), встреч на развилке и втягивания в бой; список
 *  перезакреплён без единой правки.
 *  29 — CORE-DMG-3: членство и порядок не тронуты, сменились ПРАВИЛА УРОНА — ауры и
 *  пассивы героя дошли до неближних каналов огня; список перезакреплён без правок.
 *  30 — PERK-3.1: а вот тут ЧЛЕНСТВО и изменилось — добавлен `veteran`, надбавка за
 *  пережитые бои. Ровно тот случай, ради которого сторож и стоит.
 *  31 — PERK-1.2: членство и порядок снова не тронуты, сменились ПРАВИЛА УРОНА — техи,
 *  фракция и аура переехали в параллельную корзину и теперь складываются, а не
 *  перемножаются; список перезакреплён без единой правки.
 *  32 — PERK-3.2: снова ЧЛЕНСТВО — добавлен `promotion`, случайный промоушен. Его место
 *  в списке значимо: строго перед `auto-rally`, оба слушают `unit.built`.
 *  34 — AUD-20: членство и порядок не тронуты, сменились ПРАВИЛА и форма `swarmAdapt`
 *  (рецепт Роя, рост покрова, волна по рецепту); список перезакреплён без правок.
 *  35 — VET-6: членство и порядок не тронуты, сменились ПРАВИЛА УРОНА сетевой партии —
 *  сила ветерана стала правилом хоста, и сеть её не получает; список перезакреплён без
 *  единой правки.
 *  36 — ROADS-8: членство и порядок не тронуты, сменились ПРАВИЛА боя на дороге —
 *  победитель продолжает марш (`Fleet.resume`); список перезакреплён без правок.
 *  37 — сеть Роя: ЧЛЕНСТВО — добавлен `swarmNet` строго между `swarmMemory` и
 *  `swarmAdapt`: память рождает наблюдение, сеть кладёт его свидетелю, адаптация читает
 *  знание части.) */
const PINNED_FOR_VERSION = '37';

/** Граф модулей версии `PINNED_FOR_VERSION` — идентификаторы В ПОРЯДКЕ ВЫПОЛНЕНИЯ.
 *  Не алфавит и не набор: порядок здесь и есть предмет договора (инвариант #6). */
const PINNED_MODULE_IDS = [
  'sector',
  'planet-type',
  'tax',
  'economy',
  'movement',
  'hero',
  // CORE-PARITY: провайдеры `hero.effect.<type>` стоят сразу за героем — они вешают
  // хуки `combat.damage` (аура), а место в конвейере значений и есть предмет договора.
  'heroEffects',
  'diplomacy',
  'espionage',
  // Боевая семья, разложенная по швам шины: orbital штампует орбиту до combat,
  // а свой залп отрабатывает до artillery.
  'orbital',
  'combat',
  // PERK-3.1: надбавка за пережитые бои читает счётчик, который начисляет `combat`.
  // На число место не влияет — вклады в `combat.damage` перемножаются, — но членство
  // графа это меняет, и версия манифеста поднята именно поэтому.
  'veteran',
  'intercept',
  'capture-on-arrival',
  'salvage', // EVT-2: перед construction — оба слышат station.destroyed
  'construction',
  'arsenal-sync',
  'station',
  'technology',
  'scientist',
  'steward',
  'faction',
  'market',
  'army',
  'fleet-ops',
  // PERK-3.2: бросок промоушена обязан пройти ДО того, как авто-сбор унесёт свежие
  // корабли из гарнизона во флот — оба слушают `unit.built`.
  'promotion',
  'auto-rally',
  'shuttle',
  'capital',
  'standing-orders',
  'instant-repair',
  'fleet-repair',
  'forced-march',
  'pve',
  'fleetBrood',
  'swarmMemory',
  'swarmNet',
  'swarmAdapt',
  'swarmJournal',
  'missionFacts', // задачи забега 2026-09-24: удержание, потери, беженцы
  'victory',
  'visibility',
  // 'division' СНЯТ ОСОЗНАННО (H4-REVERT, 2026-07-31), не потерян при правке.
  // Игра вернулась к системе отдельных наземных юнитов: armyModule возит UnitStack'и
  // между гарнизоном и трюмом, combatModule разрешает десант — этот путь никогда не
  // исчезал, дивизии стояли ПАРАЛЛЕЛЬНО ему. Прежде чем вписывать 'division' обратно,
  // прочти причину отката в docs/backlog.md: возврат строки откатывает решение
  // владельца, а не чинит опечатку.,
  'effects',
  'seatClaim',
];

describe('сторож манифеста', () => {
  it('закреплённый список относится к текущей MODULE_MANIFEST_VERSION', () => {
    // Подняли версию — значит граф изменился: перезакрепите PINNED_MODULE_IDS ниже
    // и обновите PINNED_FOR_VERSION.
    expect(MODULE_MANIFEST_VERSION).toBe(PINNED_FOR_VERSION);
  });

  it('состав и порядок DEV_MODULES не менялись без подъёма версии манифеста', () => {
    const ids = createKernel(DEV_MODULES).manifest.modules.map((m) => m.id);
    // Правили DEV_MODULES? Поднимите MODULE_MANIFEST_VERSION (scenario.ts) и перенесите
    // новый порядок сюда — иначе живые матчи молча доиграются на другом графе модулей.
    expect(ids).toEqual(PINNED_MODULE_IDS);
  });

  it('свежий матч штампует эту версию в state.version.manifest', () => {
    // Связь между константой и тем, что реально ложится в снапшот и проверяется на
    // загрузке: разорвись она — сторож стерёг бы пустоту.
    const room = createDevMatch(loadShippedData(), { now: () => 1000, time: 1000 });
    expect(room.state.version.manifest).toBe(MODULE_MANIFEST_VERSION);
  });
});
