# Бэклог-кирпичики — как делим работу

> Гранулярные, назначаемые задачи. **1 кирпичик ≈ 1 PR ≈ 1 сессия Claude**, в
> пределах **одной зоны** (пакет / модуль / данные) — чтобы параллельная работа не
> конфликтовала. Регламент — `CONTRIBUTING.md`; высокоуровневый план —
> `roadmap.md` и `deep-technical-roadmap.md`; что уже готово — `state.md`.

## Как взять кирпичик

1. Возьми кирпичик со статусом ⏳ (не 🔒 — у того ещё не закрыты зависимости).
2. Застолби его (issue/доска), помечен зоной — чтобы не пересечься с напарником.
3. Ветка от `main` → код + тесты → **`pnpm run check`** зелёный → PR в `main`.
4. **Один кирпичик — одна зона.** Не бери два кирпича в одном файле одновременно.
5. Закрыл — отметь ✅ здесь и обнови `state.md` (в том же PR).

## Зоны

`[core]` `packages/shared-core/src/modules` (+ `data`) · `[act]` `action-layer` ·
`[srv]` `server` · `[cli]` `client` · `[proto]` `prototype` · `[data]` `data/*.json` ·
`[docs]` `docs` · `[sec]` CI/сканеры (`.github/workflows/security.yml`, `docs/security`, конфиги сканеров) ·
`[ops]` эксплуатация/хостинг (запуск сервера, автообновление, инфраструктура плейтестов)

Словарь зон машинно проверяется гейтом (`scripts/docs-check.mjs`) — вместе с
целостностью всех `.md`-ссылок в docs/: новая зона сначала добавляется сюда и в
`ZONES` скрипта, иначе `pnpm run check` красный.

## Статусы

✅ готово · ⏳ можно брать · 🔒 ждёт зависимость (в скобках — какую)

> **Фундамент готов** (не переделывать): микроядро/шина/хуки/манифест, RNG+golden,
> модель времени, экономика, карта+движение (incl. `fleet.stop`), секторы, бой
> (орбита/десант/захват/бомбардировка/ПВО), здания, флот⊕армия+транспорт, **типы
> планет**, **victory-модуль** (data-driven очки), **`visibleState`** (туман войны),
> каркас **action-layer**, multiplayer-slice, **F6 туман на рассылке**, играбельный прототип,
> **дипломатия в ядре** (D1+D2+D3), **предматчевые технологии** (C3), радарные бонусы
> тех/фракций (A2), **шпионаж-фундамент** (SPY-1). Тесты зелёные (актуальный счётчик — в
> шапке `state.md`). Блоки A, B, C, D закрыты целиком — ядро (этап 1) собрано.

---

## Блок A · Ядро: туман войны / видимость `[core]`

- **A1** ✅ Проекция `visibleState(state, viewerId, data)` — identify (1 прыжок) +
  radar-сигнатуры (`◆S/M/L` по `signature`), прячет чужое/расписание; 7 тестов incl.
  anti-leak по JSON; поля `signature`/`radarRange` в схеме. Прообраз в прототипе.
- **A1m** ✅ Память последнего увиденного (вариант B): `GameState.fog` (per-player
  снимки), `visibilityModule` обновляет на `time.advanced`/`planet.captured`/`fleet.arrived`;
  `visibleState` отдаёт серое «last known» (`remembered[]`) вместо unknown. 3 теста.
- **A2** ✅ Радарная дальность от зданий: **радар-постройка с 3 уровнями** в `data/buildings.json`
  (`radarRange` 180→300→420), `radarRange` **уровневый** (`BuildingLevelSchema`). Радар ловит по
  **физическому расстоянию** (евклидово по `position`), а не по прыжкам графа — близкий в космосе
  узел виден, даже если недостижим по лейнам. Вклад тех/фракций — **данными, не kernel-хуком**
  (проекция чистая и живёт вне кернела): `radarRangeBonus` в `TechnologyEffects`/`FactionPassives`,
  `coverageFor` множит reach каждого радара игрока (зданий и кораблей) на (1+Σбонусов); 3 теста.
  Сенсорная дальность «не только радара» покрыта моделью данных: `radarRange` может нести
  **любое** здание (уровнево) и любой юнит — отдельный механизм не нужен.
- **A3** ✅ Разведка флотом: транзитный флот опознаёт ближайший узел по ходу (`fleetNode`),
  память (`visibilityModule`) фиксирует пройденные узлы на `time.advanced`/`fleet.arrived`;
  закреплено тестом «маршрут A→B→C оставляет B в памяти + `remembered` в проекции».
- **A4** ✅ Хелпер `isVisibleTo(state, viewer, {planetId|fleetId}, data)` — «видим ли объект
  на уровне identify», ровно то правило, по которому режет `visibleState`: своё — всегда,
  radar-blip / память / неизвестный id — false (fail-secure). Туман opt-in: хост без
  тумана хелпер просто не зовёт. 2 теста.
- **A5** ✅ Общая видимость союза/коалиции: `coverageFor` объединяет покрытие по «блоку
  зрения» (зритель + все, с кем `alliance`; коалиция = эта же стойка), поэтому союзники
  пулят разведку — миры, контент, флоты и бои. ПРЯМОЕ соседство, не клика победы и не
  связная компонента: при A–B и B–C (A–C война) A и C не видят друг друга через общего
  друга. `pact`/`peace` не делят. Делится только карта — казна/техи/герои приватны.
  Одна точка правки, поэтому проекция, память тумана, фильтр событий и скан угроз
  согласованы; сервер не тронут. 6 тестов (обе мутации проверены).

## Блок B · Ядро: фракции `[core]` `[data]`

- **B1** ✅ Расширить `FactionDef`: стартовый лоадаут (`startingLoadout`: ресурсы/флот/
  гарнизон/постройки) + `uniqueUnits` + `passives` (prod/speed/combat, зеркало tech-effects)
  в схеме; `data/factions.json` наполнен 2 фракциями (vanguard/swarm), все ссылки
  на юниты/постройки валидны. Тест `factions.test.ts` + bundle-валидация. _(necromancer вырезан — B4.)_
- **B2** ✅ `factionModule`: пассивы фракции через хуки `economy.production`/`fleet.speed`/
  `combat.damage` (зеркало tech-effects); мягкая деградация без модуля. 5 тестов.
- **B3** ✅ `factionStart(data, faction)` — чистая детерминированная сборка старта из
  `startingLoadout` (казна/флот/гарнизон/постройки, hp из данных); 4 теста.
- **B4** ❌ **вырезано:** `reanimationModule` (некромант `raise_fallen` поднимал павших как
  `reanimated_drone`) удалён вместе с фракцией necromancer — модуль, юнит, событие и тесты сняты.

## Блок C · Ядро: древо технологий `[core]` `[data]`

> ✅ **Сведено и проверено** (ветка devin `session-tech-tree`, влита в рабочую линию,
> гейт зелёный). Блок закрыт: C3 доехал последним.

- **C1** ✅ Схема `TechnologyDef` + `data/technologies.json` (анлоки/бонусы/стоимость/время).
- **C2** ✅ `technologyModule` + действие `technology.research`, состояние анлоков (`Player.technologies`).
- **C3** ✅ Предматчевый выбор: `SlotAssignment.technologies` (buildFromMap) — стартовые
  технологии выдаются посаженному в слот игроку как `completed` (бонусы/анлоки работают
  с первой секунды через существующие хуки); неизвестный id валит сборку (fail-secure),
  дубликаты схлопываются; 2 теста. Prerequisites намеренно не проверяются — стартовый
  кит может дарить узел из середины дерева.
- **C4** ✅ Бонусы тех на хуках (производство/скорость/бой) + гейт постройки юнитов/зданий.

## Блок D · Ядро: дипломатия `[core]`

- **D1** ✅ Состояние дипломатии в `GameState`: `diplomacy?: Record<pairKey, DiplomaticStance>`
  (`war`/`peace`/`pact`/`alliance`, симметрично, публично — не режется туманом). Чистые
  примитивы `state/diplomacy.ts` (`pairKey`/`getStance`/`setStance`/`DEFAULT_STANCE='war'`,
  дефолт сохраняет текущее FFA: разные владельцы = враги без модуля). В `delta`-META; 10 тестов.
- **D2** ✅ `diplomacyModule` (`modules/diplomacy.ts`): действие `diplomacy.declare
{target, stance}` — **эскалация унилатеральна** (только к войне по оси
  alliance→pact→peace→war; смягчение → `E_CONSENT_REQUIRED`, иначе жертва выключала бы
  чужой бой односторонним «миром»); событие `diplomacy.changed`. Провайдит capability
  `diplomacy` (`getRelation`, контракт в `state/diplomacy.ts`), которую `combat.isHostile`
  читает с фолбэком на прямой D1-стенс (мягкая деградация); маппинг `stanceToRelation`
  (war→hostile, peace/pact→neutral, alliance→ally). Схема payload'а в гейте; в
  `DEV_MODULES`. 8 тестов (вкл. e2e peace→declare→бой).
- **D3** ✅ Consent-протокол смягчения — то же действие `diplomacy.declare`, взаимно:
  первое дружественное объявление записывает **оффер** (`GameState.diplomacyOffers`,
  направленный `from>to`; станс не меняется, событие `diplomacy.offered`), встречное
  объявление того же станса **коммитит** пару (`diplomacy.changed`) и чистит офферы;
  любая эскалация аннулирует переговоры (инвариант: живой оффер всегда строго дружелюбнее
  текущего станса). Офферы **приватны паре** — `visibleState` вырезает чужие переговоры;
  в `delta`-META. Заменил D2-заглушку `E_CONSENT_REQUIRED`; `E_ALREADY_OFFERED` на повтор.
  9 тестов (модуль + примитивы + туман + дельта).
- **D4** ✅ `[proto]` Миграция прототипа на ядровый `diplomacyModule` завершена. Игровая
  часть была закрыта NETP0-5 (те же D2/D3-семантики на хелперах ядра, UI-офферы, бот по
  favour-шкале); финальный шаг — **дедуп**: прототипная реализация `diplomacy.declare`
  удалена из `game.ts`, в `MODULES` подключён **ядровый** модуль (D2-эскалация, D3-consent,
  `E_BOT_ALLIANCE`, свип офферов на `player.eliminated`, capability `diplomacy`). Посев
  `peace` в `newGame` не тронут (чистый D1-стейт). Диффы кодов: same-stance →
  `E_SAME_STANCE` (был `E_ALREADY`), кривой target → `E_BAD_PAYLOAD` (был `E_BAD_TARGET`),
  `stance` обязателен (дефолт 'war' остался в билдере `declareWar`); UI на коды не
  ветвится. Одна реализация `diplomacy.declare` на весь репозиторий.

- **E1** ✅ Зод-схемы на каждый тип действия (закрыт SV-1.2:
  `shared-core/actions/payloadSchemas.ts` инжектится в гейт как `payloadValidator`).
- **E2** ✅ Стор receipts с интерфейсом под персистентность (закрыт сервером:
  `ReceiptStore` в `store/` — in-memory + Postgres, durable receipts переживают рестарт).
- **E3** ✅ Интеграционный тест: невалидное / повтор по тому же id / несанкц.
  действие → безопасный отказ с кодом (абьюз-e2e зелёный, см. `state.md`).

## Блок F · Сервер (Этап 3) `[srv]` _(опирается на `[act]`)_

- **F1** ✅ Скелет Fastify + health-роут (SV-0.1: `/health`·`/ready`·`/metrics`, pino, drain).
- **F2** ✅ Postgres JSONB: load/save `GameState` + квитанции (`store/postgres.ts`),
  строгий commit-before-broadcast.
- **F3** ✅ (переопределён): будилка по `scheduled`-событиям — v1 без Redis/BullMQ
  (`clockDriver.ts`, in-process); durable-эволюция — **pg-boss** на мульти-процессе
  (решение в `tech-stack.md`).
- **F4** ✅ WebSocket-слой: пуш per-player дельт (`wsServer.ts` + `protocol.ts`).
- **F5** ✅ Последовательная обработка действий: per-room актор-mailbox на durable-пути
  (double-spend невозможен; per-player rate-limit сверху).
- **F6** ✅ Фильтр видимости **перед** отправкой клиенту: `MatchRoom` рассылает
  per-player дельты от `visibleState` (+ сигнатуры/память отдельными полями), события
  тоже фильтруются по видимости; e2e-тест «враг скрыт + нет утечки `red_1` по проводу».
  Дальше: AOI-оптимизация.
- **F7** ✅ JWT в WS-handshake (SE-0.1: join-токен в `?token=`, `auth.ts`, пин алгоритма,
  Origin-allowlist; opt-in через `AUTH_JWT_SECRET`).
- **F8** ✅ Persist + драйвер пробуждения в `packages/server/src/main.ts` (паритет с
  прото-сервером, который имел их с PA-4.1). `persistence.ts` (`createStores`: Memory по
  умолчанию, Postgres по `DATABASE_URL` + `migrate`; `snapshotOf`), `clockDriver.ts`
  (`msUntilNextEvent`→`tick`, cap `MAX_DELAY`), `main.ts` wired (observe→persist+receipt,
  rehydrate на старте, graceful shutdown). **Побочно — реальный баг-фикс:** `seq`
  сбрасывался в 0 при рестарте, из-за чего optimistic-by-seq store дропал пост-рестартные
  сохранения (`WHERE seq <= EXCLUDED.seq`), пока счётчик не догонит — добавлен
  `MatchRoom.initialSeq`, прокинут в **оба** сервера (`main.ts` и `netserver.ts`).
  5 тестов (`f8-persistence.test.ts`: persist/resume, seq-restore + guard, драйвер
  advance/idle). _Оговорка:_ save — после commit (fire-and-forget), не строгий
  commit-до-broadcast risk14 (тот — F2/SV-1.1). Детали — `infra-sizing-roadmap.md`.
  ✅ Закрыто: same-instant runaway теперь даёт ограниченный частичный адванс (часы не
  двигаются, `inf` остаётся) вместо клина — `E_ADVANCE_OVERFLOW` из кода убран
  (тест `advanceTo.test.ts`).

## Блок G · Клиент (Этап 4) `[cli]` _(параллелен серверу)_

> **Переориентировано на PWA-first веб-клиент** — детальные бирки CP0–CP7 в
> [`cross-platform-roadmap.md`](cross-platform-roadmap.md). Соответствие: G1→CP1,
> G2→CP4 (Pixi вместо Skia), G3→`@void/client`+CP1, G4→CP3. Бирки G1–G4 ниже —
> исходный (RN) вариант.

- **G1** ✅ _(в PWA-варианте, G1→CP1)_ Оболочка — Vite-shell вместо RN (решение
  `cross-platform-roadmap.md`); WS-подключение — CP1.1 (`net.ts`/`MultiplayerClient`);
  **graceful reconnect — CP1.4**: авто-реконнект с экспоненциальным бэкоффом, ресинк
  полным `welcome`, ограниченная очередь неотправленных интентов с флашем после
  реконнекта (без дублей), дроп немонотонной дельты → форс-ресинк. Фаза CP1 закрыта.
- **G2** 🔒(G1) Skia-рендер карты (зум / скролл / culling).
- **G3** ✅ (в `@void/client`: `applyDelta`-применение серверных diff'ов уже зашиплено; RN-вариант не нужен).
- **G4** ✅ Превью через `shared-core` («если атакую — что будет?»): ядровая половина —
  `previewBattle` (см. ONB-6), уже играбельна в прототипе. Client-обёртка —
  `createBattlePreviewModel` в `@void/client` `matchHud.ts`: тот же fail-secure
  `{ok:false,code}`-паттерн, что и у `createSelectionModel`/`createBattleModel`;
  форкаст для десанта пристыкованного флота против гарнизона враждебного (data-driven
  `sectorKinds.capturable`) мира под ним — фог структурно безопасен (пристыкованный
  флот уже опознал свой мир, скрывать нечего). Отказы: чужой/отсутствующий флот, флот
  не пристыкован (в пути / на трассе / уже в бою), свой/некапчурабельный мир, пустой
  десант или гарнизон. Тесты: `matchHud.test.ts` (все гейты + одна прогоняемая через
  реальный `previewBattle` победная сцена + JSON-сериализуемость).

## Блок H · Прототип / играбельность `[proto]`

- **H1** ✅ `victoryModule` подключён в кернел прототипа (`game.ts` MODULES — а значит и в
  `netserver`/плейтест); `checkEnd` читает авторитетный `state.match` (победа/поражение/ничья
  по domination/elimination/score/timeout) вместо хардкода `CRIMSON`/`HOME`. Баннер срабатывает
  в обоих режимах. _Заметка:_ domination считает долю от ВСЕХ узлов (вкл. некапчурные void) —
  на текущей карте реально только elimination; денонатор «только капчурные» — отдельный кирпич.
- **H2** ✅ Индикация огня ПВО (+залповый режим, два яруса): **орбитальное** ПВО
  (здания) бьёт раз в игровой час, **ближняя** (юниты гарнизона) — раз в 15 игровых
  минут четвертью часовой ставки; сетки по мировому времени, перецеливание на каждом
  залпе, нырок между залпами безнаказан. Ядро объявляет каждый залп событием `aa.fired
{planetId, owner, fleetId, by, damage}` (до применения урона — чтобы клиент дорисовал
  залп, даже если цель им же и уничтожена; жертве в тумане не уходит — фильтр по
  location/owner); прототип рисует трассер земля→орбита (штрих ползёт вверх) со вспышкой
  на цели, ~0.7с. Бонус: фазы боя различимы — орбита = красные расходящиеся кольца
  «⚔ орбита», десант = янтарный пунктир + «линия фронта» под миром, «⚒ десант».
- **H3** ✅ Предматчевый выбор **фракции** (setup-экран): пикер из 4 лор-домов
  (`data.factions`: blue/red/amber/violet), каждый — **чисто пассивный бонус к экономике
  или юнитам** (пока что): Azure +12% экономика · Crimson +10% урон · Amber +15% скорость
  флотов · Violet +5%/+5%. Применяет ядровый `factionModule` (добавлен в прототип-кернел)
  через те же хуки, что и технологии; человек выбирает дом, ИИ-места разбирают оставшиеся
  (имя места = имя дома, цвет остаётся за местом). Карточка игрока показывает дом + его
  пассив. `factions.test.ts` (3: каталог economy-or-units, кернел+сид, +12% реально
  двигает казну) + CDP-verify. _(Выбор стартовых технологий — уже покрыт советом учёных
  C3/`SlotAssignment.technologies`; отдельного экрана не требует.)_
- **H4** 🗑 Конструктор наземной армии: пехота в 3 вариантах (`militia` — дешёвое мясо,
  🗑 **СНЯТО (H4-REVERT, 2026-07-31).** Игра вернулась к системе отдельных наземных
  юнитов: они строятся как обычные юниты, стоят в гарнизоне и ездят в трюме флота
  (`armyModule`), десант разрешает `combatModule`. Текст кирпича ниже оставлен как
  запись о проделанной работе — но описанной механики в игре БОЛЬШЕ НЕТ.
  `heavy_infantry` — щит обороны, `special_forces` — элита с ПТ-средствами) + танк;
  матрица контр в `GROUND_ROSTER` (танки бьют любую пехоту, спецназ — единственная
  пехота, опасная танкам). Именные офицерские дивизии `OFFICER_TEMPLATES` — ГОТОВЫЕ
  залоченные шаблоны со встроенным офицером (мобилизация прикрепляет его сервер-
  авторитарно: `division.mobilize {officer:true}`); ручное назначение офицеров убрано.
  Окно «⚙ Конструктор дивизий» (Stellaris-style): правка слотов/имени своих 3 шаблонов
  ДО постройки (`division.rename`, trim+cap 24), превью «Урон по:» каждому роду войск
  (матрица видна барами), синергии (окоп ≥3 тяжей, рейд ≥2 спецназа, людская волна ≥4
  ополчения + прежние), стоимость иконками; панель мира — только выбор шаблона и
  «Мобилизовать» (дивизия — снапшот). Back закрывает окно. +3 теста (26 в division),
  миграция ростера по 4 тест-файлам; RU/EN полные. Гейт 1030 зелёный. **Порт на
  канонический сервер:** `state/groundCombat.ts` (FND-4, до этого) получил Officer-бонусы;
  дивизии/шаблоны/наземный тик-бой перенесены в `@void/shared-core`
  (`data/formations.ts` + `modules/division.ts`, `Division`/`divisions`/`divisionSeq`/
  `groundBattles` — формальные поля `GameState`, `Player.divisionTemplates`) и подключены
  в `packages/server/src/scenario.ts`'s `DEV_MODULES` — `division.mobilize/template/
  rename/load/unload` теперь играбельны на настоящем многопользовательском сервере
  (`main.ts`), не только на прототип-хосте (`netserver.ts`, у которого свой кернел из
  `game.ts` — уже мог их раньше). Аддитивно: наземный бой дивизий не трогает легаси-
  гарнизон/`combat.ts` (та же граница, что и в прототипе). Прототипная реализация
  (`prototype/src/division.ts`/`groundcombat.ts`/`formations.ts`) НЕ тронута — осталась
  отдельной, уже проверенной копией (сознательное дублирование, не объединено ради
  минимального риска для живой фичи). 15 новых тестов в shared-core.
- **H5** ✅ Шпионаж играбелен (SPY-1 → прототип): ядровый `espionageModule` в `MODULES`
  (и в netserver), билдер `spyOn`; UI — «🕵 казна»/«🕵 флоты» в ростере дипломатии,
  «🕵 Разведать мир» на карточке чужого мира (в т.ч. по памяти «LAST KNOWN»); intel-окна
  пробивают клиентский туман (`planet` → identify+память, `fleets` → флоты цели на карте,
  `treasury` → строка казны с остатком окна); тосты `intel.stolen`/`espionage.failed`.
  4 e2e-теста.
- **SPY-2** ✅ `[core]` Контрразведка: каждый оплаченный `espionage.spy` дополнительно
  бросает ОБНАРУЖЕНИЕ для жертвы (пайплайн `espionage.detect`, база 0.5 после провала /
  0.25 после чистого успеха, кламп [0,1]; бросок всегда — форма RNG-потока не зависит
  от исхода). Обнаружение → `espionage.detected` адресовано ЖЕРТВЕ (`owner`): при
  провале в payload есть `spy` (агент пойман с поличным), при успехе — только `kind`
  (утечка замечена, вор неизвестен); шпиону о провале контрразведки не сообщают.
  Прототип: тост «🛡 Контрразведка…» жертве; бот, поймавший агента, роняет одобрение
  на `FAVOUR_SPY_CAUGHT_HIT` (=20, botDiplomacyModule слушает событие). +5 тестов
  (4 core + 1 proto favour-e2e).
- **SPY-UX** ✅ `[proto]` Весь шпионаж в одном месте (фидбек плейтеста 2026-07-18):
  третья вкладка «Шпионаж» в Дипломатии — активные окна интела с таймерами (planet-окно
  тапом летит к миру), операции «казна/флоты» по каждому противнику (те же `.dp-spy`
  обработчики, что и в ростере), сессионный журнал попыток (успех/провал/контрразведка,
  ≤30 записей); ленивый 5с-рефреш таймеров; на телефоне заголовок окна уступает место
  вкладкам. Playwright-e2e: попытка → провал в журнале → успех → окно «⏳ 24ч».
  Дальше: **SPY-3** перехват пингов/приказов (metagame
  §шпионаж) — ещё не нарезан; цена контрразведки как ЗДАНИЕ/тех — через хук
  `espionage.detect` данными, отдельный кирпич по мере надобности.
- **H6** ✅ Локализация прототипа (1 язык = 1 файл): `prototype/src/i18n.ts` —
  `t()`, `tData(name)`, `localizeStaticDom()` (boot-проход по статичному HTML:
  `data-i18n`/`-title`/`-ph`/`-aria`) и `setLocale` (persist в
  `localStorage['vd.locale']`, смена языка перезагружает страницу — ни один рендерер
  не остаётся на старом языке). Переключатель `#clang` на экране приветствия. Весь
  игровой UI прошёл через `t()`/`tData()`: панель флота/мира, дипломатия, чат, биржа,
  техи, конструкторы героя/корабля/дивизий, mock-кабинет корпорации, баннер победы,
  коды отказа ядра (`errText`). Браузер-проверено на обоих языках по каждому экрану.
  ⚠️ **Схема ключей с тех пор сменилась — см. LOC-1 ниже:** msgid'ом была сама русская
  строка, а локали лежали в `prototype/src/locale/{ru,en}.ts`; теперь это ключи
  (`t('err.no-capacity')`) в корневой папке `/localization`.
- **LOC-1** ✅ `[proto]` `[docs]` **Локализация на КЛЮЧАХ, единая папка `/localization`
  (этап 1 из 2).** Прежняя схема «msgid = каноническая русская строка» связывала текст
  с кодом намертво: правка формулировки по-русски осиротляла английский перевод (ровно
  это случилось в PR #371/#372), а сам текст был размазан по `main.ts`. Теперь текст
  живёт **только** в `/localization/{ru,en}.ts` — плоские карты `ключ → текст`; ключ =
  `домен.сущность.аспект`, точки — иерархия, kebab-case внутри сегмента
  (`err.no-capacity`, `data.metal-mine`, `hero.ability.corridor.desc`). Правило целиком
  — в корневом `CLAUDE.md` §«Локализация». Движок (`prototype/src/i18n.ts`): выбранная
  локаль → **русский как источник** (непереведённый ключ виден по-русски, а не как
  голый ключ) → сам ключ (заметная опечатка); `tData()` строит ключ детерминированным
  слагом (`dataKey()`), поэтому таблица «имя данных → ключ» не нужна; `localizeStaticDom`
  читает ключ из ЗНАЧЕНИЯ атрибута (`data-i18n="hub.play"`) и продолжает понимать
  старую форму. Мигрированы **словарные** домены, где ключ выводится из идентификатора
  кода: `err.*` (таблица `ERR_RU` на 60 записей УДАЛЕНА — ключ считается из кода отказа,
  `E_NO_CAPACITY` → `err.no-capacity`), `data.*` (88 имён), `hero.*`, `ship.*`,
  `diplo.*`, `market.*`, `tech.branch.*`, `hud.resource.*` — **169 ключей**. Гейт
  (`i18n.test.ts`, 7 тестов): паритет наборов ru/en, ключ из кода заведён, ключ локали
  не осиротел (динамические семейства — явным списком с объяснением, кто их строит),
  в EN нет кириллицы, у каждого юнита прототипа есть переведённое имя, и мост
  совместимости только сокращается. Побочно закрыты дыры, которые старый гейт не видел
  (он смотрел лишь `main.ts`+`game.ts`): 20 кодов отказа героев/дерева навыков и 7 строк
  песочницы никогда не имели английского — переведены.
- **LOC-2** ✅ `[proto]` **Этап 2: домиграция остальных вызовов на ключи — ЗАКРЫТ.**
  Мост совместимости (`/localization/legacy/`) снят вместе с веткой в `i18n.ts`:
  русского текста в коде прототипа не осталось, `t()`/`tData()` принимают только
  ключи, промах виден как сам ключ. Итог: **1529 ключей** в `ru.ts`/`en.ts` против
  1185 записей моста на старте. Работа шла доменами (одна секция ≈ один PR), гейт
  держал корректность на каждом шаге — список доменов ниже.
  - ✅ **Статичная разметка** (`build.mjs`): все **143** слота `data-i18n`/`-title`/
    `-ph`/`-aria` в 118 элементах переведены на ключ в ЗНАЧЕНИИ атрибута — **133 ключа**
    в доменах `welcome.*`, `hub.*`, `rail.*`, `win.*`, `setup.*`, `seatpick.*`,
    `speed.*`, `upd.*` (английский взят из моста, ни одна строка не потеряла перевод).
    Текст из разметки УБРАН (узел пуст, текст приходит из `/localization`), ветки моста
    «ключ из текста узла» удалены из `localizeStaticDom()`, мост сократился на 81 запись
    (1185 → 1104). Гейт: новый тест «статичная разметка переведена ключами, старой формы
    не осталось» + проверка сирот расширена на `build.mjs` (7 → 8 тестов).
  - ✅ **Боковая панель матча** (секция `side panel`, `main.ts` L5085–5928): **172**
    вызова → **158 ключей** `side.*` — конвейеры стройки, оперативная группа, карточка
    флота и сводка армии, авиагруппа, бой и отход, бомбардировка/штурм с прогнозом,
    погрузка десанта, мир вне сенсоров, досье мира, вкладки гарнизона. Одинаковые
    русские слова в разных ролях разведены по контексту («десант» —
    `side.summary.troops` и `side.battle.side.landing`, «оборона» —
    `side.battle.defender` и `side.world.bonus.defense`). Мост: 1104 → 992.
  - ✅ **Досье объектов и кодекс** (секции `object dossiers` + `build/unit codex`,
    `main.ts` L5926–6407, плюс хаб справочника ONB-4): **116** вызовов → **116 ключей**
    `dossier.*` (здания, корабли, стройка в очереди и её выхлоп, вкладки гарнизона,
    характеристики флота и дивизии) и `codex.*` (строки характеристик карточки, теги
    здание/модуль/фитинг/корабль, 8 ключей `codex.hub.*` справочника). Одинаковое
    русское слово в разных ролях разведено по контексту («Флот» — `dossier.fleet.name`
    и `dossier.tab.ships.name`, «Атака» — `dossier.stat.atk.name` и `…datk.name`,
    «Корпус» — `dossier.stat.hull.name` и `codex.row.hull`). Статьи глоссария больше не
    держат копию текста в коде: `GlossaryArticle` отдаёт `titleKey`/`bodyKey`
    (7 статей → 14 ключей `codex.term.*`), у механики в поисковом индексе `title` пуст —
    ключ в стоге сена заставил бы «co…» совпадать со всеми статьями, а находятся они по
    двуязычным `tags` и локализованной подписи из UI. Мост: 992 → 892. Гейт: новый тест
    «статьи глоссария заведены ключами, а не текстом» (`titleKey`/`bodyKey` не проходят
    через разбор литералов, опечатка доехала бы до игрока) — 8 → 9 тестов.
  - ✅ Побочная находка первого домена закрыта: **5** русских атрибутов в `build.mjs`,
    которых не видел ни гейт, ни `localizeStaticDom` (`title` у профиля,
    рельсы-«Инструменты», смены эмблемы и кнопки песочницы, `aria-label="Закрыть"`),
    плюс найденный рядом заголовок окна эмблем помечены к переводу — английского у них
    не было и в мосте, он заведён вместе с ключом (`hud.crest.*`, `rail.toggle.*`,
    `hub.avatar.*`, `hub.emblem.*`, `hub.sandbox.*`).
  - ✅ **Онбординг** (ONB-1/2/3/5/7): четыре чистых модуля с копией перестали держать
    текст в коде — `intros.ts` (`IntroCard.title/body` → `titleKey`/`bodyKey`,
    11 карточек → 22 ключа `onb.intro.*`), `firstGoals.ts` (`Goal.label` → `labelKey`,
    4 цели), `onboardingTour.ts` и `firstMatchTour.ts` (у `SpotlightStep.copy` в доке и
    так стоял «locale key», но значением ехал русский msgid — 5 + 8 шагов →
    `onb.tour.hud.*` / `onb.tour.first.*`). Обвязка: 9 вызовов в `main.ts` (итог
    обучения и цели первой сессии, карточка первого контакта, сводка возвращения) и
    4 в `spotlightDom.ts` (шаг тура, «Далее», «Понятно», «Пропустить обучение») —
    `spotlightDom.ts` полностью сошёл с моста. **52 ключа**, мост 892 → 842. Побочно:
    у двух строк не было английского НИГДЕ (`onb.goal.fleet`, `onb.tour.first.fleet`) —
    они ехали в `t()` переменной из таблицы, гейт моста их не видел, и на EN игрок читал
    русский текст; переведены. Гейт: тест глоссария обобщён до «таблицы копии отдают
    ключи, а не текст» и покрывает все пять таблиц (GLOSSARY, INTROS, FIRST_GOALS, оба
    тура) — у всех ключ уходит в `t()` переменной и невидим разбору литералов.
  - ✅ **Командная панель флота** (`renderCmdBar`): **43** вызова → **42 ключа** `cmd.*` —
    приказы (курс, стоп, штурм, цель, каст, обстрел, слияние/деление, форс-марш,
    авто-штурм, дежурный вылет) парами «подпись + подсказка», режим огня артиллерии
    (4 режима с пояснениями) и подписи каста способности героя. Мост: 842 → 809.
  - ✅ **Экран настроек** (hub → «Ещё» → Настройки): **55** вызовов → **30 ключей**
    `settings.*` — радарная развёртка с прозрачностью, свои метки, компактный режим,
    цвета сторон (свой, нейтральные, палитра с «дальтоник»-вариантом и сбросом),
    графика (свечение, звёздный фон, счётчик FPS). Подписи тумблеров `вкл`/`выкл` и
    кнопка `ГОТОВО` — по одному ключу на все 10 переключателей. Мост: 809 → 782.
  - ✅ **Кабинет корпорации и войны альянсов** (AVA-C1/C2): **70** вызовов →
    **64 ключа** `corp.*` — создание и поиск корпорации, обзор с журналом, заявки,
    роспуск и выход, готовность корпорации и своя, полный жизненный цикл вызова AvA
    (ждёт ответа → набор ростера → заперт → завершён, исходы победа/ничья), лента и
    история, заглушки «Владения/Чат — скоро», карточка корпорации. Мост: 782 → 733.
  - ✅ **Дыра в гейте закрыта** (найдена на домене корпорации): разбор литералов читал
    только литерал сразу после `t(`, поэтому вызов `t(cond ? 'А' : 'Б')` был невидим
    целиком — через него уехала строка «идёт набор ростера», у которой английского не
    было НИГДЕ (на EN игрок читал русский текст; заведён `corp.war.roster-open`).
    `extractHiddenMsgids()` собирает кириллические литералы внутри `t(...)` с
    нелитеральным аргументом и кормит ими проверку моста; проверено мутацией.
    Остальные два таких литерала в репозитории (setup-оверлей) английский имеют.
  - ✅ **Журнал событий матча** (клиентский слой приказов): **55** вызовов →
    **54 ключа** — `log.*` (бой с фазой и потерями, изучение технологии, вахта
    Хранителя обоими режимами, шпионаж с контрразведкой, захват мира, дипломатические
    события, стройка, флот, сделка на бирже) и `war.confirm.*` (диалог «ОБЪЯВИТЬ
    ВОЙНУ?» с двумя поводами — мир дружественной фракции и транзит через мирные миры).
    Мост: 733 → 689.
  - ✅ **Штаб героев**: **50** вызовов → **45 ключей** `hero.hq.*` / `hero.tree.*` /
    `hero.abil.*` / `hero.fit.*` — карточки героев, дерево навыков (требования, цена,
    что открывает узел), способности и фиттинги с необратимостью установки. Вкладки
    штаба (`TABS`) переведены таблицей целиком: подписи рендерятся через `t(l)` из
    массива, мигрировать одну и оставить три русскими — рассинхрон. Мост: 689 → 654.
  - ✅ **Аккаунты и браузер матчей** (SES-2.5): **47** вызовов → **40 ключей** —
    `acc.*` (правила позывного и пароля, исходы регистрации и входа, истёкшая сессия,
    недоступный сервер, переподключение) и `browser.*` (карточка матча в ленте: день,
    игроки, слоты, цель, окно входа, статус; кнопки войти/восстановить/в архив с
    отказами, одиночный режим, пустые состояния). Мост: 654 → 616.
  - ✅ **Переписка и шпионаж** (вкладки окна дипломатии): **45** вызовов → **37 ключей**
    — `chat.*` (общий канал сессии и чат коалиции), `spy.*` (окна интела с таймерами,
    операции, журнал попыток) и `diplo.*` (окно, вкладки, фильтр человек/ИИ, сортировка
    ростера); хвост диапазона отдан своим доменам (`codex.close`, `codex.build-here`,
    `side.tiles.empty`). Четыре ярлыка интела переиспользуют `log.spy.*` — тот же текст
    и смысл, второй ключ означал бы два места для одного перевода. Мост: 616 → 592.
  - ✅ **«Верфь»** (единая вкладка оснащения): **38** вызовов → **37 ключей** `yard.*` —
    корпус и типизированные слоты под модули, предупреждения про живой арсенал и
    фиксацию лоадаута, разбивка стоимости и заказ, конструктор дивизий (слоты, доктрина,
    мобилизация), подсказки выбора цели для героя. Мост: 592 → 557.
  - ✅ **Дерево технологий** (TT-3.1): **32** вызова → **31 ключ** `tech.*` — требования
    узла, что он открывает, куратор ветки, рельса дней, четыре состояния и семь подписей
    действия. Куратор заведён как `tech.curator*`, а НЕ `tech.branch.*`: там уже лежат
    имена веток, и `tech.branch.lead` читался бы как ветка с id `lead`. Мост: 557 → 528.
  - Порядок в локалях приведён в чувство: ключи чужих доменов разложены по своим
    секциям, записи внутри секции отсортированы по ключу, отбивка нормализована;
    сортировщик проверяет, что меняется только порядок строк.
  - ✅ **«Хранитель»**: **26** вызовов → **26 ключей** `steward.*` — журнал вахты с
    прогнозом потерь, активная вахта обеими позами и срок возврата, гейт неизученного
    протокола, настройка передачи. Мост: 528 → 502.
  - ✅ **Коммуникации** (три экрана одной темы): **68** вызовов → **53 новых ключа** —
    `chat.win.*` (плавающее окно чата: каналы, настройки окна, штамп), `comms.*` (меню
    связи: объявление войны, одобрение бота с порогами, окна интела, предложения, две
    операции кражи) и `ping.*` (композер меток провинции). Ещё 13 вызовов ключей не
    завели — переиспользованы существующие (`chat.tab.*`, `chat.input.ph`, `chat.you`,
    `chat.jump`, `chat.members`, `log.diplo.offer*`, `log.diplo.sent`,
    `log.spy.kind.treasury`, `spy.op.fleets`, `settings.title`). Мост: 502 → 443.
  - ✅ **Биржа, конец матча и карточка игрока**: **47** вызовов → **43 ключа** —
    `market.*` (книга заявок, выставление лота, комиссия и эскроу), `end.*` (исход,
    опыт и новый уровень, итоговая таблица, реванш) и `card.*` (фракция, владения,
    очки с порогом победы, боевой счёт). Секция `market` в локалях слита в одну —
    у домена уже была секция под вкладки ресурсов. Мост: 443 → 404.
  - ✅ **Хвост: вызовов `t('русская строка')` больше НЕТ (0).** Последние **169** в
    23 мелких секциях `main.ts` и `sandbox.ts` → **166 ключей**: `threat.*`, `queue.*`,
    `div.*` (дивизии), `cargo.*`, `ai.*` (условия конца матча), `map.*`, `split.*`,
    `hint.*` (17 подсказок прицеливания), `steward.log.*`, `meta.*`, `arsenal.*`,
    `auth.*`, `setup.*`, `scipick.*`, `hud.*`, `net.*`, `back.*`, `tgt.*`, `upd.*`,
    `sandbox.*`, `fmt.*`. Ещё 17 вызовов переиспользуют существующие ключи, а
    `spy.hours-left` и `tech.hours` схлопнуты в общий `fmt.hours` — все трое держали
    один и тот же `'{n}ч'`. Мост: 404 → **228**.
  - ✅ **Таблицы-справочники на ключах.** 162 значения таблиц, которые уходят в `t()`
    ПЕРЕМЕННОЙ: в `main.ts` — группы дерева технологий и эффекты узла, ветви и хуки
    героев, вкладки и слоты «Верфи», ресурсы в родительном падеже и характеристики,
    позывные ботов, «Арсенал», рода войск, бонусы фракций, корпорация
    (`tech.group.*`, `tech.fx.*`, `hero.branch.*`, `hero.hook.*`, `yard.tab.*`,
    `yard.slot.*`, `res.of.*`, `stat.*`, `callsign.*`, `arsenal.*`, `form.*`,
    `setup.home.*`, `seatpick.bonus.*`, `corp.tab/role/audit.*`); в остальных
    модулях — ветви и 12 узлов прокачки, шаблоны формаций и синергии, архетипы
    героев, автоимя флота, офицеры дивизии, ресурсы и переключатели песочницы
    (`meta.branch/node.*`, `form.tpl/syn.*`, `hero.arch.*`, `fleet.size.*`,
    `ground.officer.*`, `sandbox.res/tog.*`). Мост: 228 → **96**.
    Побочно: **22** строки не имели английского НИГДЕ (ветви и хуки героев, бонусы
    фракций, «ИГРОК», все переключатели песочницы с подсказками) — та же слепая зона,
    значение уходит в `t()` переменной и разбору литералов не видно. Переведены.
    Намеренно не тронуты: `CHAT_BADWORDS` (список цензора, не текст для игрока) и
    `'—'` в составе дивизии (заглушка пустого списка).
  - ✅ **Игровой каталог прототипа** (`prototypeData.ts`): **80** русских строк на
    ключи — описания всех 25 технологий, шесть учёных совета, четыре фракции, четыре
    героя, способности, пассивки, узлы деревьев навыков и описания фитингов
    (`tech.node.*`, `sci.*`, `faction.*`, `hero.unit/ability/passive/tree/fit.*`).
    Ключ выводится из секции и id каталога, а не пишется руками. Имена модулей и
    фитингов приведены к английским, русский текст переехал в `data.*`: они идут
    через `tData()`, а `dataKey()` вырезает всё кроме `[a-z0-9]`, поэтому русское имя
    схлопывалось в ключ `data.` — перевод был недостижим в принципе.
    Главное: у **68** из 80 строк английского не было НИГДЕ — весь тираж дерева
    технологий, учёных, фракций и героев на английской локали читался по-русски.
    Переведены. Мост: 96 → **63**.
    _Опасение про «`name` под проверкой целостности данных» из прошлой записи не
    подтвердилось: `dataHash` стамповится боевым сервером над `data/*.json`
    (`buildStateFromMap`), а прототип его не ставит — `prototypeData.ts` под проверку
    не попадает._
  - ✅ **Мост снят.** `/localization/legacy/` удалён вместе с веткой в `i18n.ts`.
    Оставшиеся 63 записи были уже мертвы — их держал тест сирот, искавший msgid
    ПОДСТРОКОЙ. Гейт перестроен: два теста моста и разбор скрытых msgid'ов удалены
    вместе с ним, добавлен тест «в коде нет русского текста — только ключи» (русский
    литерал в `t()`/`tData()` теперь не переведётся ни при каких условиях), а тест
    сирот ищет ключ В КАВЫЧКАХ. Осознанно не тронуты: `testmode.ts` (дев-экран, его
    строки никогда не проходили через `t()`), поисковые алиасы `codexIndex.ts`
    (двуязычны по назначению) и `CHAT_BADWORDS`.
  - ⚠️ **Про счётчик «русских литералов» на будущее — это была верхняя граница, а не
    список работы.** Во-первых,
    он мерился разными скриптами: на коммите до домена «досье» прежняя методика давала
    «802», текущая — 702. Во-вторых (найдено при замере домена «онбординг»): шаблонная
    строка, ВНУТРИ которой стоит `${t('русская строка')}`, считается ещё раз как
    отдельный «русский литерал», поэтому миграция одного вызова снимает сразу две
    единицы — из 46 снятых этим доменом 39 пришлись на таблицы, а 7 были именно такими
    обёртками. В-третьих, часть остатка переводу не подлежит вовсе: 31 «литерал» в
    `codexIndex.ts` — это двуязычные поисковые алиасы (`'туман'`, `'асинхрон'`), они и
    должны остаться русскими. Сравнивать между собой можно только числа одной методики.
- **LOC-3** ✅ `[cli]` **Подключить `packages/client` к `/localization`.** Новый тонкий
  рантайм `packages/client/src/i18n.ts` (порт `prototype/src/i18n.ts`: `t()`/`tData()`/
  `lookup()`, детект локали через `localStorage`/`navigator.language` с фолбэком на
  русский-источник) — импортирует `/localization` напрямую (`../../../localization`),
  третьей системы не заведено. Мигрированы все хардкод-строки: `welcomeScreen.ts`
  (`ruStrings` → `defaultStrings`, собран из `t('welcome.*')`; `CALLSIGNS` — из
  `t('callsign.*')`, тот же порядок слов, что и раньше — детерминизм `nextCallsign`
  не сломан), `loadoutEditor.ts` (`STAT_LABELS` → `t('loadout.stat.*')`, новый ключевой
  домен), `main.ts` (~14 строк статуса/сети/ошибок → новый домен `client.*` + `err.no-nick`/
  `err.unknown-provider`). `welcomeScreen.ts` уже было i18n-driven (принимает
  `WelcomeStrings` бандлом) — просто источник бандла сменился с литералов на `t()`.
  `prototype/src/i18n.test.ts`'s `srcFiles()` расширен на `packages/client/src` — общий
  гейт (паритет ru/en, «ключ из кода заведён», «нет осиротевших переводов», «в коде нет
  кириллицы») теперь видит ОБОИХ потребителей `/localization`, не только прототип.
  Побочно найден и исправлен реальный, ранее невидимый баг: **16 задвоенных ключей**
  в каждом из `ru.ts`/`en.ts` (`tsc` никогда не парсил эти файлы как модуль до этого
  PR — прототип собирается esbuild'ом, не `tsc`) — часть дублей была честными
  копипаст-опечатками (`auth.google.soon` с текстом про Apple), часть — РАЗНЫЕ
  UI-подписи, случайно заведённые под ОДИН ключ (`tgt.add-wait` держал 5 разных
  текстов кнопок «Домой»/«Огонь»/«Штурм»/«Сюда»/«+1ч» — JS-семантика «последний
  побеждает» уже показывала игроку одну и ту же подпись на всех пяти кнопках).
  Исправлено МИНИМАЛЬНО и безопасно: оставлено последнее (текущее реальное поведение)
  значение каждого дубля — синтаксическая ошибка снята, счётчик ключей не изменился
  (1548 в обоих файлах), но UI-баг (не та подпись у 4 из 5 кнопок `tgt.add-wait` и
  подобные) НЕ переразведён по правильным отдельным ключам — это отдельная, более
  крупная [proto]-задача (нужно пройти каждый вызов `t('tgt.add-wait')` и т.п. и
  завести правильные различающиеся ключи), сознательно не взятая в этом PR. Тесты:
  182 файла, 1963 (+5 к предыдущему PR).
- **LOC-4** ✅ `[proto]` **Разъединить смёрженные ключи локализации — ПРОВЕРЕНО, работы нет.**
  Кирпич предполагал живой UI-баг: 16 ключей держали несколько смыслов, дедуп в LOC-3
  оставил последнее значение, значит часть подписей отрисовывается неверно («сейчас
  везде «+1ч»», «сейчас везде «атака»»). **Разбор показал, что вызовы в коде НИКОГДА не
  были слипшимися** — дубли были МЁРТВЫМИ строками в файле локали, а не общими call
  site'ами: скрипты миграции LOC-2 вписывали лишнюю строку с чужим значением рядом с
  правильной, а в объектном литерале побеждала последняя — правильная. Игрок неверной
  подписи не видел; `tsc` молчал, потому что файлы локалей тогда никто не типизировал.
  - Метод проверки (воспроизводим): миграция была заменой ЛИТЕРАЛА на КЛЮЧ, поэтому
    «форма» строки (всё, кроме литералов) сохранилась. Строки текущего `main.ts`
    сопоставлены со строками версии ДО миграции (`bae23d7^`) по форме и позиции
    литерала в строке, значение ключа сверено с русским оригиналом на том же месте.
    Итог по 16 ключам: **13 совпало автоматически, 4 разобраны вручную, расхождений 0.**
  - Четыре ручных случая: `fmt.hours` ×3 — на этих местах стояли `'{n}ч'`,
    `t('tech.hours')` и `t('spy.hours-left')`, то есть один и тот же текст, намеренно
    схлопнутый в общий форматтер; `auth.google.soon` — кнопка `cgoogle` зовёт про
    Google, `capple` про Apple, перепутано не было.
  - Отдельно сверены `sandbox.ts` (`sandbox.title`/`toggles`/`end-wars`/`wars-ended`/
    `no-wars`) и композер приказов (`tgt.add-wait` — только кнопка ожидания, «Домой»/
    «Огонь»/«Штурм»/«Сюда» зовут `tgt.step.home`/`tgt.step.fire`/`cmd.assault`/
    `tgt.step.here`). В `packages/client` ни один из 16 ключей не используется.
  - Что осталось от этой истории по существу: гарантия неповторения. Она уже стоит —
    тест «в исходнике локали нет дублей ключей» (`prototype/src/i18n.test.ts`, добавлен
    отдельным PR), проверяющий ФАЙЛ, а не собранный объект.
- **LOC-5** ✅ `[proto]` `[cli]` **Один рантайм локализации вместо двух копий.** LOC-3
  породил `packages/client/src/i18n.ts` — порт `prototype/src/i18n.ts`; два файла с
  одной и той же логикой (`detect`/`lookup`/`t`/`tData`) и без единой проверки на
  расхождение. Это уже стоило правки в двух местах вручную: страховку `tData()` (промах
  слага → поиск по самому аргументу, чтобы на экране совета учёных не светилось
  `sci.overseer.name`) пришлось вносить дважды. Логика переехала в
  **`localization/runtime.ts`** — рядом с текстами, которые она и ищет; обе копии
  удалены, все 7 импортов (`prototype/src/main.ts`, `sandbox.ts`, `spotlightDom.ts`;
  `packages/client/src/main.ts`, `welcomeScreen.ts`, `loadoutEditor.ts` + его тест)
  ведут на общий файл. Клиент получил то, чего в его копии не было: `hasKey()`,
  `setLocale()`, `localizeStaticDom()`. Реэкспорт `LOCALE_LABEL` оставлен, чтобы
  потребителю хватало одного импорта. Тест рантайма переехал вместе с кодом
  (`packages/client/src/i18n.test.ts` → `localization/runtime.test.ts`, +3 случая:
  `hasKey`, перевод имени данных через слаг, страховка `tData()` от приехавшего ключа);
  `vitest.config.ts` получил `localization/**/*.test.ts` — папка лежит вне `packages/`
  и `prototype/`. Гейт ключей (`prototype/src/i18n.test.ts`) не тронут: он сканирует
  `prototype/src` и `packages/client/src`, а в удалённых файлах ключей не было.
  Проверено сборкой обоих потребителей (esbuild-прототип 1326 KB и `vite build`), в
  бандле прототипа переводы на месте. Тесты: 182 файла, 1979.

- **REL-5** ✅ `[srv]` `[cli]` `[proto]` `[ops]` **Замок мест: посадочный билет на ник-логине
  (`SEAT_LOCK`).** Дыра релиза: dev-рукопожатие пускало кого угодно (`?player=p1` —
  любое место напрямую; `?nick=Vasya` — чужой ник = чужое место). Теперь транспортная
  опция `seatLock`: первый вход ника минтит тикет (`randomBytes(24)` base64url), в
  `AccountStore` ложится ТОЛЬКО sha256 (`bindSeatTicket` — первый bind атомарно
  выигрывает; Memory-карта + колонка `seats.ticket_hash` в PG, `ALTER … IF NOT EXISTS`,
  до-REL-5 ряды дозамыкаются при следующем входе владельца), плэйнтекст едет клиенту
  один раз в `welcome.seatTicket`; каждый следующий вход обязан предъявить `?ticket=`
  (constant-time сравнение) или 401; `?player=` под замком отклоняется. Клиент:
  `MultiplayerClient.onSeatTicket` → прототип хранит билет в localStorage
  (`void.ticket.<base>|<match>|<nick>`) и добавляет `&ticket=` при коннекте — нулевой UX
  (игрок ничего не видит). netserver: `SEAT_LOCK=1|true` + строка в баннере; compose —
  релиз-постура `${SEAT_LOCK:-1}`. Верификация: 5 e2e-тестов транспорта
  (`seatLock.test.ts`), стор-контракт Memory+Postgres 16 (включая миграцию старой
  схемы и adopt-путь), живой raw-ws прогон (минт → 401 без/с подделкой → реконнект тем
  же местом без ре-минта → `?player=` 401) и CDP-прогон РЕАЛЬНОГО браузерного клиента
  (3 захода: билет ложится → реконнект пускает → без билета заперт). Границы (честно):
  билет невосстановим с сервера (hash-only) — потерянный localStorage = владелец чистит
  ряд `seats`; второй девайс того же игрока требует перенести билет; archive-интенты
  match-браузера всё ещё доверяют нику (read-model, не игровое место). Гейт 1081.
- **REL-4** ✅ `[srv]` `[ops]` **Action-гейт включён на играбельном пути (netserver).**
  Прото-хост `prototype/netserver.ts` принимает `GATE=1|true` — комната получает тот же
  `ActionGate({payloadValidator: isValidActionPayload})`, что и боевой вход `main.ts`
  (зеркало serverConfig; баннер печатает `gate: ON/off`). В compose релиз-постура —
  `GATE` по умолчанию **ON** (`${GATE:-1}`; `GATE=0` — дев-откат к голым actions).
  Клиент менять не пришлось: `MultiplayerClient` самонастраивается по `welcome.gated`
  (REL-2 дал полные 42 схемы — ни одна механика не режется). Серверные драйверы
  (ИИ/Хранитель/стоячие приказы) идут через `room.submitAction` МИМО гейта — задумано:
  гейт стоит на проводе, не внутри хоста. Верифицировано вживую в обе стороны:
  gated — `welcome{gated:true, sessionId}`, голый `action` → `E_BAD_MESSAGE`,
  `action.v1`-конверт применён (delta); ungated — голый `action` применён (back-compat).
- **REL-3** ✅ `[ops]` **Сервер одной командой + отказоустойчивая инфраструктура.**
  `pnpm stack` (или `cd deploy && docker compose up -d --build`) поднимает ВЕСЬ стек:
  игровой сервер (existing distroless-образ: игра на `/`, WS-матч, `/health`) + Postgres.
  Отказоустойчивость: `restart: unless-stopped` на обоих (краш/OOM/ребут самовосста-
  навливаются), durable-матчи (снапшот в PG — рестарт РЕЗЮМИРУЕТ матч; volume
  `void-pgdata`), healthchecks (сервер ждёт healthy-PG; образ сам чекает `/health`),
  bounded-логи (json-file 10MB×3 — диск не переполнить), PG только на loopback.
  `deploy/README.md` — runbook: запуск/обновление/статус/бэкап+cron/восстановление/
  tmux-альтернатива/честные границы (один процесс — один хост, hot-standby = этап
  pg-boss; TLS — реверс-прокси). Верификация: compose config валиден; CMD образа
  (`node prototype/netserver.mjs`) прогнан вживую — `/health {ok:true}`, игра (1MB HTML)
  отдаётся, чистый шатдаун. (Полный образ-билд в песочнице недоступен — registry
  blob'ы режет прокси; Dockerfile — прежний, деплоенный и trivy-сканируемый в CI.)
- **REL-2** ✅ `[core]` `[proto]` **Полнота гейт-схем — вся игра играбельна через
  gated-провод.** Было: 27 типов в `payloadSchemas` — gated-сервер молча резал дивизии
  (`division.*`), подъём флота (`fleet.launch`), эскадрильи (`fleet.split`/`merge`/
  `engage`), Хранителя (`steward.*`), капиталь, стоячие приказы (`order.auto`/`scramble`)
  и рынок прототипа (`market.take`, `side`); `unit.build` терял `modules` (лоадаут
  «Верфи»). Стало: **42 типа** — полный интент-набор прототипа; `market.cancel` —
  union обоих хостов (`orderId`|`id`); `patrol.stamp` **намеренно не клиентский**
  (рантайм-штамп серверного драйвера — клиентский штамп заправлял бы своё крыло).
  Паритет закреплён `prototype/src/gateparity.test.ts`: сэмпл каждого интента через
  РЕАЛЬНЫЕ билдеры → `isValidActionPayload`; queue-типы и `patrol.stamp` — отвергнуты.
  +4 прото-теста, schema-тест ядра расширен. Гейт 1075 зелёный.
- **REL-1** ✅ `[proto]` **Цепочка приказов УДАЛЕНА к релизу** («пока убери» — решение
  2026-07-10). Вырезаны: `orderQueueModule` + `subscriptionModule` (кернел 28→26 модулей),
  `state.orders`/`state.subscribers`, все `order.enqueue/clear/pop/remove/block/retry/hold`
  билдеры и хелперы (`QStep`/`stepActions`/`popChainStep`/`waitStatus`/`chainOrderCount`/
  `loadHereActions`/`serverQueueActions`), клиентский план (`fleetQueues`/`driveQueues`/
  `drawChainPlans`/`chainEta`), весь UI очереди («Очередь приказов», «➕ строить», пипы
  плана, апселл подписки, стаб `vd.premium`), серверный драйвер `runServerQueues`
  (netserver), тесты orderqueue/commandqueue (−46). **Стоячие приказы CC-2/CC-4
  остались** (`standingOrdersModule`: авто-штурм + дежурный вылет + patrol.stamp,
  драйверы соло и NET). Команды панели (штурм/обстрел/погрузка/выгрузка/стоп) — прямые
  действия. CC-заметки ниже — история дизайна (код в git до REL-1). CDP-смоук: матч
  живёт, 0 следов очереди-UI, 0 исключений; гейт 1071 зелёный.
- **CC-6** ✅ Лимит очереди по ПРИКАЗАМ игрока (не шагам) + подписка: один enqueue =
  один приказ = 1..4 шага с общим `group`-штампом (сервер ставит сам) — тап по чужому
  миру строит «⚔ захват» `[move, assault]` за один слот (владелец через fog). Лимит
  `CHAIN_ORDERS_BASE`=3 через хук `order.chainLimit`; `subscriptionModule` поднимает
  подписчику (`state.subscribers`) до `CHAIN_ORDERS_PREMIUM`=5 (только-удобство,
  `main-menu.md`); 🔁 вне лимита; `order.remove` сносит приказ целиком. Панель: строки
  по приказам со счётчиком `N/лимит`, кнопки гаснут на лимите, апселл «🔒 … до 5»,
  цель приказа — ссылка (панорама к миру + кольцо, выделение не трогается); пипы карты
  нумеруются по приказам. Соло зеркалит серверное правило (`enqueueOrder`); стаб
  подписки на плейтест — `localStorage['vd.premium']='1'`. +4 теста (46 в orderqueue);
  браузер-проверено RU/EN: 3/3 и 5/5, отказ 4-го/6-го, ссылки, апселл. Гейт 888 зелёный.
- **CC-srv-2** ✅ Стоячие приказы серверно-авторитетны (CC-2 авто-штурм + CC-4 дежурный
  вылет — было «только сингл», клиентские Set/Map): `standingOrdersModule`
  (`order.auto`/`order.scramble`/`patrol.stamp` → `state.autoAssault`/`state.patrols`,
  сервер сам считает патруль, sweep мёртвых флотов), чистые драйверы
  `serverAutoAssaultActions`/`serverPatrolActions` (fog-честно: цели только из
  `identifiedNodes` владельца; только при войне; перезарядка по меткам `rearmAt` — живёт
  при редких пробуждениях), хост-цикл `runServerStanding` в netserver, fog-фильтр обоих
  ключей в `visibility.project`. Клиент: кнопки работают в NET (шлют экшены, читают
  состояние), сингл — на прежних локальных драйверах. +15 тестов (standingorders) +1
  (visibility); e2e по WS: `order.auto` → флаг в broadcast. Гейт 904 зелёный.

## Блок ECON · Ядро: внутриматчевая экономика `[core]` `[data]`

> Внутриматчевые ресурсы + биржа (≠ мета-экономика `economy-roadmap.md`: Суверены/Варранты).

- **ECON-1** ✅ Набор ресурсов → 5: `credits`(деньги)/`metal`/`food`/`energy`/`microelectronics`
  (`data/resources.json` + ремап ссылок biomass→food, dark_matter→energy; убраны
  artifacts/premium_shard). Гейт зелёный.
- **ECON-2** ✅ Сессионная биржа (`marketModule`, `GameState.market`): `market.list`
  (эскроу) / `market.buy` (за деньги, **15% комиссия сжигается**, частичная покупка) /
  `market.cancel` (возврат остатка). Публичный ордербук, в delta. 5 тестов.
- **ECON-3** ✅ Производители `energy`/`microelectronics` (здания): `power_plant` (Fusion
  Reactor, 3 ур.: energy 25→60→110) и `fabricator` (Microelectronics Fab, 3 ур.:
  microelectronics 8→18→32; стоит metal+credits+energy, гейт техом `microelectronics_fabrication`).
  Прописаны в ростеры `sectorKinds`; теперь у каждого экономического ресурса (кроме `credits`)
  есть производитель. Тесты: производство energy/micro экономикой + referential-integrity
  (любой `produces`/`cost`/`upkeep`-ресурс ∈ `resources`). Движок не тронут (агностичен). +3 теста.
- **ECON-4** ✅(прототип) UI биржи: окно «Рынок» в рельсе — вкладки ресурсов, продажа/покупка с эскроу, листинг/отмена лотов; сетевой матч использует ту же сессионную биржу. _(Клиент packages/client — когда shell дорастёт до HUD.)_
- **ECON-5** ✅ Экономика зданий (содержание + brownout): `BuildingDef/BuildingLevel`
  получили `upkeep` (суточный, единый слив с юнитами в `economyModule`); неоплаченный
  ресурс → `Player.arrears` (приватно, срезается в fog) и **brownout ×0.5** ТОЛЬКО для
  зданий-потребителей этого ресурса до погашения (`BROWNOUT`, состояние-переносимо,
  формула непрерывна). Прототип: ростер замкнул все 5 ресурсов — `farm` (пища),
  `power_plant` (энергия), `fabricator` (микро; апгрейды стоят собственную продукцию —
  бутстрап), энергосодержание у refinery/radar/orbital_aa/metal_station; наземка ест
  пищу (upkeep food), эскадрильи и `siege_doctrine` стоят микро. `netIncome` зеркалит
  (потоки < 1/ч — с десятичной), чип ресурса краснеет ⚠ при дефиците (тап объясняет),
  кодекс показывает «Содержание». ИИ: строит реактор/ферму при отрицательном потоке и
  держит рабочий резерв перед распродажей излишков. +9 тестов (5 core economy, 4 proto);
  браузер-проверено. Гейт 913 зелёный.

## Блок BF · Баг-охота 2026-07-10 (перед плейтестом) `[core]` `[srv]` `[proto]` `[cli]`

> Полный триаж — **`docs/bughunt-2026-07-10.md`** (8 агентов-охотников, каждая находка
> подтверждена временным тестом против реального ядра). Здесь — короткий индекс со
> статусом; детали, сценарии и направления фиксов — в триаж-доке. Живая QA
> (CDP-прогон клиента) дала 0 непойманных исключений в обоих локалях.

- **BF-1..BF-35** ✅ Все подтверждённые находки (5 CRIT + ~30 MAJOR/MINOR/хвост) исправлены,
  каждая мелким PR с регресс-тестом из репро охотника — полный список с номерами PR в
  триаж-доке (`docs/bughunt-2026-07-10.md`). Блок закрыт.

## Блок SES · Доделка сессионного слоя (GDD-пробелы, ревизия 2026-07-10)

> Аудит «сессия vs GDD» показал 4 пробела, не покрытых кирпичами. Здесь они
> нарезаны; roadmap §Этап-1 ссылался на EFX-1 — теперь кирпич существует.

- **SES-1** ✅ `[core]` `[proto]` **Коалиционный порог победы (GDD §3.3).** Score-гонка
  идёт по «юнитам победы»: соло-игрок ИЛИ alliance-компонента активных игроков
  (коалиция — только люди: `E_BOT_ALLIANCE` не даёт ботам стойку alliance). Порог
  коалиции = `scoreLimit × N × coalitionFactor` (деф. **0.7**, `VictoryConfig.coalitionFactor`)
  — сублинейный (2 игрока: 700 при соло-600, 350/голову), и он **замещает** соло-порог
  участникам: альянс — обязательство, а не читерский двойной шанс. Коалиция побеждает
  ВМЕСТЕ: `match.winners[]` (сортированный состав) + топ-скорер в `match.winner`;
  `winners` едет в `match.ended`. Прототип: баннер «🏆 ПОБЕДА КОАЛИЦИИ (A + B)», XP
  начисляется каждому победителю (RU/EN). Детерминизм: BFS по сортированным id. +3 теста.
- **AVA-0** ✅ `[core]` `[proto]` **Играбельный командный бой (2v2 и т.п.) — первый шаг к
  AvA без мета-слоя.** `SeatConfig.team?` + посев дипломатии по стороне в `newGame`:
  одна сторона стартует ALLIED (побеждают вместе через SES-1, без дружественного огня),
  между сторонами — WAR с первого часа; нет команд ни у кого → классический FFA (все
  пары `peace`, обратная совместимость). Альянс — посеянное состояние, поэтому ИИ-союзник
  реальный (в обход `E_BOT_ALLIANCE`-гейта; клика-победа читает стойку). Прототип: тумблер
  «⚔ Командный бой» в сетапе + A/B-чипы на местах (ты залочен в A), стороны едут в
  `SetupConfig`. Коалиционный чат/пинги/победа — из коробки. +4 теста ядра; e2e соло 2v2
  (союзник=alliance, соперники=war, коалиция из двоих). **Дальше:** NET-2v2 (netserver
  сеет команды), сгруппированный спавн союзников (общий фронт), полный AvA-жизненный цикл
  (`corporation-wars.md`: вызов/принятие/ростер/фазы) — server/meta.
- **SES-2** 🔶 `[core]` `[srv]` **Награды по итогам сессии (GDD §3.4).** Первый срез ✅:
  `victoryModule.endMatch` считает детерминированную таблицу `Record<PlayerId,
  {place, xp}>` — place = standard competition ranking (1224) по финальным очкам,
  XP = участие + капнутая доля счёта + win-бонус каждому члену победившей клики;
  масштаб — данными (`data/rewards.json` → `GameData.rewards`, `RewardsDefSchema`
  с дефолтами = прото-`matchXp` 40/÷10/cap 100/160; переопределяется бандлом).
  Таблица в `state.match.rewards` + payload `match.ended` + observe-`end`
  прото-хоста (JSONL плейтеста). Остаток кирпича: запись на аккаунт — ждёт
  мета-экономику EC-* и серверный XP/уровень (AC-0.3).
- **SES-3** ✅ `[core]` `[data]` **Премиум-добыча (GDD §4.3).** Развилка решена
  владельцем (2026-07-12): **премиум = energy**, нового ресурса нет. Каталог:
  тип мира `energy_nexus` (`data/planetTypes.json`: +100% energy через
  `productionByResource`, +30% обороны — горячая точка, score 45) — работает через
  существующий `planetTypeModule`, кода не потребовал. Слив: действие
  **`technology.boost {technology}`** (technologyModule) — платит
  `data.researchBoost.cost` (деф. 50 energy) и режет ОСТАВШЕЕСЯ время одного
  активного исследования на `initialPercent × decay^boosts` (деф. 25%, затухание
  ×0.5 — геометрически убывающая эффективность, мгновенного завершения не купить;
  НЕ юниты/боевая мощь/опыт героев). Ускорение = решедул: `completesAt` сдвигается,
  старое `technology.complete` становится no-op по несовпавшему `completesAt`.
  Конфиг `GameData.researchBoost` (`ResearchBoostDefSchema`, zod-default — бандлы
  без блока не тронуты), payload-схема в гейте. Коды: `E_NOT_ACTIVE`, `E_TOO_LATE`,
  `E_INSUFFICIENT`, `E_BAD_PAYLOAD`. Осталось за скоупом: расстановка energy_nexus
  на картах (map-дизайн), прото-UI кнопки буста, источник 2 (покупка на мета-рынке
  — EC-*).
- **EFX-1** ✅ `[core]` **Универсальный движок трейтов/эффектов.** `effectsModule` —
  интерпретатор `data.events` (`EffectRuleSchema`): `planet_captured` — trait-scoped
  (правило = трейт: срабатывает, когда захватившая сила несёт id правила в `traits`
  юнита — ровно пример architecture.md §2.2), `schedule` — глобальное тёмное событие
  по абсолютной сетке `cadenceHours` (детерминированно через `time.advanced`-спаны,
  каждый активный игрок катает `chance` независимо; cap 100 срабатываний/спан).
  Эффекты — через capability `effect.<name>` (шов расширения) с фолбэком на встроенные
  `add_trait`/`modify_resource` (кламп ≥0); неизвестный trigger/effect → правило
  инертно, никогда не краш; каждое срабатывание эмитит `effect.applied`. Юнитовые
  `traits` теперь читаются генерически (`data/traits.ts`: `defHasTrait`/`unitHasTrait`/
  `stacksHaveTrait`; artillery/immobile/hero переведены на общий словарь). Подключён в
  кернел прототипа (инертен при `events: {}`). 11 тестов. Разгрёб главный дизайн-долг
  Этапа 1.

## Блок AVA · AvA-жизненный цикл (server/meta) `[srv]`

> Полный жизненный цикл Alliance-vs-Alliance из `corporation-wars.md` (мета-карта в
> долгий ящик). Слой — server/meta, вне детерминированного ядра. Лесенка: **база
> корпораций** → флаги готовности + вызов/принятие → ростер + оркестрация фаз.
> **Детальный технический роадмап (опоры/модель данных/API/тесты по каждому кирпичу) —
> `docs/ava-lifecycle-roadmap.md`.** Крит-путь: AVA-1 → AVA-3 → AVA-4 → AVA-6 → AVA-7 → AVA-8.

- **CORP-0** ✅ `[srv]` **База корпораций (членство/роли RBAC + store + REST).**
  Организационный слой из `corporations.md`: `CorpStore` (Memory + Postgres) —
  корпорации, членство `head|officer|member|recruit` (рекрут-строка = заявка),
  аудит; структурные инварианты на уровне хранилища (одна корпа на аккаунт — PK по
  `account_id`; уникальное имя без учёта регистра; атомарные `createCorp`/`swapHead`
  в транзакции; аудит переживает роспуск). `CorpService` применяет матрицу прав
  `corporations.md` §2 fail-secure стабильными кодами (`E_FORBIDDEN`/`E_NOT_MEMBER`/
  `E_HEAD_MUST_TRANSFER`/`E_NAME_TAKEN`/`E_IN_CORP`/…): ровно один Глава, Главу не
  кикнуть, офицер не эскалирует (не меняет роли, не трогает Главу/офицера), передача
  главенства — отдельное явное действие, Глава уходит только передав главенство (или в
  одиночку — это роспуск). REST в `main.ts` (session-gated на каждом роуте, per-IP
  rate-limit на записи по образцу auth/match API, аудит-лог). Контрактные тесты обоих
  адаптеров (memory + Postgres) + вся матрица прав + HTTP-контракт. **Отложено (не
  спекулятивно):** медали (нужна история матчей MM-3.1), гейт `createCorp` по уровню
  аккаунта (нужен серверный XP/уровень AC-0.3). **Дальше:** флаги готовности к AvA
  (корп-флаг Главы + player-consent) + вызов/принятие (тратит очки влияния, истечение);
  затем ростер (пауза/лок) + оркестрация фаз S3–S7 через существующий MatchRegistry +
  посев команд AVA-0.
- **AVA-1** ✅ `[srv/core]` **Командная дипломатия на серверном пути.** `buildStateFromMap`
  сеет `state.diplomacy` из `slot.team` (хелпер `seedTeamDiplomacy`, тот же посев, что
  прото-`newGame`): карта без команд → peace-FFA; та же сторона → `alliance` (seeded —
  минует `E_BOT_ALLIANCE`); между сторонами — по опции `crossTeamStart` (`war` дефолт /
  `peace` — мирный старт AvA под AVA-8). Пары из отсортированных id — канонический JSON.
  Хвост **NET-2v2** тоже: `TEAMS=2v2` на прото-хосте сеет 4 командных seats (A: p1+p2
  западные углы, B: p3+p4 восточные) — живой 2v2 до оркестратора; пустые кресла играет
  серверный ИИ. Тесты в `buildFromMap.test.ts` (war/peace/2v2/FFA/детерминизм).
- **AVA-2** ✅ `[srv]` **Очки влияния корпорации.** Корп-валюта `influence` в `CorpStore`
  (`addInfluence`/`spendInfluence` — списание атомарное, guard `influence >= cost` внутри
  UPDATE, `E_INSUFFICIENT`, никогда < 0; аудит-действие `influence`). Отдельно от
  внутриматчевой казны. Memory + Postgres (`ALTER … IF NOT EXISTS` backfill), контракт-тесты
  обоих адаптеров.
- **AVA-3** ✅ `[srv]` **Флаги готовности к AvA.** Корп-флаг (глава → пул готовых,
  `setCorpReady`/`clearCorpReady`) + игровой флаг (член → согласие на офлайн-развёртывание,
  `setPlayerReady`/`clearPlayerReady`, привязан к текущей корпе — выход/кик/роспуск чистит);
  `GET /ava/pool`. RBAC fail-secure (`AvaService`). Memory + Postgres (`corp_ready`/
  `player_ready`) + контракт-тесты.
- **AVA-4** ✅ `[srv]` **Вызов/принятие (S0–S2).** `AvaService.challenge` (глава готовой
  корпы, тратит влияние ПЕРЕД созданием заявки — возврат при отказе создания) /
  `accept` (глава цели → `accepted` = S2-матчап) / `decline` (возврат влияния); истечение —
  `sweepExpired(now)` на инжектируемом таймере, без клиентов. Инварианты в сторе: одна
  `pending`-заявка на пару (partial unique index), exactly-once `pending→terminal`
  (условный UPDATE — гонка double-accept закрыта, без двойного возврата). Коды `E_FORBIDDEN`/
  `E_NOT_READY`/`E_SELF_CHALLENGE`/`E_ALREADY_CHALLENGED`/`E_INSUFFICIENT`/`E_NO_CHALLENGE`/
  `E_CHALLENGE_CLOSED`. REST `POST /ava/challenge` + `/:id/(accept|decline)`,
  `GET /ava/challenges`; session-gated, per-IP rate-limit; свип-интервал в `main.ts`. Memory
  - Postgres + сервис-тесты (весь state-машинный набор) + HTTP-контракт.
- **AVA-5** ✅ `[core/data]` **Пул AvA-карт + eligibility.** `MatchMapSchema.avaEligible`
  (bool, дефолт false); форма ВЫВОДИТСЯ из slots хелпером `avaShape` (`sides`/`slotsPerSide`,
  null при <2 сторонах / неравных — тегу и раскладке неоткуда разъехаться);
  `validateMatchMap` валит eligible-карту с кривой формой (`E_AVA_SHAPE`). `ava-duel-1`
  помечена (2×1), добавлена `ava-2v2-1.json` (2×2, союзные слоты сгруппированы по флангам,
  нейтральные призы west/east). `pickAvaMap(maps, sides, slotsPerSide, rng)` в server/meta:
  фильтр тег+форма, канонический порядок по id, один draw seeded-Rng — выбор карты
  воспроизводим для реплеев. Тесты: `buildFromMap.test.ts` + `avaMapPool.test.ts`.
- **AVA-6** ✅ `[srv]` **Сбор ростера + лок (S3).** `accept` теперь ОТКРЫВАЕТ окно паузы
  (`pauseEndsAt` на матчапе, деф. 24ч); статусы матчапа расширены: `accepted` →
  `locked`/`cancelled` (exactly-once через условный UPDATE, как AVA-4). `AvaRosterStore`
  (Memory+Postgres, таблица `ava_roster`): одна запись на (matchup, account) — PK,
  пер-сайд кап atomically-guarded (FOR UPDATE на строке матчапа сериализует гонку за
  последний слот). Сервис: `setRoster` (глава/офицер, ТОЛЬКО из флагнутого пула AVA-3,
  кап, replace-side целиком), `join` (самозапись члена в окне, нефлагнутый тоже —
  явка и есть согласие; идемпотентно), `rosterView` (свой состав + только счётчики
  чужого — приватность до боя), `sweepRosters` (обе стороны ≥ minPerSide → `locked`,
  недобор → `cancelled` + возврат цены вызова ровно один раз). Кап = слоты карты
  придёт из AVA-7 (пока инжектируемый параметр, деф. 4). Коды `E_NOT_FLAGGED`/
  `E_ROSTER_FULL`/`E_ROSTER_LOCKED`/`E_WINDOW_CLOSED`. REST `GET /ava/matchup/:id`,
  `POST …/roster`, `POST …/join`; свип на интервале `main.ts`. Тесты: стор-контракты
  обоих адаптеров + сервис (окно/RBAC/кап/лок/отмена+возврат) + HTTP.
- **AVA-7** ✅ `[srv]` ★ **Оркестратор: сессия из ростера (S4).** `AvaOrchestrator`:
  `pickAvaMap` (размер = бо́льшая сторона ростера) → чистая рассадка `seatAvaRoster`
  (каждая сторона на слоты своей команды, союзники сгруппированы, пустые кресла →
  серверный ИИ; `playerId = slotId`) → `buildStateFromMap({slots, crossTeamStart:'peace'})`
  (мирный старт S5) → комната поднимается через инжектируемый `createRoom` (снапшот в стор,
  ленивый реестр грузит на коннекте). `AvaSessionStore` (Memory+Postgres `ava_sessions`):
  связка matchup↔match_id + фикс-рассадка `account→slot`, restart-safe. AvA-осведомлённая
  рассадка: `resolveAvaSeat(matchId, accountId)` → фикс-место / `E_NOT_ROSTERED` / `null`
  (не-AvA матч → обычный путь), встроен в `join` (`main.ts`). Свип `orchestrate` по
  залоченным без сессии (мимо клиента, рядом с roster-свипом); идемпотентно, seeded по
  matchupId. Тесты: seating (группировка/боты/детерминизм) + orchestrate (state/peace/
  реестр/идемпотентность/коды) + resolveAvaSeat + стор-контракт обоих адаптеров.
- **AVA-8** ✅ `[srv]` ★ **Мир→война→итог (S5–S7).** Полный цикл собран.
  **S5 мир:** оркестратор поднимает сессию со стартом `peace` (combat-lock бесплатно из
  посева); PLAYER-`diplomacy.declare` в AvA-комнате отклоняется НА ПРОВОДЕ — новая
  опция `MatchRoom.denyPlayerActions` (wire-правило в `receive`, оба пути bare/gated;
  серверные драйверы идут мимо), код `E_AVA_DIPLOMACY` — стойками владеет оркестратор.
  **S6 война по таймеру:** `AvaSession.warAt` (= создание + `peaceMs`, деф. 24ч,
  `AVA_PEACE_MS`); `sweepWar` на общем интервале — эскалация через инжектируемый
  `escalateWar` (main: `registry.resolve` будит комнату, `warDeclarationsFor(state)` —
  чистые системные декларации ровно по кросс-командным peace-парам с детерминированными
  id `ava-war:<match>:<a>:<b>` — реплей батча дедупится квитанциями), штамп
  `markWarDeclared` exactly-once (условный UPDATE); транзиент-провал → ретрай следующим
  свипом; матчап, рассчитанный до войны (тайм-аут в мире), вычищается из очереди БЕЗ
  эскалации. **S7 итог:** state-машина `locked`→`ended` exactly-once; `settleMatch` —
  архив + `AvaResultStore` (история MM-3.1) + влияние победителю (деф. 150) + аудит;
  ничья → без влияния; ПРОВОДКА: observe-`end` AvA-комнаты → `onMatchEnded` →
  `winnerSideOf` (слот/`bot:`-слот → сторона по командам карты; неизвестный → null,
  fail-secure) → `settleMatch`. XP отложен (нужен AC-0.3). Тесты: стор-контракты
  (dueWar/markWarDeclared оба адаптера), оркестратор (таймер/exactly-once/ретрай/
  пурж/декларации/маппинг победителя/ничья/не-AvA), wire-deny `MatchRoom`.
- **AVA-9** ✅ `[srv]` **Публичная лента корпораций.** `AvaFeedStore` (append-only,
  Memory+Postgres `ava_feed`): `matchup` на accept (S2), `result` на settle (S7) — только
  публичные факты (имена корп снапшотом + победитель, БЕЗ ростера). Публикация в
  `AvaService.accept`/`settleMatch` (последним шагом, best-effort — не валит уже
  закоммиченный переход); чтение `publicFeed(limit, before)` newest-first с курсором по
  `at`. Публичный `GET /ava/feed` (без сессии, рядом с open-matches feed). Тесты:
  стор-контракт обоих адаптеров + сервис (матчап на accept/итог на settle/без утечки
  ростера) + HTTP (публичный, без заголовка сессии).
- **AVA-C1/C2** ✅ `[proto]` **Кабинет корпорации — реальный, не макет.** `#corp`
  (хаб «Альянсы» + рейл-кнопка в матче) теперь читает живой CORP-0/AVA-2..9/MED-1
  API вместо `MOCK_CORP`. Новый чистый `prototype/src/corp.ts` — типы + fail-secure
  парсеры на каждый ответ (битый/чужой JSON роняет поле в `[]`/`null`, никогда не
  крашит панель), `main.ts` — только fetch/DOM-клей (`corpFetch` переиспользует
  сессионный паттерн ARS-5: `resolveServer`/`probeAuthMode`/`sessionKey`, без кэша —
  данные слишком летучие для витрины). Вкладки по деградации из самого
  `corporation-ui.md` §7: **Обзор** (личность + влияние + роль + журнал аудита
  голове/офицерам), **Участники** (ростер с ролями, приём/отклонение заявок,
  кик, смена роли member↔officer, передача главенства, выход/расформирование —
  вся RBAC-матрица CORP-0), **Войны** (флаги готовности корпы/себя — optimistic,
  GET-эндпоинта на статус нет; пул готовых корп; вызов/приём/отклонение AvA;
  self-enroll в ростер запертого матчапа; публичная лента), **Казна** (баланс +
  audit-based леджер). **Владения** и **Чат** — честные заглушки «скоро»: секторов
  и постоянного корп-чата на сервере нет вообще (нет Контура 2 метагейма) —
  доку явно об этом просит не подделывать, а показывать заглушку. Курируемый
  `setRoster` реализован довеском: новый `GET /corps/:id/ready-players`
  (`corpService.readyPlayers`, head/officer only — тот же флажковый пул AVA-3, что
  `setRoster` уже валидировал на сервере) даёт клиенту список eligible-игроков; в
  «Войнах» глава/офицер тоглит их чек-кнопками (`ava-roster-toggle` → полный
  `POST .../roster` — сервер REPLACE'ит сторону целиком). Медали (MED-1) в кабинет
  не выведены — остаются будущим довеском. Тесты: `corp.test.ts` (парсеры/сортировка/
  RBAC-хелпер), `corpService.test.ts`/`corpApi.test.ts` (readyPlayers RBAC).

## Блок ARS · Арсенал — персистентный мета-инвентарь `[srv]` `[data]` `[proto]`

> Роадмап — [`arsenal-roadmap.md`](arsenal-roadmap.md): корпуса/модули/фитинги/чертежи
> аккаунта МЕЖДУ сессиями; в матч — только снапшотом (`SlotAssignment`), ядро не знает.
> Аукцион/лутбоксы/крафт живут в EC-* — арсенал их склад. Крит-путь: ARS-0→1→2→3.

- **ARS-0** ✅ `[docs]` **Развилки решены владельцем (2026-07-14):** гибрид (корпуса/
  базовые модули — чертежи, редкие/заточенные — экземпляры); full-loot — НЕТ (sink'и:
  износ аренды + комиссия аукциона); аренда — возврат + износ `durability` за войну;
  валюта аренды — `influence` (AVA-2). Резолюции — в `arsenal-roadmap.md` ARS-0.
- **ARS-1** ✅ `[data][core]` **Схема предмета/чертежа.** `ArsenalItemSchema`
  (`shared-core/data/arsenalSchema.ts`, паттерн mapSchema): `kind` (hull/module/
  hero_fitting) + **`form` (blueprint/instance)** — гибрид ARS-0 зашит в схему
  (grade EC-2.1 +1..+3 и `durability` — только у экземпляров, superRefine),
  `soulbound`, `origin` (starter/drop/craft/auction/lootbox/rent), `acquiredAt`;
  `parse`/`safeParse` + `validateArsenalItem(item, data)` — defId сверяется с
  каталогом СВОЕГО kind (стабильный код `E_UNKNOWN_DEF:<kind>:<id>`, kinds не
  кросс-валидируются). Тесты против реальных шипованных каталогов.
- **ARS-2** ✅ `[srv]` **`ArsenalStore`** (Memory + Postgres `arsenal`, item_id PK):
  `grant` идемпотентен по itemId (первый выигрывает — реплей регистрации/дропа не
  дублирует), `transfer`/`consume` — owner-guarded условные записи (двойная продажа
  невозможна; **soulbound не передаётся структурно** — анти-RMT на уровне стора),
  `listOf` отсортирован. **Стартовый набор данными** (`data/starterArsenal.json`:
  3 корпуса + 3 базовых модуля) — валидируется против шипованных каталогов на буте
  (fail-secure), выдаётся хуком `authApi.onRegistered` при регистрации:
  детерминированные itemId `starter:<acc>:<kind>:<def>` → идемпотентно end-to-end;
  всё soulbound-чертежи (фарм регистраций не станок для аукциона); сбой хука не
  валит регистрацию (аккаунт есть, идемпотентный грант перезапускаем). «Пустой
  арсенал» не существует как состояние. Тесты: стор-контракты обоих адаптеров +
  сервис (шипованный набор/идемпотентность/soulbound) + хук в authApi.
- **ARS-3** ✅ `[core/srv]` **Снапшот в матч.** `Player.arsenal` (`PlayerArsenal`:
  hulls/modules/fittings — уникальные отсортированные defId) сеется через
  `SlotAssignment.arsenal` (копия — мутации у вызывающего не дотягиваются до матча);
  гейт владения в ядре: `unit.build` требует корпус + каждый модуль в снапшоте,
  `hero.fit` — фитинг (`E_NOT_OWNED`, fail-secure); БЕЗ снапшота — без ограничений
  (обычные/dev-матчи и боты не тронуты, мягкая деградация). Снапшот приватен
  владельцу (`visibleState` стрипает как казну — что враг МОЖЕТ строить, интел).
  Сервер: `arsenalSnapshotOf(items)` проецирует стор в снапшот (чертежи и
  экземпляры равно дают строимость — гибрид ARS-0); оркестратор снимает его на
  launch (лок = момент консервации GDD §2) через deps.arsenalOf → main.ts
  (`stores.arsenalStore.listOf`). LARS-1 добавит живой путь поверх. Тесты:
  гейт unit.build/hero.fit/деградация/туман + рассадка билдера + оркестратор.
- **ARS-4** ✅ `[srv]` **Дроп по месту + сальваж.** `dropRoller.ts` на observe-`end`:
  шанс по `rewards.place`, пул весами, pity (гарантия за K сухих матчей), exactly-once
  на (матч, аккаунт) через `DropStore.claim`; дроп — торгуемый чертёж `origin: drop`;
  телеметрия JSONL. Сальваж: осколки победителю каждого `battle.resolved` от павшего
  ВРАЖЕСКОГО состава (`unit.died` с `owner`), баланс в `DropStore`. Таблицы —
  `data/dropTables.json`, валидируются при старте сервера (fail-secure); симуляция не тронута.
- **ARS-5** ✅ `[proto][srv]` **Витрина + фильтр Верфи.** Вкладка «⚔ Арсенал» в хабе
  (паттерн META-1: hub-panel + bottom-nav таб), фильтр kind/grade, карточка →
  кодекс (расширен на 'md'/'hf' — раньше кодекс покрывал только юниты/здания).
  Новый `GET /arsenal/me` (`arsenalApi.ts`, session-gated как medals/corp) —
  ARS-1..4 были server/core-only, у клиента не было ни одной точки чтения
  арсенала. Кэш-first по позывному (`localStorage`, паттерн META-1), фоновый
  рефреш переиспользует уже добытый токен сессии (никогда не просит пароль
  просто ради просмотра хаба). Верфь (в матче, `conLoadoutPane`) и её палитра
  модулей сужаются `Player.arsenal` (снапшот ARS-3, тот же источник, что и
  ядерный гейт) — `createLoadoutEditor(..., { ownedModules })`, без снапшота
  без ограничений (мягкая деградация как в ядре). Ростер героев не тронут —
  в прототипе нет пикера фитингов (только живые инстансы боя), фильтровать
  нечего; гейт `hero.fit` уже на ядре (ARS-3).
- **ARS-6** ✅ `[srv]` **Корп-склад + аренда.** «Corp_arsenal» — тот же `ArsenalStore`
  (ARS-2), `accountId` = id корпорации (переиспользован буквально, без нового стора
  каталога). Новый `CorpRentStore` (Memory+Postgres, `corp_arsenal_rent`) держит
  только АКТИВНУЮ аренду: `rent(itemId,...)` атомарно (предмет — максимум в одной
  войне разом), `closeRent` — exactly-once (как `AvaChallengeStore.endMatchup`).
  `CorpArsenalService.rentOut`: гейт глава/офицер (тот же паттерн, что `MedalService`
  — читает `CorpStore.membershipOf` напрямую, не через `CorpService`), предмет должен
  принадлежать корпе, матч — ДО лока (`status === 'accepted'`), цель — на РОСТЕРЕ
  своей стороны (`AvaRosterStore.rosterOf`); аудит `CorpStore.appendAudit` (действия
  `'rent'`/`'rent_return'` добавлены в `CorpAuditEntry.action`). Снапшот-мёрдж:
  `AvaOrchestrator.corpRentalOf?` — вызывается ПЕРЕД снятием ARS-3-снапшота, сливает
  арендованное с личным через новый чистый `mergeArsenal` (`arsenal.ts`) — рентованный
  предмет строится неотличимо от своего, ядро не знает о корпе вообще (гейт владения
  не тронут). Возврат: `CorpArsenalService.returnWar(matchupId)` — хук на `onEnd` в
  `main.ts` (тот же `matchExtras.onEnd`, что ARS-4), закрывает каждую активную аренду
  матчапа ровно один раз, `ArsenalStore.wear(itemId, 1)` (новый метод — атомарный
  декремент durability, clamped на 0, no-op для чертежей) — резолюция ARS-0.3
  буквальна: возврат ВСЕГДА, победа или поражение, никакого сгорания-по-проигрышу;
  износ — единственный sink. HTTP: `POST /corp-arsenal/rent` (session-gated, паттерн
  medals/arsenal API). Тесты: `store.test.ts` (`wear`, `corpRentStoreContract` — оба
  адаптера), `corpArsenalService.test.ts` (RBAC, не-ростерный/чужая сторона отказ,
  до-лока/после-лока, двойная аренда отказана, аудит, exactly-once возврат+replay),
  `corpArsenalApi.test.ts` (гейт сессии, круговой рейс через HTTP), `avaOrchestrator
  .test.ts` (мёрдж снапшота).

## Блок LARS · Живой арсенал — мид-сессионный анлок модулей `[core]` `[srv]` `[proto]`

> По запросу владельца: приобрёл/заточил модуль в мете (в т.ч. за донат) → сразу
> **открывает постройку НОВЫХ кораблей с ним в ИДУЩЕЙ сессии** (существующие не трогает,
> бой мгновенно не меняет — сила через постройку+логистику). Роадмап —
> [`live-arsenal-roadmap.md`](live-arsenal-roadmap.md). **Реально новое — ослабление
> снапшота-на-старте ТОЛЬКО для билд-каталога** («что можно построить» живое; «что уже
> построено» — no-refit, цело). P2W-рамка уже принята (`economy-roadmap.md` дизайн-лок),
> здесь только доведение силы до живого матча детерминизм-safe. Крит-путь: EC-1.*+ARS-2 →
> LARS-0 → LARS-1 → LARS-2.

- **LARS-0** ✅ `[docs]` **Решено владельцем (2026-07-14):** живым становится только
  билд-каталог (no-refit цел); источник владения — живой `ArsenalStore` на серверном
  гейте; **F2P-паритет** — одно правило «владеешь → строишь» для всех источников;
  искусственного лага снабжения на MVP нет (естественный — постройка+логистика;
  фикс-лаг позже данными, если плейтесты покажут — LARS-3). Резолюции — в
  `live-arsenal-roadmap.md` LARS-0.
- **LARS-1** ✅ `[core/srv]` **Живая авторизация постройки.** Гейт владения в ядре
  (`construction.ts`/`hero.ts`, ARS-3) НЕ тронут — вместо этого новый server-only
  экшен `arsenal.sync` (`shared-core/modules/arsenalSync.ts`, вне `actionPayloadSchemas`,
  как `patrol.stamp` — клиент не форджит) полностью заменяет `Player.arsenal` свежим
  `arsenalSnapshotOf(ArsenalStore.listOf(accountId))`. `MatchRoom.maybeSyncArsenal`
  перед допуском `unit.build`, чей корпус/модули не покрыты стартовым снапшотом, живьём
  читает стор и — если аккаунт теперь владеет — коммитит `arsenal.sync` (обычным
  `commitApply`, реальное персистентное хеш-согласованное действие, не тихий патч
  стейта) ПЕРЕД исходным действием; не владеет — тот же `E_NOT_OWNED`, что и раньше.
  `accountId` теперь доходит до `MatchRoom` (`addPeer`, из JWT-claim через `wsServer.ts`
  handshake — раньше терялся). Работает только на durable (`persist`) путях — гейтед
  (`admitCommitted`) и голом (`doCommittedSubmit`); нет `arsenalStore`/`accountId`/
  снапшота на месте — поведение как раньше (мягкая деградация). Детерминизм цел:
  ядро как было чистой функцией, `arsenal.sync` — обычный залогированный экшен;
  реплея действий нет (персистентность — снапшотами `GameState`), так что live-чтение
  никогда не переигрывается. Тесты: `arsenalGate.test.ts` (модуль в середине матча
  становится строимым; без снапшота sync отказан `E_NO_SNAPSHOT`; full-replace не
  union — проданное исчезает), `matchRoom-commit.test.ts` (end-to-end через `receive`:
  live-докупка проходит, всё ещё неимеемое — тот же отказ, нет accountId/уже покрыто
  снапшотом — live-чтение не срабатывает вовсе).
- **LARS-2** 🔒(EC-1.2, EC-2/EC-3, LARS-1) `[srv]` **Цепочка валют → фронт end-to-end.**
  Суверены→Варранты→приобретение/заточка→`ArsenalStore`→доступно к постройке; интеграция
  существующих звеньев, аудит-леджер, атомарность.
- **LARS-3** ✅ `[srv/data]` **Баланс и гайки честности.** Три из четырёх ручек спеки
  оказались уже верными ИНВАРИАНТАМИ существующего кода, не новой механикой —
  закрыто тестами, подтверждающими контракт, а не новым кодом: **только свои миры**
  (`ownedPlanet` в `construction.ts` уже требует `planet.owner === action.playerId`
  для ЛЮБОЙ постройки — арсенальный гейт его не обходит, тест на чужом мире даёт
  `E_FORBIDDEN`, не `E_NOT_OWNED`); **F2P-паритет** (снапшот ARS-3 несёт только
  defId — `origin` предмета уже стрипается проекцией `arsenalSnapshotOf`, гейт
  физически не может отличить дроп/крафт/аукцион/старт — тест пинает это явно как
  контракт, не случайность формы). **Прозрачность источника/тайминга** — уже закрыта
  LARS-4 (заметка «не мгновенно» + тег происхождения в Верфи). **Лаг снабжения
  (`supplyLagHours`)** помечен опциональным в самой спеке — НЕ реализован: нет
  решённых чисел баланса, добавлять его сейчас — спекулятивная сложность без
  playtest-сигнала; остаётся будущим кирпичом, если плейтесты покажут нужду.
  Тесты: `arsenalGate.test.ts` (свой/чужой мир, паритет происхождения).
- **LARS-4** ✅ `[proto]` **Живая Верфь в матче.** `conLoadoutPane` уже читал
  `Player.arsenal` заново на КАЖДЫЙ рендер (без мемоизации) — свежекупленный модуль
  и так появлялся сам; закрыт был явный UX-сигнал: заметка «⚡ Арсенал живой…» (не
  мгновенно, через постройку) при наличии снапшота + тег источника предмета
  (`conOriginTag`, новый `originOf` в `arsenal.ts`) на карточках отсека/палитры —
  best-effort кросс-referencing по кэшу хаб-витрины (ARS-5, `arsenalItems`); пусто,
  если вкладку «Арсенал» ещё не открывали в этой сессии — никогда догадка. «Где
  взять → магазин/аукцион» из исходной спеки НЕ реализован — аукциона (EC-3) в коде
  ещё нет, вести там некуда; честно отложено до EC-блока. Тесты: `arsenal.test.ts`
  (`originOf` — находит/не гадает/берёт самый свежий из нескольких), `constructor
  .test.ts` (палитра реагирует на выросший снапшот между двумя рендерами без
  памяти состояния — тот же вызов, что делает `conLoadoutPane`).

## Блок META · Мета-прокачка командующего `[proto]`

- **META-1** ✅ Деревья прокачки + меню: `prototype/src/meta.ts` — 3 прямые ветки
  (Командование/Экономика/Наука) × 4 узла, цена узла = его tier в очках; XP только
  за завершённые матчи (`matchXp`: участие 40 + счёт до 100 + победа 160), уровень
  по растущим порогам (100, +50/уровень), 1 очко за уровень. Эффекты — снапшот в
  `newGame` через существующие швы (нового кода движка нет): скрытые сессионные
  техи `meta_*` (бонусы едут штатными хуками технологий; из окна исследований
  спрятаны по префиксу), +уровень совету учёных, множитель стартовой казны —
  `SetupConfig.meta` (только человеку). Персист v1 — localStorage по позывному
  (`vd.meta.<ник>`, fail-secure парсер); сервер-аккаунт (SE-1.x) заберёт при
  мета-слое. UI: вкладка хаба «★ Прокачка» (XP-бар, очки, узлы ✓/Изучить/Закрыто),
  тосты «+XP»/«новый уровень» на баннере конца матча. Прокачка не продаётся —
  только игра (гардрейл main-menu.md §5). 8 тестов; браузер-проверено. Гейт 1023.

## Блок HERO · Ядро: герои `[core]` `[data]`

> Дизайн — [`heroes.md`](heroes.md). Принцип: **всё — данные, движок один**
> (`heroModule` интерпретирует JSON; экзотика — через `capability` `hero.effect.<type>`).
> Герой = корабль (переиспользует `movement`/`combat`). Скелет (HERO-0) уже есть.
>
> **Прим. (прототип-маршрут):** прототип теперь ездит на **полной новой модели**: 5
> hero-каталогов инлайн в `game.ts` (зеркало `data/*.json`, те же id), ростер меню сеется
> **core-инстансами** (grade→архетип 1:1, главный — флагманом домашнего флота, остальные —
> резервом как в `buildFromMap`), окно «Герои» (`rail-hero` → `renderHero`) покрывает весь
> цикл: `hero.spawn` (armed-тап: свой мир / свой флот / мир союзника по маркер-перкам),
> `hero.ability` (armed-тап цели; встроенные live, типизированные без эффекта — «скоро»),
> `hero.skill.unlock`, `hero.fit`. См. `state.md` §7.

- **HERO-0** ✅ Скелет: герой-позиция (`GameState.heroes`/`tempLanes`/`topology`),
  `heroModule` с `hero.move`/`hero.path.create`/`planet.annihilate`, `dead_world`,
  приватность в `visibleState`, кэш маршрутов по `topology`. 11 тестов (PR #31).
- **HERO-1** ✅ `[data]` Схемы + `data/heroes.json` (архетипы: `commander/ravager/vanguard/warden`,
  `HeroArchetypeDef {name, branch?, ship{unit?|stats?}, slots, startAbilities[], startPassives[]}`,
  ветка героев `transhuman|psionic`) + `data/heroAbilities.json` (`HeroAbilityDef {name, type,
cooldownHours, range, cost, params}`: `corridor/annihilate/rally/scan/recall/bulwark`); загрузчик
  (`loadGameData`) дополнен; `parseGameData` валидирует; тесты схем + referential-integrity
  (`startAbilities`∈heroAbilities, `ship.unit`∈units) + дефолты + fail-closed. 4 теста.
- **HERO-2** ✅ Движок: герой → **корабль**. Позиция героя = **нода его корабля** (`heroNode`:
  `fleets[fleetId].location`, в полёте/без корабля — фолбэк `Hero.location`); все способности
  (legacy + `hero.ability`) действуют от корабля; `Hero.location` — память последней ноды,
  синкается на `fleet.transit`/`fleet.arrived` (и якорь респауна после `home`); `hero.move`
  развёрнутому герою → `E_HERO_DEPLOYED` (кораблём ходит `fleet.move`); `on('fleet.destroyed')`
  — второй сигнал смерти рядом с `unit.died` (общий `killHero`, идемпотентно по `alive`).
  4 теста.
- **HERO-3** ✅ `hero.spawn {heroId, at}` — ручной подъём корабля героя на **своём** мире:
  гейты живости (`E_HERO_ALIVE`, stale-`fleetId` не блокирует), респаун-кулдауна
  (`E_RESPAWN_COOLDOWN`), легальности точки (`E_NO_PLANET`/`E_BAD_SPAWN`) и кэпа **3
  активных/игрок** (`E_HERO_CAP`, `activeHeroCount`); корпус корабля — из архетипа
  (`Hero.archetype` → `data.heroes[..].ship.unit`, фолбэк `hero`); общий деплой-путь
  `formHeroShip` с авто-респауном, который теперь тоже уважает кэп. Ручной спавн — путь
  спасения: бездомно-мёртвый (авто-респаун не нашёл мира) или удержанный кэпом герой
  поднимается вручную, когда мир/слот появился. Событие `hero.spawned`; payload-схема
  в гейте. 5 тестов.
- **HERO-4** ✅ Обобщённый `hero.ability {heroId, abilityId, target?}`: генерические гейты из
  `HeroAbilityDef` (владение/живость/экипировка `Hero.abilities`/кулдаун/дальность/стоимость;
  `E_HERO_DEAD/E_NO_ABILITY/E_NOT_EQUIPPED/E_NO_EFFECT` + существующие коды) + диспетчер по
  `type`: встроенные `temp_lane`/`annihilate` (общие тела эффектов `castTempLane`/`castAnnihilate`
  с legacy-действиями `path.create`/`annihilate`, поведение которых сохранено 1:1) или
  `capability hero.effect.<type>` (контракт `HeroEffect`, экспортирован); неизвестный тип без
  capability → `E_NO_EFFECT` (fail-secure). Кулдаун-ключи встроенных типов делятся с legacy
  (`path`/`annihilate`) — анти-double-dip; кастомные — по `abilityId`. `params`-оверрайды
  (`durationHours`/`speedBonus`) из данных. Payload-схема `hero.ability` в гейте (SV-1.2). 6 тестов.
- **HERO-5** ✅ Пассивки из данных → хуки. `data/heroPassives.json` + `HeroPassiveDefSchema
{hook: 'fleet.speed'|'combat.damage', scope: 'heroFleet'|'ownFleetsNear', params{bonus, radius}}`
  (enum-гейт хуков — как каталог tech-условий); `Hero.passives?: string[]` (сеется из
  `startPassives` архетипа); живой герой множит хук на ×(1+Σбонусов) поверх лейна/ауры —
  `heroFleet` бафает флот героя, `ownFleetsNear` — свои флоты в `radius` от ноды героя
  (`heroNode`, HERO-2); мёртвый герой/неизвестный id — ноль (мягкая деградация). Шипованы
  `vanguard_impulse` (+10% скорость флота героя) и `rally_beacon` (+8% урона в 300).
  Referential-integrity `startPassives`∈heroPassives. 4 теста.
- **HERO-6** ✅ Фитинги корабля: `data/heroFittings.json` (`HeroFittingDef {statMods,
grants{ability?|passive?}, cost}`, анти-self-expansion рефайн как у ship-модулей) +
  действие `hero.fit {heroId, fitting}`: слот-бюджет из архетипа (`slots`;
  `E_NO_SLOTS`, безархетипный герой — 0 слотов), `E_ALREADY_FITTED`/`E_NO_FITTING`,
  казна (nonnegative), **без refit** (owner-правило ship-модулей). `grants` живые
  (HERO-4/5, общий `applyGrants` с дедупом); `statMods` едут данными до шва
  эффективных статов (SHIP-3/4) — «designed, not live». Шипованы «Пси-усилитель»
  (scan), «Матрица „Эгида"» (rally_beacon), «Абляционная обшивка» (hp+40, не live).
  3 теста.
- **HERO-7** ✅ Дерево навыков: `data/heroSkillTrees.json` (`HeroSkillNode {name, branch?,
requires[], cost, grants{ability?|passive?}}`; ветки **transhuman**/**psionic**, по руту на
  каждую: `neural_lace`/`overclocked_helm` и `void_attunement`/`psi_veil`) + действие
  `hero.skill.unlock {heroId, node}`: гейты владения/живости, каталога (`E_NO_NODE`),
  повтора (`E_ALREADY_UNLOCKED`), ветки архетипа (`E_WRONG_BRANCH`; узел без ветки — общий,
  открыт и безархетипным героям), цепочки `requires` (`E_REQUIRES`) и казны
  (`E_INSUFFICIENT`, nonnegative cost). Грант ложится на инстанс (`Hero.abilities`/
  `Hero.passives` + `Hero.skills`) — движки HERO-4/5 подхватывают без доп. вайринга;
  stat-/param-бонусы узлов — когда появится их движковый шов (не шипуем обещания).
  Payload-схема в гейте; referential-integrity дерева (requires/grants). 4 теста.
- **HERO-8** ✅ Спавн **на флоте / у союзника** — маркер-типы способностей (`spawn_fleet`/
  `spawn_allied`): ноская (не кастуемая) способность такого типа расширяет легальные цели
  `hero.spawn` — свой флот (герой **абордажится в стек хоста**, аура кроет весь флот,
  `fleetId`→хост) и союзный мир (дипломатия D1: `stanceToRelation(getStance)==='ally'`;
  нейтрал/война — по-прежнему `E_BAD_SPAWN`). Чужой флот закрыт и с маркером. Шипованы
  «Абордажная транслокация» (ravager) и «Дипломатическая высадка» (commander). 2 теста.
- **HERO-9** ✅ Ростер: `SlotAssignment.heroes?: string[]` (buildFromMap) — пред-матч
  выбор **до 3 РАЗНЫХ архетипов** (open-question решён по прецедентам C3/совета учёных:
  снапшот при сборке, дубли — fail-secure `E_DUPLICATE_HERO`; `E_UNKNOWN_HERO`/
  `E_TOO_MANY_HEROES`/`E_HERO_NO_HOMEWORLD`). Каждый сеется **неразвёрнутой** Hero-записью
  (`hero:{player}:{n}`, якорь `home`/`location` = первый владеемый мир слота, лоадаут =
  `startAbilities`/`startPassives` архетипа); корабли поднимает `hero.spawn` (HERO-3).
  Найм по ходу матча — отдельная будущая механика, не этот кирпич. 2 теста.
- **HERO-FX1** ✅ `[core]` Первый провайдер шва `hero.effect.<type>` — `heroEffectsModule`
  (`modules/heroEffects.ts`, экспортирован из пакета): `hero.effect.recall` мгновенно
  телепортирует корабль героя в столицу (`Hero.home`, якорь HERO-2 следует за кораблём).
  Гейты: `E_HERO_NOT_DEPLOYED` (нечего отзывать — резервный герой), `E_FLEET_BUSY` (не
  выдёргивать из активного боя; stale-`battleId` не блокирует), `E_NO_CAPITAL`,
  `E_SAME_LOCATION` (no-op у столицы — не тратить кулдаун). Кулдаун 24ч по `fx:recall`
  ведёт диспетчер HERO-4; событие `hero.recalled`. Доказывает шов end-to-end (контракт
  `HeroEffect` был экспортирован-но-неиспользован); новые эффекты = новый провайдер, ядро
  не трогается. Прототип: `heroEffectsModule` в кернел, `recall` — кнопка «Активировать»
  (`HERO_CASTABLE`) вместо «скоро». 8 core + 1 proto теста.
- **HERO-FX2** ✅ `[core]` Второй провайдер — `hero.effect.aura` (rally/bulwark):
  **таймбоксед** боевая аура. Каст кладёт `{bonus, radius, until}` в новое
  `Hero.activeAuras` (прунинг истёкших на касте — рост ограничен, т.к. кулдаун >
  длительности), а собственный хук `combat.damage` модуля бафает флоты владельца в
  `radius` от ноды героя, пока `until > now` — временный близнец пассивки HERO-5
  `rally_beacon` (мультипликативная композиция ×-факторов: порядок модулей неважен).
  `combatBonus`/`defenseBonus` оба кормят единственный боевой хук, что даёт модель;
  кривая (0-бонус/0-длит.) аура → `E_BAD_EFFECT` (кулдаун не тратится). Данные:
  rally/bulwark `range:300`→`0` (самобаф-untargeted) + `params.radius:300`. Событие
  `hero.aura`. Прототип: `aura` в `HERO_CASTABLE` (rally/bulwark — «Активировать»).
  5 core (вкл. боевой ×1.1 рядом/вне радиуса/истёкшая) + 1 proto теста.
- **HERO-FX3** ✅ `[core]` Третий провайдер шва — `hero.effect.reveal` (scan):
  **таймбоксед fog-шов**. Ranged-каст (диспетчер проверил цель в радиусе) кладёт
  `{center, radius, until}` в `Hero.activeReveals` (прунинг на касте); проекция тумана
  `coverageFor` (`state/visibility.ts`) читает раскрытия **только своих** героев
  (per-viewer — не течёт сопернику) и поднимает полный identify на миры в `radius` от
  `center`, пока `until > state.time`. Данные: `scan.params.radius:250`. Кривой reveal →
  `E_BAD_EFFECT`; событие `hero.revealed`. Прототип: `reveal` в `HERO_CASTABLE`
  (armed-тап цели), stale-флаги `heroes.ts` (`live`) выправлены — **«скоро» не осталось**,
  все не-встроенные эффекты (recall/aura/reveal) имеют провайдеры (спавн-маркеры не
  кастуются). 6 core + 1 proto теста; CDP-verify (Разведка → «Цель…», 0 «СКОРО»).
  _(Трилогия recall/aura/reveal закрывает блок HERO-FX.)_

## Блок SHIP · Модульность кораблей `[proto]` `[core]` `[data]`

> Дизайн — GDD §6.1 («корабль собирается из модулей»). **ТОТ ЖЕ модульный движок, что
> у героев** — слоты + модули-данные (`statMods`/`grants`) → вклад в те же хуки
> (`combat.damage`/`fleet.speed`). Развилки зафиксированы из доков:
> **(а)** лоадаут на **КЛАСС/корпус**, не на инстанс (GDD §6.1 + сотни юнитов + прецедент
> `FormationTemplate`) — иначе взрыв JSON-стейта; **(б)** **заморожен на старте** матча
> (GDD §2 «дека модулей фиксируется при создании сессии») — лоадаут в `SetupConfig`, как
> `templates`/`heroes`; **(в)** **decoupled от мета-экономики** (предметы/заточка/аукцион
> EC-1..3) — это чистая внутриматчевая модель, как герои до сервера.
>
> **Этот блок — про МОДУЛИ корабля** (фиттинг в «Верфи», SHIP-1..6). **Эскадрильи /
> авианосцы / десант** (включая десантную эскадрилью-конверсию) — **отдельный канон:**
> [`squadrons-roadmap.md`](squadrons-roadmap.md) (SQ-блок; модель: общий трюм + вылет юнитом).
> Парные доки: `missiles-roadmap.md`, `shields-roadmap.md`, `command-chains-roadmap.md`, `mechanics-roadmap.md`.

- **SHIP-1** ✅ `[proto]` Модель: `prototype/src/ships.ts` — корпуса (`SHIP_HULLS`: cruiser 3 ·
  siege 2 · scout 1 · dropship 2), модули (`SHIP_MODULES`: батарея/броня/щит/двигатель/
  наведение, дробные `mods`, стэкаются), `shipStats(base, loadout)` (детерминированная
  деривация статов), `DEFAULT_SHIP_LOADOUTS`. 11 тестов. Модули пока `live:false` (превью).
- **SHIP-2** ✅→♻ `[proto]` UI «Верфь». Первая версия — pre-match вкладка в setup — была
  **удалена** (`6be9217`: замораживать лоадаут ДО матча некогерентно, модули открываются
  техом в матче). Переехало **в-матч**: см. CON-1.
- **CON-1** ✅ `[proto]` **Единый таб-конструктор «Верфь»** (`rail-constructor` →
  `renderConstructor`, оверлей `#constructor`): in-match Stellaris-свитчер
  `[Корабли|Эскадрильи|Армия|Герои]`. Панель **Корабли** рендерит view-model
  `@void/client/loadoutEditor` **напрямую** (типизированные слоты + `canEquip` + живой
  `effectiveStats`-превью + `loadoutCost`) и строит через `buildShip` → `unit.build{modules}`
  (лоадаут заморожен на постройке, SHIP-3). Инлайн-данные прототипа дополнены `modules`
  (зеркало `data/modules.json`) + типизированными `slots` на корпусах. 5 тестов
  (`constructor.test.ts`) + CDP-смоук.
- **CON-2** ✅ `[proto]` Панель **Эскадрильи**: тот же `loadoutEditor` через общий
  `conLoadoutPane(hullList)` над корпусами эскадрилий (`fighter_squadron` `{weapon:1}`,
  `strike_carrier` `{defense:1,utility:2}` — space-домен, те же 6 модулей). +2 теста.
- **CON-3** 🗑 `[proto]` Панель **Армия**: редактор шаблона дивизии (`division.template`) —
  🗑 **СНЯТО (H4-REVERT, 2026-07-31).** Игра вернулась к системе отдельных наземных
  юнитов: они строятся как обычные юниты, стоят в гарнизоне и ездят в трюме флота
  (`armyModule`), десант разрешает `combatModule`. Текст кирпича ниже оставлен как
  запись о проделанной работе — но описанной механики в игре БОЛЬШЕ НЕТ.
  6 слотов (тап цикл пусто→пехота→танк), живой агрегат `formationStats` (атака/оборона/
  корпус), доктрина состава и стоимость мобилизации; сама мобилизация — в панели мира. +1 тест.
- **CON-4** ✅ `[proto]` Панель **Герои**: штаб героев **свёрнут** в таб (`heroBodyHtml`:
  способности/дерево/фиттинги, клики роутятся конструктором). Рельс `rail-hero` и окно
  `#hero` **ретайрнуты** — разгрузка игрового HUD (исходная цель фичи).
- **CON-5** ✅ `[proto]` **Мобильная адаптация «Верфи»** (`@media (max-width:560px)` +
  short-viewport): палитра модулей на 2 колонки (без переноса имён), крупнее тап-таргеты
  (close/шаг/табы/корпус-кнопки), уже рамка → шире бокс; `#constructor`/`#market`
  добавлены в правило скролла коротких вьюпортов (ландшафт-телефоны). CSS-only; проверено
  CDP-скриншотами portrait 390×844 по всем 4 панелям.
- **SHIP-3** ✅ `[core]` Эффекты живые: `util/loadout.ts` (`effectiveStats`) читается боем
  (`combat.ts`) и постройкой (`construction.ts` валидирует лоадаут на `unit.build`, платит
  `loadoutCost`, считает HP/щит от эффективных статов); каталог `data/modules.json`;
  `util/loadout.test.ts`. _(Статус выправлен аудитом доков — код был готов раньше пометки.)_
- **SHIP-4** ✅ `[core]` Обобщён фиттинг-движок: **один генерик инсталл-гейт
  «слоты+предметы»** (`util/fitting.ts`: `canInstall`/`validateInstalled(FittingSpec)` —
  каталог → дубль → `allowed` → бюджет по категории, generic-причины
  `unknown|duplicate|not_allowed|no_slot`). Ship-лоадаут (`canEquip`/`validateLoadout`)
  и `hero.fit` — обёртки: каждый мапит причины в СВОИ стабильные `E_*`-коды
  (герои = одно-категорийный бюджет архетипа без предиката) — **поведение и коды
  байт-в-байт** (пинящие тесты обоих потребителей не тронуты и зелёные). Экспортирован
  из `@void/shared-core` — будущие потребители (модули зданий) садятся на тот же шов.
  `fitting.test.ts` (6): порядок проверок, категория-vs-категория, одно-бюджетный кейс,
  skip-unknown. `grants`-механика героев осталась своей (это не слот-гейт).
- **SHIP-5** 🔒(EC-1.1) `[srv]` Модули как **предметы мета-экономики** (data-driven схема,
  soulbound/заточка/аукцион) → снапшот лоадаута в матч (GDD §5.2). _(зависит от блока ECON)_
- **SHIP-6** ✅ Типизированные слоты: корпуса в `data/units.json` несут
  `slots:{weapon,defense,utility}`, `loadout.ts` энфорсит покатегорийный бюджет
  (`ShipSlotType`/`canEquip`/`slotUsage`). _(Статус выправлен аудитом доков.)_
- **SHIP-7..11** → **перенесено в** [`squadrons-roadmap.md`](squadrons-roadmap.md) (канон,
  чтобы не дублировать): десантная эскадрилья-конверсия (`SQ-7`), эскадрильи-истребители +
  выпуск/возврат/топливо/патруль (`SQ-0..4`), носитель `dropship` ✅ (большой трюм / мало
  пушек), ПВО-counter (`aaDamage` штатно), бой с гарнизоном (`SQ-7.2`). Эти кирпичи едут там.

## Блок NET-P0 · Честный сетевой матч (аудит 2026-07-07) `[proto]` `[srv]`

> P0-кирпичи перед первым дружеским плейтестом двух живых игроков — найдены полным
> аудитом готовности main (все — маленькие, по одному PR).

- **NETP0-1** ✅ `[proto]` (PR #128) Порог победы в сети: netserver не передаёт `config` в
  `MatchRoom` → victory берёт дефолт 600, а HUD обещает 450. Передать конфиг
  (score limit + timeScale) при создании комнаты; тест.
- **NETP0-2** ✅ `[proto]` (PR #128) Совет учёных для p2: `DEFAULT_SETUP` помечает p2 как AI и
  учёные выдаются только людским местам, а netserver снимает ai-флаг ПОСЛЕ `newGame`
  — живой игрок на p2 навсегда без совета (и без «Хранителя»). Сеять сетевой матч
  с обоими людскими местами (или выдавать совет всем местам); тест.
- **NETP0-3** ✅ `[cli]` `[proto]` (PR #128) Доменные события по сети: сервер уже шлёт
  `delta.events` с фог-фильтром, но `MultiplayerClient` их отбрасывает — ПВО-трассеры,
  дуги артиллерии, тосты потерь/итогов боя не видны в NET-матче. Прокинуть события
  в `onSnapshot`/отдельный колбэк → `handleEvents` прототипа; тест на фог-фильтр.
- **NETP0-4** ✅ `[proto]` `[srv]` `[cli]` Сетевой чат: relay по образцу ally-пингов —
  `chat.send`/`chat.msg` в протоколе, разадресация на сервере (session = все,
  coalition = живой альянс, dm = двое), бэклог 100 с реплеем на джойне, rate-limit
  по wall-clock; клиентский рендер с эха (dedupe по id); тесты сервер+клиент; e2e.
- **NETP0-5** ✅ `[proto]` Переговоры между людьми: consent-офферы в прототипной
  дипломатии (эскалация односторонняя и стирает офферы; смягчение = оффер, встречное
  объявление коммитит пару; бот отвечает сразу по фавору — принимает или отклоняет со
  стиранием, «висит» только у людей). UI: ✓ «принять» / ⏳ «отправлено» на кнопках
  ростера, тосты по диффу снапшотов; `E_ALREADY`/`E_ALREADY_OFFERED`. Тесты; e2e
  двух живых игроков (война → оффер мира → принятие). Заодно закрывает старый долг
  **D4** в его игровой части: прототипная дипломатия теперь живёт на хелперах ядра
  (`getOffer`/`setOffer`/`clearOffers`/`STANCE_RANK`) с теми же D2/D3-семантиками.
- **NETP0-6** ✅ `[proto]` (PR #128) EN-локализация свежих окон: steward («Хранитель») и
  настройки собраны из голых RU-строк без t(); дубликаты ключей в en.ts («Наука»
  дважды → ветка прокачки показывает «Tech»).

## Блок ONB · Обучение новичка (онбординг) `[proto]` `[cli]`

> Активного онбординга нет (проверено): ни туториала, ни гайд-марок, ни help-экрана —
> новичка роняют в матч. При этом обучающая копия уже написана (`.hint`/кодекс/`en.ts`),
> но вся «pull». Онбординг = **пушить существующий контент** в нужный момент + учить
> самый трудный концепт жанра (async-мир идёт офлайн). **Детальный технический роадмап —
> `docs/onboarding-roadmap.md`** (субстрат — фреймворк удержания `engineering-review.md §5`).
> Крит-путь: ONB-0 → ONB-1 → ONB-2.

- **ONB-0** ✅ `[proto]` **Флаг первого запуска + воронка.** Признак «прошёл онбординг»
  (`vd.onboard.<ник>`) отдельно от сохранённого ника; ветвление new/returning; хуки
  «докуда дошёл» (агрегаты — OPS, без PII). Чистая модель `onboarding.ts` (fail-secure
  парсер, идемпотентные переходы) + 13 тестов; хаб-предложение новичку + «Ещё → Обучение»
  реплей, «Начать» запускает гайдовый первый матч (ONB-2). _Серверная часть воронки
  (`[srv]`, агрегаты в `/metrics`) — когда появится онлайн-онбординг._
- **ONB-1** ✅ `[proto]` ★ **Движок гайд-марок (spotlight).** Переиспользуемый примитив:
  оверлей + подсветка узла (дыра из 4 dim-панелей по bounding-box) + пузырь со счётчиком
  «шаг k из n» + шаги из данных; продвижение по `tap`/`action:<type>`/`state`-предикату.
  Чистый движок `spotlight.ts` (DOM-free, 22 юнит-теста: advance, скип, optional-скип vs
  safe-stop, re-query-устойчивость, геометрия, + CSS-регрессия ниже) + браузерный адаптер
  `spotlightDom.ts` + data-цепочка `onboardingTour.ts` над реальным HUD;
  `playerOrder`→`notifyAction`, RU/EN, **баг найден живьём (реальный игрок + независимо
  headless-тач-репро): на action/state-шаге («построй Шахту» и далее) тур не пропускал
  НИЧЕГО** — `#spotlight{position:fixed;inset:0}` сам по себе обычный div (дефолт
  `pointer-events:auto`), и хотя все 4 `.sl-dim`-панели уходили в `pointer-events:none`
  под `.sl-passthrough`, сам корень продолжал глотать тап в ЗАЗОРЕ между панелями — то
  есть ровно там, где игрок должен был тапнуть реальный HUD. Исправлено: `#spotlight.sl-
  passthrough` тоже `pointer-events:none` (пузырь остаётся кликабельным — явный
  `pointer-events:auto` на потомке перебивает `:none` предка, `build.mjs`). +2 теста,
  пинящих CSS-текст, чтобы регрессия не вернулась молча.
  запуск через шов `window.__vdTour`. На нём строятся ONB-2/3.
- **ONB-2** ✅ `[proto]` ★ **Гайдовый первый матч.** Скрипт по соло-песочнице без ботов
  (`startGuidedMatch` → `firstMatchTour.ts`): домашний мир → шахта (`action:building.construct`)
  → флот (`action:fleet.launch`) → курс (`action:fleet.move`, туман) → двухфазный захват
  (`state`) → счёт (`state`). «Do X»-беты ждут реального приказа, захват/счёт — по живому `s`;
  пропускаемо, успех → onboarded + XP-пакет (ровно раз) + нудж. Тесты: `firstMatchTour.test.ts`
  + `applyTourOutcome`; проверено headless-бутом. _Микрошаги захвата (орбита/десант/дивизия) —
  доводка за ONB-3._
- **ONB-3** ✅ `[proto]` **Just-in-time интро механик.** Разовая карточка при первом открытии
  панели (технологии/рынок/Хранитель/верфь/дипломатия) — чистый `intros.ts` (`resolveIntro`,
  per-nick `vd.seenIntros`, 10 тестов) + оверлей `#intro`; ветеран (`meta.xp>0`) — помечено молча.
  Прогрессивное раскрытие. **Доводка закрыта:** триггеры ретрит/артиллерия подключены тем же
  «учим на первом реальном использовании» паттерном, что `asyncDelay` (ONB-5) — `retreat`
  (`firstFail`) на первом `fleet.retreat`, копия — существующий боевой `.hint` про −40% корпуса
  (не выдумана заново, только вынесена из мид-боя в спокойный момент); `artillery`
  (`firstAvailable`) на первом `fleet.barrage`, новая короткая копия (готового текста под
  обстрел не было). Оба хука в `playerOrder` (main.ts) рядом с `asyncDelay`, той же гвардой
  «не во время гайд-тура». **Плюс `hero`** (`panelOpen`) на первом открытии вкладки «Герои»
  внутри Верфи (по запросу владельца — героев/навыки push-обучение раньше вообще не
  касалось, только pull-досье узла дерева навыков) — новая копия про дерево навыков/
  способности/возрождение на столице; хук рядом с `corp`/`ava` (ONB-8) в обработчике
  вкладок конструктора. RU/EN.
- **ONB-4** ✅ `[proto]` **Help/кодекс-хаб («?» везде).** Существующий кодекс — в поисковый
  справочник + глоссарий (7 терминов): чистый `codexIndex.ts` (`buildCodexIndex`/`searchCodex`,
  9 тестов) + оверлей `#codexhub` (поиск + категории), deep-link в `openCodex`; входы — хаб
  «Ещё → Справочник» + рельс-«?». RU/EN. Surfacing, низкий риск. _Per-panel контекстная «?» —
  доводка позже._
- **ONB-5** 🟡 `[proto/srv]` ★ **Async-модель + дневной дайджест.** Клиент-часть сделана:
  интро задержки первого `fleet.move` («мир идёт офлайн», `asyncDelay` через ONB-3-механизм);
  recap возврата — чистый `recap.ts` (`buildRecap`, attention по emoji-маркерам, 5 тестов),
  теперь портирован в `@void/shared-core` (`util/recap.ts`) так и клиент, и сервер строят
  дайджест по одной логике; `prototype/src/recap.ts` — тонкий re-export. Оверлей `#recap`
  (авто на `visibilitychange` + ручной «🛰» в сводках, тап→jumpToPing). RU/EN. Серверная
  сторона push: `packages/server/src/push.ts` (VAPID-конфиг, `digestPushPayload`, `sendPush`
  через `web-push`, 404/410 → `gone`) + `PushStore` (Memory/Postgres, `push_subscriptions`) +
  `pushApi.ts` (`GET /push/key`, `POST /push/subscribe`, `POST /push/unsubscribe`,
  session-gated) — 14 тестов. _Осталось: сам триггер («когда именно слать дайджест» — учёт
  online/offline по аккаунту, cooldown, привязка к матчу) и клиентский service worker +
  подписка на push из UI — приёмник (`sendPush`) уже готов, зовущего пока нет._
- **ONB-6** ✅ `[core/proto]` **Combat-preview.** «Если атакую — что будет?» — чистая
  `previewBattle(attacker, defender, data)` в `shared-core` (`state/previewBattle.ts`):
  зеркало раундового цикла combat-модуля (симультанный залп attack/ответка defense,
  tier-порядок, щиты, клапан 240 раундов) БЕЗ хук-конвейера `combat.damage` — умышленно:
  hook-точный прогноз протёк бы скрытыми бонусами врага (техи/фракция/герой); базовая
  модель читает только видимые составы, расхождение = туман войны. Тесты: ТОЧНЫЙ паритет
  (победитель/раунды/выжившие) с реальным кернелом без хуков на 4 матчапах + совпадение
  по знаку при хуке сектора + чистота (deepFreeze). Прото: строка «Прогноз штурма» в
  секции «Удар» панели флота (десант vs гарнизон, хедж-подпись про бонусы), RU/EN.
  Fog-дисциплина на вызывающем: флот доккнут у мира → мир идентифицирован. Client-обёртка
  (`createBattlePreviewModel` в `@void/client` `matchHud.ts`) закрыта отдельно — см. G4.
- **ONB-7** ✅ `[proto]` **Цели первой сессии.** Лёгкий сворачиваемый чеклист («шахта ✓, флот ✓,
  захват ✓, 100 очков ✓») по живому состоянию + похвала/XP на завершение (ровно раз) — чистый
  `firstGoals.ts` (`metGoals`/`mergeDone`/`goalsComplete`, 10 тестов) + оверлей `#goals`; только
  в онбординг-матче (скрыт на конце гайда/выходе). RU/EN.
- **ONB-8** ✅ `[proto]` Онбординг в соц/мета-слой (корпорации/AvA) — ONB-3-механизмом,
  два новых разовых интро (`prototype/src/intros.ts`): `corp` при первом `openCorp()`,
  `ava` при первом переключении на вкладку «Войны» кабинета корпорации.

## Блок ST · Хранитель: крыло под опекой (заказ владельца 2026-07-16) `[core]` `[srv]` `[proto]`

> Полный дизайн — `steward-roadmap.md` §ST-3. Идея: враг у расположения эскадрилий →
> Хранитель считает прогноз боя; собственные потери ≥ 35% (`STEWARD_LOSS_LIMIT`) →
> вывод крыла (самоходы улетают, гарнизон забирает транспорт с пустым трюмом);
> < 35% — опционально (отдельная поза) контрудар, только на своей территории.

- **ST-3.1** ✅ `[core]` **Доля потерь по прогнозу + трипваер «враг близко».**
  `BattlePreviewSide.damageFraction` (0..1, по корпусу через `hullPool` на общем
  с `damageUnits` `stackHull` — одна копия пул-арифметики, дрейф исключён; щиты
  вне счёта) + `scanNodeThreats` (`state/threat.ts`: `present`/`inbound`(+ETA
  через канонические `journeyDestination`/`journeyEtaMs` в `state/route.ts`;
  парк-журней предупреждает оба конца финальной полосы)/`nearby`, fog-honest
  через `isVisibleTo` с hoisted-`identified`, стансы D1, сорт (eta, id)) +
  `STEWARD_LOSS_LIMIT = 0.35` в `modules/steward.ts`. 15 тестов (рукосчитанный
  1.0/0.6, паритет hp-пулов выживших с живым боем, fog-граница, ETA×timeScale,
  кемп-журней, детерминизм, deepFreeze).
- **ST-3.2** ✅ `[srv]` `[proto]` **Эвакуация под угрозой (поза `defend`).**
  `stewardGuardOrders` (game.ts, вызван из `aiOrders('defend')`): скан своих узлов →
  прогноз → выигрываемая стойка держится всегда (анти-финт), иначе ≥ порога → доккнутые
  флоты грузят здоровый гарнизон (`findHealthyStack`-паритет) в свободные трюмы
  (`fleetCargoFree`) и уходят в ближайший безопасный свой узел; застрявшему гарнизону —
  ближайший idle-паром не с угрожаемого узла, только если успевает с запасом 2ч до ETA
  (`E_UNDER_ASSAULT` блокирует погрузку после начала штурма — опоздавший не посылается).
  Один флот — одна задача; дубль-паром не шлётся; негде укрыться → hold. 11 тестов
  (`stewardGuard.test.ts`), вкл. мульти-тиковый e2e через реальное ядро.
- **THREAT-HUD** ✅ `[proto]` **«Враг у ваших рубежей» живому игроку.** `scanNodeThreats`
  по своим мирам раз на игровой шаг → нота с ETA (клик к миру), дедуп по эпизоду
  узел+флот (паттерн radarMemory). Плюс проценты `damageFraction` обеих сторон в строке
  «Прогноз штурма». RU/EN; i18n-гейт + CDP-смоук.
- **ST-3.4** ✅ `[srv]` `[proto]` **Анти-шаттл гистерезис.** Свежая запись `evac X→Y` в
  журнале SITREP блокирует обратный рейс Y→X из списка укрытий на 12 игровых часов
  (`EVAC_RETURN_COOLDOWN_H`); третий мир обходит блок; укрытий нет → форс-холд
  («бой лучше вечного транзита»). 3 теста.
- **ST-2.4** ✅ `[core]` `[srv]` `[cli]` **SITREP — журнал решений + утренний рапорт.**
  `Player.stewardLog` (FIFO 50, в стейте — переживает офлайн-ночь и истечение вахты,
  сброс новым делегированием; owner-private: fog теперь вырезает и `steward`, и лог) ←
  действие `steward.report` (штамп серверного драйвера, вне клиентских схем как
  `patrol.stamp`, санитизация fail-secure). Драйвер цепляет report-хвост со словарём
  evac/ferry/stranded/strike/watch/hold (повторные — раз на эпизод, дедуп по журналу).
  Клиент: «Журнал Хранителя» в окне + счётчик решений в утренней заметке, RU/EN.
- **ST-3.3** ✅ `[core]` `[srv]` `[proto]` **Поза «Активная оборона».**
  `active_defend` в `STEWARD_POSTURES`: на удерживаемой стойке контрудар `fleet.engage`
  по припаркованным врагам своего узла — гейт ценит всю лестницу (победитель
  авто-перевязывается на следующего): сильнейшее крыло обязано пройти всех цепочкой с
  суммарными потерями < порога, иначе удара нет + fire-watch `order.scramble` на
  эскадрилье-крыльях у своих миров (снимается при эвакуации). Территорию не покидает,
  войн не объявляет. UI выбора позы + RU/EN. +6 тестов.
- **ST-2.1** ✅ `[core]` `[srv]` `[proto]` **Guard-режим: точки удержания.**
  Не поза, а стоячий приказ: `Player.stewardHoldPoints` (кап 2), действие
  `steward.holdpoint` (тот же тех-гейт, только свой мир, fog-стрип якорей),
  кнопка «🚩» в панели планеты. Драйвер: якорь никогда не эвакуируется —
  проигрышная стойка вызывает ОДНО праздное крыло, если оно успевает до ETA
  с 2ч-запасом И переламывает прогноз под 35% (`reinforce` в SITREP); иначе
  вынужденный hold. No-poach в обе стороны (якорь держит своё крыло; подмога
  не с угрожаемых узлов/якорей); потерянный мир сам чистит свой якорь (слот
  капа не утекает). 4 ядровых + 5 драйверных тестов + gateparity.

- **ST-4** ✅ `[proto]` **Окно «Хранителя» не закрывается по Back/Escape.** Найдено
  живой проверкой в REFM-7 (поведение НЕ новое — так было и до выноса, поэтому кирпич
  отдельный, а не правка внутри рефактора). `#steward` не попал в реестр слоёв
  `closeTopLayer()`, хотя там лежат все соседи (дерево технологий, рынок, конструктор
  дивизий, журнал, кодекс). Следствие на Android: при открытом окне Back не закрывает
  его, а проваливается в ветку «нечего закрывать» — игрок получает подсказку «ещё раз
  Back — и выйдешь из матча» вместо ожидаемого закрытия. Починка: одна ветка в
  `closeTopLayer()` + строка в `topLayerOpen()`, рядом с `marketWin`. Проверять и
  Escape (десктоп-паритет), и Back. **Fix:** `stewWin` добавлен в `topLayerOpen()` и
  `closeTopLayer()` — Back/Escape теперь закрывает окно Хранителя.

## Блок SES-2 · Сессии как в Iron Order (заказ владельца 2026-07-17) `[srv]` `[proto]`

> Полный игровой цикл: регистрация → главное меню → лента сессий → вход на
> свободное место → игра → конец сессии. Всё время — РЕАЛЬНОЕ (wall-clock,
> не игровое): ИИ-заместитель подхватывает кресло после 3 реальных дней
> отсутствия игрока (это ПОЛНЫЙ бот `expand`, НЕ Хранитель: Хранителя игрок
> включает сам, защитные позы, живое делегирование сильнее авто-подхвата);
> вход в сессию на свободное место открыт 4 реальных дня от её создания.
> Первая сессия — скорость ×24.

- **SES-2.1** ✅ `[srv]` `[proto]` **Автостарт сессий — мир живёт с создания.**
  Лобби нет: `MatchRoom.initiallyStarted` теперь работает БЕЗ `manualStart` —
  «рождена запущенной», часы якорятся на `initialState.time` (свежая — день 1
  сразу, восстановленная — с сохранённого мгновения), `TIME_SCALE` действует;
  голый wall-clock без якоря укатил бы свежий мир на десятилетия. netserver
  переведён на автостарт; клиентский лобби-оверлей (ростер/«НАЧАТЬ МАТЧ»/
  «Покинуть лобби»), его CSS/разметка и 7 ключей локали удалены; `waiting`-баннер
  оставлен под транспортный waitForPlayers. Транспортный тест (якорь+скейл, лобби
  не течёт в снапшоты) + живой raw-ws смоук (вход → дельты без всякого «Старта»).
- **SES-2.2** ✅ `[srv]` **ИИ-заместитель после 3 РЕАЛЬНЫХ дней отсутствия.**
  `AI_GRACE_MS` по умолчанию 10 мин → **3 реальных дня** (`3*24*60*60*1000`,
  env-конфигурируемо; wall-clock, независимо от `TIME_SCALE` — на ×24-сессии
  «ушедшим» всё равно считают по реальным суткам). Два ИИ явно разведены чистой
  тестируемой функцией `seatAiDecision(hasHuman, posture, graceExpired)` в
  `game.ts`: **`steward`** — свой автопилот игрока (играет по своей позе даже при
  живом коннекте, делегирование бьёт грейс), **`substitute`** — полный `expand`-бот
  на брошенном кресле (только после истечения грейса, мгновенно снимается при
  возврате), **`none`** — присутствующий игрок командует сам. netserver вызывает
  функцию вместо инлайн-условий; баннер запуска печатает окно грейса. 3 теста
  (таблица истинности: Хранитель бьёт присутствие и грейс; присутствующий не
  вытесняется; пустое кресло ждёт грейс → заместитель). Живой смоук (grace=0 →
  пустые кресла играются; ×200 мир идёт).
- **SES-2.3** ✅ `[srv]` **Окно входа 4 реальных дня.** Новое кресло можно занять
  только в течение окна от создания сессии; занятое место (свой тикет/ник) —
  реконнект в любой момент. Транспорт: опция `admitNewSeat?(matchId)` в `wsServer` —
  вызывается ТОЛЬКО для первого входа ника (проверка через неассигнящий
  `accountStore.seatOf` ДО `resolveSeat`, чтобы отклонённый новичок не сжёг кресло),
  закрытое окно → 403; реконнект (ник уже держит место) не гейтится. Read-model:
  `MatchRegistry.entryOpen(matchId)` + `MatchMeta.entryWindowMs` + поля
  `entryOpen`/`entryClosesInMs` в `MatchSummary`; закрытая сессия выпадает из
  «Доступных», но остаётся в «Активных» у своих. Возраст = `state.time / timeScale`
  (реальное время работы мира: переживает рестарт, учитывает TIME_SCALE — на ×24
  4 реальных дня = 96 игровых). netserver: `ENTRY_WINDOW_MS` (деф. 4 дня, env),
  `admitNewSeat: (id) => registry.entryOpen(id)`, строка баннера. Тесты: 6 read-model
  (открыто/закрыто по timeScale, unknown=fail-secure, выпадение из «Доступных»,
  entryClosesInMs) + 3 транспортных (seat-lock и dev-ник: новичок 403, свой
  реконнект, обратная совместимость без окна). Живой смоук (окно=0 → новичок 403).
- **SES-2.4** ✅ `[proto]` **Лента сессий в главном меню.** Браузер матчей клиента
  (`#connect`, 3 вкладки Доступные/Активные/Архив над `GET /matches?nick=`) уже
  показывал карту/matchId, «День N», места X/Y, правила (×скорость, порог), статус
  и «Войти» кликом. SES-2.4 добавил статус **окна входа** (поля `entryOpen`/
  `entryClosesInMs` из read-model SES-2.3): на «Доступных» строка «вход ещё {Nд Mч}»
  (cyan; янтарь, если < суток) / «вход закрыт» (перечёркнуто) — закрытые сессии
  сервер и так не отдаёт в «Доступные», так что это подсказка «успеешь/торопись».
  `fmtJoinWindow` (дни/часы), sentinel `Number.MAX_SAFE_INTEGER` = без окна (старый
  сервер/dev) не рисует ничего. Реконнект своих в «Активных» окном не гейтится —
  там строки нет. RU/EN, CSS `.mwin`; контракт проверен живым `GET /matches`
  (`entryOpen:true`, `entryClosesInMs:345600000`=4 дня), i18n-гейт, чистый билд.
- **SES-2.5** ✅ `[srv]` `[proto]` **Регистрация/логин на игровом пути.** С
  `AUTH_JWT_SECRET` прото-хост включает ПОЛНЫЙ контур аккаунтов (тот же SE-1.x,
  что на боевом входе): `POST /auth/register|login` (scrypt, uniform-401 + decoy,
  per-IP лимит) → session-JWT (7 дней) → `GET /matches/:id/join` (Bearer) →
  join-токен (15 мин) → WS `?token=` — nick/ticket-рукопожатия ОТКЛОНЯЮТСЯ
  (токен — единственная identity). Join-роут повторяет гейт SES-2.3: неассигнящий
  `seatOf` ДО `resolveSeat`, закрытое окно → 403 `E_ENTRY_CLOSED` только для
  ПЕРВОГО входа логина; место принадлежит логину сессии (никем другим не зайдёшь),
  реджойн возвращает то же место. Без секрета — прежний nick+ticket (LAN, zero-setup);
  клиент самонастраивается по `GET /auth/status`. Клиент: поле «Пароль» на connect
  (показывается только в auth-режиме), zero-friction identity (login → 401 →
  register → 201; 409 = «неверный пароль»), session-JWT в localStorage per-server
  (пароль не хранится), реконнект сам минтит свежий join-токен (протухшая сессия →
  «введите пароль»). `users`-таблица в общей миграции (Memory/Postgres).
  `configFromEnv` экспортирован из index. Compose: `AUTH_JWT_SECRET` passthrough.
  Живой e2e (10 проверок): register→дубль 409→неверный 401→login→join без сессии
  401→join→WS token→welcome на своём месте→nick отклонён 401→реджойн то же место;
  плюс окно входа на auth-пути (window=0 → 403 E_ENTRY_CLOSED).
- **SES-2.6** ✅ `[srv]` `[ops]` **Плейтест-цикл ×24 — полный цикл живьём.**
  Compose: `TIME_SCALE` деф. **24** (1 реальный час = 1 игровой день; окна
  отсутствия/входа остаются РЕАЛЬНЫМИ — 3д/4д); для 24/7-постуры `TIME_SCALE=1`.
  Живой e2e полного цикла на одном сервере (AUTH+GATE+×24), 7/7: регистрация →
  session-JWT → лента (день/места/окно ~96ч) → join → место → WS `?token=` →
  gated `action.v1`-приказ → дельты → замер часов от первой к последней дельте
  ≈ ×24.0. Конец сессии: `checkEnd()` работает в ОБОИХ режимах (net-снапшоты
  несут `match`), `#endscreen` рендерит финал, сам конец матча закреплён
  victory-тестами ядра — живьём не переигрывается (победа занимает игровые
  недели даже на ×24).

## Кросс-каттинг

- **PC-UI** ✅ `[proto]` **Десктоп-полировка правой панели (consolidation, ветка
  radar-radius).** Всё строго за `pcUi()` (медиа-запрос `pointer:fine` — десктоп);
  мобайл нетронут. ШТУРМ как прицельная команда с паном карты при взведённом приказе;
  правый клик по плитке стройки заказывает сразу; упрощённая правая панель +
  рейл/спидбар/конструктор; плитки гарнизона без имён + ховер-досье (`data-desc`) на
  иконки; окна 80% ширины на ПК. Влито чисто (0 конфликтов) в текущий main, гейт 1559
  зелёный, билд чист. (Ветка `ava-8-t20atq` при сведении оказалась полностью устаревшей —
  её AVA-7/8/9/MED-1/ONB-1 уже в main другими PR; единственный orphan-тест
  `spotlightDom.test.ts` бил в снятый API `SpotlightController`, не салвижен.) Диагноз по
  фактам: (1) релиз `alpha`/CI/updater-ядро исправны, но `#updbar` и ручная проверка
  жили ТОЛЬКО в `#connect` — путь возвращающегося игрока (ник запомнен → хаб) их
  никогда не видел, а тихая проверка бежала один раз на старте (офлайн-старт → никогда);
  (2) **Pages не деплоился ни разу** (0/119 зелёных: Pages не включён в Settings,
  env-гейт `github-pages` режет job до первого шага — `enablement:true` не успевает).
  Фикс: `#updbar` → **глобальный fixed-баннер** (z-96, виден с welcome/хаба/матча);
  тихий re-check на форграунд (`visibilitychange`) + каждые 4ч с троттлингом 15 мин;
  **тайл «Обновления» в хабе** (диагностика в `hub-note`; на `#connect` — как раньше,
  в `cver`); runbook-комментарий в `pages.yml` с сигнатурой падения. CDP-verify
  (мок `__BUILD__`+release API): баннер поверх хаба, ручная проверка «⬇ есть
  обновление», «Позже» скрывает. ⚠ Остался **один ручной шаг владельца**: Settings →
  Pages → Source «GitHub Actions» — после него ссылка
  https://moongametechnology.github.io/MoonGame/ всегда отдаёт свежий main (веб-«автообновление»),
  а APK-путь уже работает целиком (баннер → системный браузер → установка поверх,
  подпись стабильна).
- **PERF-2** ✅ Оптимизационный проход по `shared-core` (3-линзовый агент-ревью: горячие пути
  модулей пост-PERF-1 / кернел+проекции / упрощение). Эффективность (поведение побайтово
  идентично, golden-RNG зелёный): `kernel.runStep` — снят O(n²)-дренаж `queue.shift()` и
  дубликат-массив (события читаются из `emitted` по индексу); `artillery.runArtillery` —
  сортированный список флотов хойстится один раз на спан вместо пересортировки на каждого
  стрелка (O(S·F log F) → O(F log F)); `hero.passiveBonus`/`heroByFleet` — guard на
  отсутствие героев (без аллокаций на каждую легу/раунд в безгеройном матче);
  `technology.effectsSum` — сумма инлайном без промежуточного массива дефов;
  `delta.extensionKeys` — константный Set известных ключей хойстнут в модуль. Упрощение:
  общий `NonnegativeCostSchema` (3 идентичных cost-записи героев), хелпер `chargeOrReject`
  в hero.ts (3 идентичных терминальных гейта), удалён мёртвый экспорт `sectorAppearance`.
  Отклонено сознательно: слияние `FactionPassives`≡`TechnologyEffects` (независимые оси
  тюнинга), удаление `isBuildable` (на нём открытый вопрос NOTE-1: удалить или вайрить
  `E_NOT_BUILDABLE`-гейт), стриминговый `hashState`/copy-on-write проекций (средний риск —
  отдельным кирпичом при необходимости).
- **PERF-1** ✅ **Сведено и проверено** (ветка devin `engine-optimization`, влита):
  горячий путь планировщика (`kernel.ts`: `scheduled` отсортирован → `earliestDue` O(1)
  - бинарная вставка), route-кэш в `movement`, оптимизации `combat`/`orbit`/`economy`.
    **Детерминизм сохранён** (golden-RNG + 587 тестов зелёные).

## Блок HARD · Закалка плейтест-контура (заказ владельца 2026-07-18) `[ops]` `[core]` `[srv]` `[proto]` `[sec]`

> Четыре направления: TLS+домен, автодеплой, реплей-детерминизм в CI, fuzz/property-тесты
> ядра. Детальный план с фактами из кода, рисками и порядком — `playtest-hardening-roadmap.md`
> (операционализирует HTTPS-2.1, CR-0.2, SD-7.2/7.3; PE-1.1 отложен явно). Кирпичи бери оттуда:
> **TLS-1…5** (домен→Caddy в compose→wss-приёмка), **ADEP-0…4** (починка update-пути →
> GHCR-push из CI → pull-таймер), **RPL-1…4** (формат+раннер → рекордер MatchRoom →
> CI-тест record→replay→hash), **FUZZ-1…5** (fast-check testkit → свойства
> applyAction/advanceTo/delta). Первым — ADEP-0 (сломанный `moongame update`); блоки C/D
> не пересекаются с deploy-файлами — можно параллельной сессией.

## Блок ECON · Сессионная ресурсная экономика `[proto]` `[core]` `[data]` `[srv]`

> Дизайн + детальный роадмап — `resource-economy.md` (как есть по коду, находки Д1–Д5,
> кирпичи с точками врезки/тестами/рисками). Кирпичи бери оттуда: **ECON-1** голодная
> армия (−25% урона наземки в food-arrears) → **ECON-2** блэкаут (радары/ПВО ×0.5 в
> energy-arrears) → **ECON-3** экспресс-ремонт за metal (переплавка откачена) → **ECON-4**
> рыночная комиссия 5% → **ECON-6** экономические метрики → **ECON-5** синк canonical
> бандла с играбельным (гейт перед Stage-3). Мета-слой (Суверены/аукцион) — отдельно в
> `economy-roadmap.md`.

## Блок SEC · AppSec / DevSecOps `[sec]`

> Трек безопасности — живёт рядом с продуктовым и раздаётся как задачи. База —
> `.github/workflows/security.yml` + `docs/security/pipeline.md`. Один кирпич = один сканер/правило/шаг.
> Глубокие роадмапы (задачи/подзадачи): `secure-sdlc-roadmap.md` (как строим безопасно) и
> `secure-environment-roadmap.md` (как безопасно эксплуатируем) — SEC/F-бирки сшиты с ними.

- **SEC-0** ✅ Базовый DevSecOps-пайплайн: SAST (Semgrep) + SCA (pnpm audit + osv-scanner)
  - секреты (Gitleaks) + Trivy fs + SBOM (Syft), ratcheting-гейт. Сейчас живёт в GitHub
    Actions (`security.yml`; мигрирован с GitLab — `docs/security/audit-2026-06-27.md`).
- **SEC-1** ✅ Триаж + baseline: находок — ноль (Gitleaks v8.18.4 локально + pnpm audit +
  CI-прогоны пиненных образов; единственные подавления — обоснованные base-image CVE в
  `.trivyignore`). Разобранные сканеры переведены в **блокирующие**: Semgrep (`--error`),
  Gitleaks, OSV, Trivy fs/image (`--exit-code 1`) — gate-шаг валит джобу при находке ИЛИ
  сбое скана (fail-secure), артефакты/отчёт собираются всегда. Запись триажа —
  `docs/security/pipeline.md`.
- **SEC-2** ✅ Кастомные Semgrep-правила под инварианты ядра: запрет `Math.random`/
  `Date.now`/`new Date()` и Node-built-ins в `shared-core/src` (детерминизм/чистота как
  security-граница) + два репо-широких правила из SD-2.1 (`.innerHTML = $X`, SQL-конкатенация
  в `.query()`). `.semgrep/rules/*.yaml` — одно правило = один файл + тест
  (`semgrep --test`, `// ruleid:`/`// ok:`), подключены в `security.yml` доп. `--config`
  рядом с `p/typescript`/`p/javascript`/`p/security-audit`; CI сперва гоняет
  `semgrep --test`, потом сам скан — сломанное правило валит джобу раньше находок.
  0 находок на реальном коде; 7 законных срабатываний (константные списки колонок SQL,
  уже эскейпленный `esc()`-рендер) сняты. `pattern-regex`-правило (SQL) — `// nosemgrep:
  <id> -- причина` реально убирает результат из SARIF (прецедент SEC-1); AST `patterns:`-
  правило (innerHTML) — GitHub Code Scanning не всегда уважает inSource-suppression на
  new-alert чеке PR'а (см. `security.yml`), поэтому там путь-эксклюд в самом правиле
  вместо inline-комментария (`packages/client/src/main.ts` — единственный легитимный
  `esc()`-рендер вне `prototype/`). Diff-aware `SEMGREP_BASELINE_COMMIT` из SD-2.1 сознательно не добавлен —
  пайплайн уже держит full-repo скан на нулевом бейзлайне (SEC-1), не diff-ratchet.
- **SEC-3** ✅ Безопасность самого пайплайна: пин образов сканеров по `sha256`,
  masked+protected CI-переменные, least-privilege токены. Реализовано: документация
  (image-pinning.md / ci-variables.md / setup-github-secrets.md), скрипт обновления
  дайджестов, комментарии в security.yml. Ready for PR.
- **SEC-4** ✅ _(аудитом доков — GitHub Code Scanning половина уже была реализована, не
  отмечена)_ Агрегация находок: все 8 SARIF-джоб (semgrep/gitleaks/osv/trivy fs/trivy
  image/zizmor/scorecard + нативный codeql) безусловно льют `upload-sarif` → единая
  панель `Security → Code scanning` (`continue-on-error: true`, чтобы сбой аплоада не
  валил джобу; `security-events: write` у всех 8). DefectDojo — не сделан: требует
  внешний инстанс/API-ключ, которых у нас нет; заведётся отдельным кирпичом, когда
  появится инстанс.
- **SEC-5** ✅ Container scanning: `Dockerfile` (multi-stage, пин distroless-базы) +
  живой job `trivy-image` в `security.yml` (сборка образа, `trivy image` + image-SBOM).
  _(Статус выправлен аудитом доков.)_
- **SEC-6** ✅ DAST: `dast-zap`-джоба в `security.yml` (не было закомментированной
  заготовки под этим именем — заметка была неточной, добавлена с нуля тем же
  паттерном пиненных Docker-образов, что у gitleaks/trivy/osv). Поднимает
  `packages/server` in-memory (`pnpm dev:server`, без `DATABASE_URL` — auth/gate
  дефолт-off, F1 ✅), ждёт `/health`, гоняет ZAP baseline (`ghcr.io/zaproxy/zaproxy`,
  `--network host` — цель голый node-процесс на раннере, не контейнер) через
  `zap-baseline.py -t http://localhost:8787`. Образ закреплён по sha256, дайджест
  снят живым запросом к `ghcr.io/v2` (тот же метод, что у SEC-7). Информационная
  (`continue-on-error`, ещё без триажа на реальном рантайме — как у CodeQL/TruffleHog/
  zizmor/Scorecard при первом добавлении), станет блокирующей после первого прогона
  и разбора находок. HTML+JSON отчёт — артефакт джобы; `report`-джоба и
  `summarize-security.mjs` в курсе нового ключа `dast-zap`.
- **SEC-7** ✅ _(SEC-5 ✅ — замок снят)_ Supply-chain integrity (A08): подпись
  образов `cosign` + provenance **SLSA** + проверка на деплое. Единственный
  артефакт с реальной раздачей — APK в `android.yml` (rolling GitHub Releases,
  люди сайдлоадят); `void-dominion:scan` в `security.yml` осознанно НЕ подписан —
  одноразовый образ только для сканирования, подписывать его было бы
  security-театром (анти-цель `security/pipeline.md`).
  _Первая попытка (см. git-историю) откачена:_ `actions/attest-build-provenance`
  собрала zizmor-алерт «unpinned action reference» — этот GitHub Action не входит
  в scope GitHub-MCP-сессии и `api.github.com` недоступен через WebFetch/curl (403
  «not enabled for this session»), так что sha256 экшена было нечем проверить.
  _Рабочая версия:_ `cosign sign-blob` как **закреплённый Docker-образ**
  (`ghcr.io/sigstore/cosign/cosign@sha256:b03690...`, тот же паттерн, что
  gitleaks/trivy/osv в `security.yml`) — реестровый API `ghcr.io/v2` доступен
  напрямую (в отличие от `api.github.com`), дайджест снят и перепроверен живым
  запросом, не выдуман. Keyless (GitHub OIDC → Fulcio/Rekor, без управляемого
  ключа); `continue-on-error: true` — сбой Sigstore не блокирует раздачу APK
  игрокам; `.sig`/`.cert` уходят в build-артефакты и (на `main`) в сам релиз с
  инструкцией `cosign verify-blob` в release notes. _Не проверено живым запуском_
  (нет Docker/сети до Fulcio в этой сессии — синтаксис/переменные окружения
  стандартные для Sigstore-в-GH-Actions, но CI-прогон первым подтвердит).
- **SEC-8** 🔒(Этап 7) Полный проход **OWASP Top 10 2021** по чек-листу + threat model.
- **SEC-10** ✅ [sec] Еженедельный ре-скан: `security.yml` получил `schedule: cron
  '17 5 * * 1'`. Push-триггер сертифицирует КОММИТ, а прод — это работающий образ:
  базы пинены по дайджесту, значит деплой месяцами гоняет ровно то, что просканировано
  при мерже, пока ленты CVE едут под ним. Понедельничный прогон гоняет по `main` те же
  джобы с теми же блокирующими гейтами. Минута не `:00` (GitHub роняет крон в пик, а
  молча не запустившийся ре-скан — fail-open). На scheduled-прогоне отчёт остаётся в
  job summary: PR нет, а еженедельный коммент к одному и тому же головному коммиту —
  шум (`report`-джоба выходит по `context.eventName === 'schedule'`).
- **SEC-11** ✅ [sec] Сканирование сторонних образов прода: джоба `trivy-deps`.
  `trivy-image` смотрел только НАШ образ, а в проде рядом крутятся `postgres` и `caddy`
  — их не сканировал никто. Ссылки джоба читает **из самих compose-файлов** (grep по
  `deploy/docker-compose.yml` + `.tls.yml`), а не из второго захардкоженного списка,
  поэтому бамп дайджеста не может разъехаться с тем, что сканируется. Оба образа
  заодно пинены по sha256 (дайджесты сняты живым запросом к registry API, метод
  дописан в `image-pinning.md`). Информационная на посадке — по образцу SEC-6:
  апстримный CVE в чужом образе иначе красит `main` без того, что тут можно починить.
- **SEC-12** ✅ [sec] Хардненинг рантайма контейнеров + честная запись о том, что его
  **не проверяет ни один сканер**: у Trivy misconfig нет формата Docker Compose
  (Dockerfile/k8s/terraform/cloudformation/helm/ARM), поэтому постуру держат ревью и
  чек-лист в `deploy/README.md`. Проставлено: `no-new-privileges` на всех сервисах,
  `cap_drop: [ALL]` на `server` (non-root node на порту >1024 — capabilities не нужны)
  и на `caddy` с возвратом `NET_BIND_SERVICE` (80/443), `mem_limit`/`pids_limit`
  везде, обязательный `POSTGRES_PASSWORD` в TLS-оверлее (публичный путь не поднимется
  на документированном дефолте `void`). Осознанно НЕ включено: `read_only` для
  `server` (esbuild пишет в `/app` на старте) и `cap_drop` для `postgres` (энтрипойнт
  стартует от root и роняет привилегии сам; неверный набор caps ломает БД на следующем
  рестарте) — рецепт с пятью caps лежит в README как opt-in с проверкой на тестовом
  хосте. Заодно поправлена строка A05 в `scanner-coverage-2026-07-26.md`
  («compose покрыт» — было неверно).
- **SEC-14** ✅ [sec] Триаж 71 находки, накопившейся после посадки `trivy-deps`/SEC-11.
  Полный список брался из лога джобы `report` (артефакты с SARIF недоступны — blob-хранилище
  GitHub закрыто сетевой политикой, отсюда же и дамп в лог). Вердикты: 43 подавлено как
  недостижимые (Go stdlib в `gosu` — утилита отрабатывает один раз на старте контейнера,
  до первого соединения с БД; пакеты Alpine в образе caddy — статический Go-бинарь к
  libcurl/c-ares не обращается), ~10 оставлено ОТКРЫТЫМ риском (CVE в самом Go-бинаре
  caddy: он единственный смотрит в интернет, фиксы у апстрима есть, образ не пересобран),
  5 признаны FP. Подавления живут в НОВОМ `.trivyignore-deps`, подключённом к `trivy-deps`
  через `--ignorefile`: писать чужие CVE в корневой `.trivyignore` нельзя — его читают
  блокирующие `trivy-fs`/`trivy-image` над нашим кодом, и тот же id, всплыв у нас, молча
  перестал бы валить гейт. Проверено прогоном: **71 → 20 находок** (Trivy 66 → 15). Не
  меньше, потому что формат адресует по id без пути, а пять CVE Go stdlib есть и в gosu,
  и в бинаре caddy — подавить «для gosu» значило бы спрятать их там, где риск настоящий,
  так что они оставлены видимыми (точный инструмент — `.trivyignore.yaml` с полем путей).
  Побочная находка, важнее самого триажа: находка Semgrep висела в отчёте **не потому, что
  подавление сломано** (первая версия вывода это утверждала — прогон опроверг), а потому
  что Semgrep помечает подавленное полем `suppressions` в SARIF и выходит с кодом 0, а
  `summarize-security.mjs` это поле не читал. Теперь читает: подавленные в таблицы не
  входят, их число печатается отдельной строкой. `trivy-deps` остаётся информационной,
  пока открыт риск по caddy. Запись триажа — `docs/security/pipeline.md`.
- **SEC-15** ✅ [sec] Второй SCA-движок: джоба `dependency-check` (OWASP Dependency-Check,
  образ пинен по sha256, дайджест снят живым запросом к registry). Смысл не в «ещё одном
  источнике» — NVD и так лежит в основании Trivy и osv.dev, — а в другом СПОСОБЕ
  сопоставления: OSV/Trivy опознают уязвимость по идентичности пакета, этот матчит CPE из
  фида. Принятая цена: CPE шумит на npm (⇒ информационная до триажа), `pnpm-lock.yaml` она
  не читает (⇒ джоба сперва `pnpm install` и скармливает живое `node_modules`, иначе тихо
  сканировала бы пустоту), фид качается долго (⇒ кэш каталога базы + таймаут 45 мин).
  Ключ — секрет `NVD_API_KEY`; без него скан не запускается и сентинел пишется `ok:false`,
  так что в сводке видно «НЕ подтверждён», а не тихий зелёный. `actions/cache` пинен по
  SHA, снятому `git ls-remote` (`api.github.com` из dev-среды недоступен — способ, которого
  не хватало в SEC-7).
  **Первый живой прогон (run 30579763480) упал** — и это дало две поправки, уже внесённые:
  (1) через 40 минут `Unable to obtain an exclusive lock on the H2 database` + exit 13,
  потому что контейнер шёл не от пользователя раннера и не мог лочить базу в
  примонтированном каталоге ⇒ добавлен `-u $(id -u):$(id -g)`, а префикс ключа кэша поднят
  до `v2`, чтобы не восстанавливать пустой каталог, сохранённый упавшим прогоном;
  (2) джоба снята с push и оставлена на кроне + ручном запуске — 40 минут в петле обратной
  связи по PR неприемлемы. Пропуск на push — по дизайну, и сводка печатает «⏭ пропущено
  (по расписанию/вручную)» вместо тревоги: `summarize-security.mjs` получил флаг
  `scheduledOnly` и читает `GITHUB_EVENT_NAME`, чтобы «НЕ подтверждён» продолжало означать
  ровно «скан, который должен был отработать, не отработал». _Успешный прогон и реальный
  объём находок — всё ещё впереди._
- **SEC-13** ✅ [sec/ops] Closed loop «просканировано → то же самое в проде»: воркфлоу
  `image.yml` — `build → trivy (БЛОКИРУЮЩИЙ, тот же .trivyignore) → push в GHCR →
  cosign sign по ДАЙДЖЕСТУ` (keyless, тот же пиненный образ cosign, что в SEC-7), плюс
  self-verify тем же вызовом, каким проверяет деплой. Раньше и Render, и VPS
  ПЕРЕСОБИРАЛИ Dockerfile у себя — в проде работал образ, которого сканер не видел;
  никакого дайджеста, чтобы вообще спросить «а что там крутится». Деплой-сторона:
  `deploy/verify-image.sh` (`cosign verify` по identity воркфлоу + GitHub-иссуеру,
  отказывается верифицировать тег — его можно перевесить после проверки) и оверлей
  `deploy/docker-compose.release.yml` (`image: ${VOID_IMAGE:?…}`, `pull_policy: always`,
  запуск с `--no-build`). Сборка из исходников остаётся рабочей для playtest-хостов —
  она просто не даёт этой гарантии.
  **Первый живой прогон (run #1 на коммите мержа, 2026-07-31) — цепочка прошла и упала на
  последнем шаге.** Отработали: сборка, блокирующий Trivy, пуш в GHCR (то есть
  `packages: write` в порядке, дайджест получен), сертификат Fulcio, проверка SCT, запись в
  Rekor. Упала заливка подписи: `UNAUTHORIZED: unauthenticated`. Токен ни при чём — им же
  минутой раньше прошёл `docker push`; cosign просто не увидел учётных данных. Образ
  cosign distroless и работает НЕ от root, поэтому смонтированный
  `~/.docker/config.json` в `/root/…` ему недоступен ни по пути, ни по правам (0600 у
  пользователя раннера), и он ушёл анонимом. Починено передачей `--registry-username/
  --registry-password` — это снимает вопрос о пользователе и `HOME` контейнера целиком;
  тот же класс ошибки исправлен и в `deploy/verify-image.sh` (`DOCKER_CONFIG` + запуск от
  текущего uid). Побочно: в реестре остался ОДИН неподписанный дайджест от упавшего
  прогона — задеплоить его нельзя, `verify-image.sh` откажет, так что он инертен.
- **SEC-9** ✅ Ремедиация Code Scanning (dashboard-триаж 2026-07-24): CodeQL-варнинги
  9→2 живым ре-сканом ветки. Погашены ReDoS `js/polynomial-redos` (`authApi.ts` —
  `resetBaseUrl.replace(/\/+$/,'')` → линейная посимвольная обрезка + тест) и DOM-XSS
  `js/xss-through-dom` ×6 (`prototype/` — цвета сторон `youColor`/`neutralColor` через
  `safeHexColor` validating-guard, который taint-анализ признаёт санитайзером;
  свободный текст уже под `esc()`). Добавлен корневой `SECURITY.md` (приватное
  раскрытие → Scorecard `SecurityPolicyID`). Остаток — 2 `js/missing-rate-limiting` =
  подтверждённый false-positive (кастомный `slidingWindowIpLimiter` не моделируется
  CodeQL) → dismiss в Security UI (API-токен интеграции без scope `security_events`).
- **H4-REVERT** ✅ `[core]` `[srv]` `[proto]` `[data]` Снос системы дивизий, возврат к
  отдельным наземным юнитам. Правило владельца: **«1 юнит занимает 1 трюм»**.
  _Почему._ Дивизии (H4) были надстройкой ПАРАЛЛЕЛЬНО живому легаси-пути, а не заменой
  ему — в `gameState.ts` это прямо названо документированным швом («division combat only
  engages other divisions, never the legacy garrison»). Надстройка при этом была
  частично неработоспособна на каноническом сервере: `heavy_infantry` и `special_forces`
  не существуют в `data/units.json`, а `formationStats` молча пропускает слот без записи
  в данных (`if (!def) continue`) — «Железная линия» из 6 юнитов мобилизовалась как
  2 ополченца, деньги списывались полностью.
  _Что снесено._ `modules/division.ts`, `data/formations.ts` и прототипные копии
  (`division.ts`, `divisionDesigner.ts`, `groundcombat.ts`, `formations.ts`) + 7 тестовых
  файлов; поля `GameState.divisions`/`divisionSeq`/`groundBattles`, `Player.divisionTemplates`,
  интерфейс `Division`; пять типов действий `division.*` из `payloadSchemas`; вкладка
  «Армия» конструктора, блок «Дивизии» панели мира, полигон наземного боя в dev-режиме;
  50 осиротевших ключей локали из обеих локалей.
  _Что ОСТАВЛЕНО по решению владельца._ `state/groundCombat.ts` (GROUND_ROSTER — матрица
  контр-веба) живёт как заготовка FND-4: файл ни от чего не зависит, покрыт 12 тестами,
  и его можно подключить к наземной фазе `combat.ts` отдельным кирпичом. `heavy_infantry`
  и `special_forces` остались обычными наземными юнитами прототипа — их `attack`/`defense`
  были помечены «legacy and now unread», потому что бой шёл по матрице; теперь легаси-путь
  читает именно их, и комментарий исправлен.
  _Закрытая дыра._ `BUILD_UNITS` был чисто космическим — мобилизация дивизии была
  ЕДИНСТВЕННОЙ кнопкой, поднимающей наземные войска. Без правки этого массива снос
  оставил бы игрока без второй фазы захвата вовсе. Все четыре рода войск добавлены;
  проверено в браузере: вкладка «Земля» строит ополчение/тяжпех/спецназ/танк, стартовый
  гарнизон на месте.
  _Оба вчерашних сторожа сработали как задуманы:_ манифест потребовал осознанного подъёма
  '2'→'3' и перезакрепления списка (29→28 id, с записанной ПРИЧИНОЙ рядом), опись тумана
  уронила типизацию на четырёх строках классификации и пяти местах фикстуры.
  _Баланс._ Танк шёл с `cargoSize: 3` — теперь 1, как и все наземные. Крейсер (вместимость
  5) вёз одного танка, повезёт пять; дропшип — вёз 4, повезёт 12. Вместимости кораблей
  НЕ трогались: правило игрока важнее математики, но десантная мощь выросла втрое —
  прогнать `balance-analysis` отдельным заходом.
  _Живые матчи._ Бамп манифеста + смена `data/units.json` = два независимых замка,
  ни один сохранённый матч не поднимется. Принято осознанно (живых нет); операционный
  шаг записан в `deploy/README.md`.
  Гейт: 2176 тестов, 192 файла (было 2268/199 — ушло 7 тестовых файлов).
- **GRND-1** ✅ `[proto]` **Десант — кнопка в ряду команд + меню «кого и сколько».**
  Заказ владельца дословно: «Надо бы добавить кнопку погрузки и выгрузки к остальным
  кнопкам. Нажимая на кнопку открывается небольшое меню где можно выбрать кого и
  сколько грузить или выгрузить».
  _Что было._ Секция «Наземная армия ⇄ гарнизон» в карточке флота: кнопка на КАЖДЫЙ
  стек (при повреждениях — две кнопки «Погрузить Танк»), шаг ровно 1. Поднять пятерых
  = пять тапов. Ядро при этом всегда умело `count>1` одним действием — ограничение
  жило ровно в двух строках прототипа (`loadArmy(...,1)`, `unloadArmy(...,1)`).
  _Что стало._ Кнопка **⇅ «Десант»** в cmdbar (однофлотовая, как «Делить») → поповер
  `.cmdpop` над рядом: строка на уникальный тип, «гарнизон ▸ трюм», знаковая дельта
  `▼ − N + ▲` (плюс — погрузить, минус — высадить), одно «Подтвердить». Карта и ряд
  под меню видны — это поповер, не полноэкранная модалка.
  _Новый модуль._ `prototype/src/troopsMenu.ts` — чистая модель + строковая разметка,
  26 тестов (арифметики «сколько влезет» не было покрыто вообще: вся механика
  `pendingLoads` заперта в main.ts без экспортов).
  _Инварианты, которые легко потерять._ Двойной резерв (трюм — своей очередью,
  гарнизон — очередями ВСЕХ флотов у мира); только здоровый стек с дефолтным
  лоадаутом (`findHealthyStack`, как ядро); `floor(free / cargoSize)`, а не `free`;
  строки конкурируют за одно место; выгрузка в том же плане освобождает трюм под
  погрузку. Шаг клампится, а не блокируется — идиома `+10` из диалога разделения.
  _Почему НЕ батч._ Погрузка кладёт `count` отдельных записей в очередь, а не одну:
  ядро грузит «всё или ничего» (`army.test.ts:141`), и партия одним действием
  отскочила бы целиком, если гарнизон за этот час обмелел. Выгрузка мгновенна и идёт
  одним `army.unload {count}` на тип — там ждать нечего.
  _Fail-secure._ `garrisonUnderAssault` проверяется на входе (`troopsLiftable`), а не
  через игровой час отказом `E_UNDER_ASSAULT`; новый ключ `cargo.under-assault`.
  Причина серой кнопки НАПИСАНА в меню (`cargo.hold-full` / `cargo.garrison-empty`):
  клик по disabled в ряду не всплывает, объяснить через `note()` уже нечем.
  _Правки-спутники._ Секция карточки флота осталась сводкой без кнопок (прецедент
  подписан в коде — SO-UI); текст тура `onb.tour.first.troops` вёл по снесённому пути
  и переписан в обеих локалях; орфаны собственного сноса убраны (`beginLoad`,
  `holdFree`). 10 новых ключей, ноль осиротевших.
  _Проверено вживую_ (412×915, RU, реальная сборка): ⇅ активна только у своего мира,
  «до упора» набирает 2 ополченца, шапка `трюм 2/11 · заказано +2`, Подтвердить →
  «⏳ грузится: 2» → через игровой час `ополчение 0 ▸ 2`; ▼ → мгновенно `2 ▸ 0`;
  Escape закрывает меню, не роняя матч. Два дефекта поймала именно живая проверка:
  глифы `⇊`/`⇈` на 13px читались как «ц»/«п» (заменены на ▼/▲), и сноска «трюм полон»
  вылезала при ПУСТОМ трюме 0/11 — флаг ловил «все строки на максимуме», а максимум
  упирался в гарнизон, а не в место (регресс закрыт тестом).
  Гейт: 2202 теста, 193 файла.
  _Остаток._ `⇅ Десант` и «Делить» — единственные однофлотовые команды в ряду;
  групповая погрузка (выделил три флота — набил все) не делалась и не заказывалась.
- **CHAIN-UX** ✅ `[proto]` **Режим «Приказ» — цепочка тапами по карте с иконками и
  временем.** Заказ владельца дословно: «кнопка приказ, на мобиле убирается нижний хаб
  и тыкаешь точку на карте и в зависимости от того что это, там появится возможные
  действия. На карте рисуется вся эта цепочка и в точках действия иконки приказов и
  показ времени до исполнения. И так приказ за приказом можно сделать».
  _Что было._ Ядро CC-1 уже многоточечное (каждый шаг несёт свою цель, `order.chain`
  ставит план атомарно) — но UI (TGT-1) прибивал план к ОДНОЙ цели: одноразовый прицел
  «◎ Цель», фиксированный набор чипов без контекста точки, на карте только прицел на
  якорном мире — ни линии, ни иконок шагов, ни времени.
  _Что стало._ «◎ Приказ» — персистентный режим: нижний хаб на мобиле убирается
  (`body.chain-mode`), полоска плана живёт в ноде #cmdbar (4 медиа-раскладки
  бесплатно), тап по точке открывает меню У ТОЧКИ по её типу (свой/нейтральный мир —
  курс+⏱; вражеский — +штурм+огневое окно с гейтом по артиллерии; вражеский флот —
  огонь; герой — ★ способности), цепочка рисуется целиком: полилиния по маршрутам,
  капсулы-глифы, накопленное «~T» (голова отправленного плана авторитетна по
  arrivesAt/waitUntil, хвост — оценка). ◎-бейдж плана — тап открывает редактирование.
  _Ядро НЕ тронуто_ — вся работа в prototype/. Дизайн выбран состязательно (3 варианта
  × 3 судьи): «меню у точки» победил как единственный, делающий заказ дословно; из
  проигравших пересажены ⟲ (откат ЖЕСТА целиком — курс+штурм одной кнопкой), рост
  часов повторными тапами без закрытия меню, «~T» через textContent вне HTML-сигнатуры
  полоски (тикающее время не перестраивает DOM и не ест тапы), меню едет с камерой
  покадровой позицией (болезнь старого композера, а не обход), ⚠ при разных планах
  группы, гейт «нет артиллерии» с причиной В пункте меню.
  _Инварианты._ Режим ПЕРВОЙ ветвью в topLayerOpen/closeTopLayer (двухступенчатый
  Back: меню → режим; у targetAim/tgtEditor этой регистрации не было — дыра класса
  ST-4 закрыта самой заменой); первая ветка selectAt; самогашение при пропаже флотов;
  сброс в installMatch и на net-welcome; long-press исключён в режиме; радиусы хитов
  те же, что у selectAt. Времена — только из `s.time`/`waitUntil`/`arrivesAt` и
  `estimateTravelHours` (детерминированная оценка, всегда с «~»).
  _Чистый модуль._ `prototype/src/chainPlanner.ts` (жесты/меню/таймлайн/разметка) +
  24 теста; в gateparity добавлен позитивный сэмпл `orderChain` через реальный билдер —
  разведка нашла, что его НЕ было, и дрейф формы шага всплыл бы только на боевом
  gated-сервере.
  _Локаль._ 6 новых `chain.*`; `cmd.target` → «Приказ», `hint.pick-order` переписан;
  4 сироты сняты из обеих локалей (`tgt.empty`, `tgt.step.remove`, `tgt.step.fire`,
  `hint.no-target`); остальные `tgt.*` переиспользованы. Стале-баннер
  `command-chains-roadmap.md` реконсилирован (утверждал «цепочек в игре нет» — протух
  ещё 2026-07-21).
  _Остаток._ «Обстрел МИРА» шагом цепочки невыразим — `strike/barrage` целятся во
  вражеские ФЛОТЫ (`fleet.bombard` в словаре шагов нет); если понадобится — один новый
  вид шага в трёх синхронных копиях + драйвере, манифест не тронется. Канонический
  сервер `packages/server` цепочки не исполняет (было так и до этого кирпича).
- **SND-1** ✅ `[proto]` **Синтезированные звуки интерфейса.** Заказ владельца:
  «ненавязчивые консольные звуки при взаимодействии с интерфейсом, с примесью
  мистического тёмного космоса и помпезностью космической оперы». Первый звук в
  игре вообще — закрывает часть engineering-review.md §4.3 (там же требование
  «все отключаемые» — выполнено тумблером и громкостью в настройках).
  _Как._ Ноль аудио-ассетов: `prototype/src/sound.ts` синтезирует всё WebAudio-графом,
  патчи — чистые данные (5 жестов: `tap` тап по кнопке, `close` закрытие слоя,
  `error` отказ действия, `send` отправка приказа, `start` вход в матч). «Космос» —
  двойная feedback-задержка через lowpass 1100 Гц + суб-тоны; «опера» — чистые квинты
  (тест буквально проверяет ×1.5 в данных send/start) и расстроенные пары осцилляторов.
  «Ненавязчивость» — не вкус, а инварианты: тесты держат длительности (тап < 0.35с …
  фанфара < 2.2с), потолок суммы пиков ≤ 0.6, атаки ≥ 2 мс (без щелчков), эхо ≤ 0.5;
  объективная мера с живой сборки (OfflineAudioContext): пики −26…−29 dBFS,
  RMS −42…−54, хвосты 0.6–1.6 с.
  _Куда вшито (7 точек)._ Один document-слушатель кликов на всплытии с фильтром
  `ev.isTrusted` (синтетические `.click()` не звучат; long-press глушится существующим
  stopPropagation на capture); отказ — в двух воронках ошибок (соло `playerOrder` и
  сетевой `onRejection`), НЕ внутри `note()` — тот несёт и инфо-тосты, и 2с-дедуп;
  `send` — в chsend; `start` — в installMatch и в сетевом admitted с гейтом
  `!reconnecting` (читается ДО сброса флага); `close` — три вызова `closeTopLayer`
  (popstate / Escape / кнопка «назад»).
  _Fail-SILENT, не fail-secure._ Звук — декорация: любой сбой WebAudio (нет
  AudioContext, autoplay-политика) даёт тишину, не сломанный интерфейс; инверсия
  правила №4 подписана в коде комментарием.
  _Ловушка, пойманная живой проверкой._ `Number(null) === 0`, не NaN — отсутствующий
  `void.sndVol` без явной проверки проходил кламп 0..1 и рождал громкость 0 на каждом
  ЧИСТОМ профиле; юнит-тест с `initSound(null)` это маскировал (`getItem` там вообще
  не звался). Закрыто явным `raw == null ? NaN : Number(raw)` + регресс с fake-store,
  возвращающим `null`.
  _Настройки._ Секция «Звук» (тумблер + слайдер громкости с превью-тапом на input),
  ключи `settings.snd.*`, персист `void.snd`/`void.sndVol`; выключение суспендит
  AudioContext, а не просто обнуляет громкость.
  Гейт: 2278 тестов, 196 файлов (на базе с REFM-13).
  _Остаток._ Звук только в `prototype/`; app shell `packages/client` не озвучен (не
  заказывался). Из §4.3 не сделаны haptics, ambient-фон и нарастающие alert-звуки
  атак. `#settings` не в реестре Back-слоёв (доигровой экран) — закрытие настроек
  не озвучено вместе с остальными слоями.
- **HUD-DOCK** ✅ `[proto]` **Низ экрана ведёт себя как одно целое.** Заказ владельца
  (два дефекта одной фразой): «добавь убирание нижнего хаба, когда нажимаешь кнопку
  движения; хаб с кнопками должен быть привязан к нижнему хабу, а то баг — между ними
  иногда появляется большое пространство».
  _Дефект 1 — «Курс» был исключением._ Лист (`#side`) прячется, когда карта становится
  рабочей поверхностью: так уже вели себя слияние, набор группы (SEL-1) и режим
  «Приказ» (CHAIN-UX). Движение — самый частый приказ в игре — в этот список не входило:
  игрок жал ⤳ и тапал в лист, закрывавший пол-карты. Теперь признак ОДИН на все режимы
  (`hudDock.mapIsWorkspace`), и на телефоне вместе с листом уезжает нижний хаб
  (`body.aim-mode` #rail/#speedbar/#goals — те же правила, что у chain-mode).
  _Дефект 2 — ряд стоял на догадке._ `#cmdbar` поднимался над листом на фиксированные
  `34vh/50vh/22.5vh`, а лист приклеен к низу и растёт ПО СОДЕРЖИМОМУ при
  `max-height:той же величины`. Короткий лист → дыра. Замер на живой сборке (412×915):
  лист 422px → зазор **44px**, лист 295px → зазор **171px** (треть экрана — отсюда
  «иногда»). Теперь высота листа меряется `ResizeObserver` и уезжает в `--sheeth`,
  vh остались фолбэком до первого замера: в обоих случаях зазор **8px**, ровно
  задуманный отступ. Проверено и то, что привязка живёт: приказ сменил высоту листа
  422 → 295 прямо в кадре, зазор остался 8.
  _Ловушки, которые всплыли._ Копий правила оказалось не две, а ЧЕТЫРЕ (базовая, телефон,
  короткий экран, PC-раскладка) — четвёртую (`22.5vh`) нашёл собственный тест, а не глаз;
  это и делало баг «иногда» (зависел от раскладки). Нулевую высоту от `display:none`
  наблюдатель игнорирует — иначе ряд на кадр падал бы на дно при каждом открытии листа.
  Берётся `borderBoxSize`, а не `contentRect`: у листа в высоте сидит safe-area телефона,
  и ряд наехал бы на него снизу.
  _Сторожа._ `prototype/src/hudDock.ts` (чистая модель) + 9 тестов: режимы прицела,
  кламп значения, уезжающего в `calc()` (мусор там роняет ВСЁ правило, а не одно
  свойство), и скан самой CSS — любое правило, поднимающее ряд над листом, обязано
  считать от `--sheeth`, иначе догадка в vh вернётся незаметно.
  Гейт: 2287 тестов, 197 файлов. Локаль не тронута (текста кирпич не добавляет).
  _Остаток._ `chain-mode` оставлен своим классом (механизм соседнего кирпича, переписывать
  без заказа не стал) — в режиме «Приказ» висят оба класса, правила одинаковые. Слияние и
  набор группы теперь тоже убирают нижний хаб на телефоне — прямое следствие общего
  признака, раньше они прятали только лист. `#goals` на телефоне всё ещё отстоит от низа
  на догадку `120px` (высота ряда) — тот же класс дефекта, в заказ не входил.
- **SEC-16** ✅ `[core]` `[srv]` Два сторожа под авто-мердж: правила, которые до сих пор
  держались на памяти ревьюера, переведены в гейт.
  **Сторож тумана** (`shared-core/state/fogInventory.test.ts`): фог-проекция — чёрный
  список, поэтому новое поле `GameState` публично по умолчанию (так утекали
  `player.scientists`, `state.capital`, `divisions`). Опись `Record<keyof GameState, …>` /
  `Record<keyof Player, …>` роняет типизацию на новом поле, пока автор не выберет
  `public`/`stripped`/`filtered`, а три проверки бьют по решению (`stripped` нет,
  `public` бит в бит, `filtered` ОТЛИЧАЕТСЯ от исходного) + скан канареек `CANARY_*`
  по всей проекции. Первая же находка сторожа: **`state.rng` уезжал клиенту** — поток
  костей мира, по которому предсказываются будущие броски; вырезан (кернел на клиенте
  не гоняется, в `hashState` поле не входит).
  **Сторож манифеста** (`server/moduleManifest.test.ts` + 3 теста загрузчика в
  `f8-persistence.test.ts`): проверка `MODULE_MANIFEST_VERSION` на загрузке (PR #434)
  бессильна, если правку `DEV_MODULES` не сопроводили подъёмом версии, — теперь состав
  и порядок закреплены списком id рядом с версией, и правка графа модулей чинится
  только осознанным подъёмом. Заодно закрыт тестовый пробел #434: сам загрузчик
  (грузится / отказывает на старом манифесте / отказывает без манифеста) не был покрыт.
  _Следующий слой (не сделан):_ перевести блокирующие сканеры и `smoke:browser` из
  `continue-on-error` в обязательные — сейчас авто-мердж их мнение игнорирует.

> **Зонтичный план** над треками SEC/SD/SE/GI — `security-master-plan.md`: north star
> «безопасное — единственно возможное», сквозная модель угроз (приложение + игра),
> тир-план (Tier 0–4, сшит с бирками) и гейт «secure-enough to launch». Восемь новых
> предложений оформлены как бирки **MP-1..MP-8** (secure-by-default launch guard;
> shadow re-simulation из action-log; economy-conservation property-инвариант; подпись
> game-data бандлов; Trusted Types-first клиент; anti-automation регистрации;
> PII-firewall логов; refresh-token rotation с reuse-detection) — детали и статусы
> живут в самом плане, раздаются как обычные кирпичи.

## Блок NET-A2 · Аудит клиент-серверного слоя (netcode, 2026-07-22) `[proto]` `[srv]` `[cli]`

> Полный аудит клиент-серверного взаимодействия (протокол/хендшейк/комната/реконнект)
> в 4 под-области, каждая находка верифицирована по коду. Три P0-бага уже закрыты в
> том же заходе (ниже ✅); остальное нарезано на маленькие одно-зонные кирпичи. Три
> сквозных критерия для КАЖДОГО кирпича: **простота логики · тестируемость · без
> дублирования** (устранить дрейф — один фикс не должен требовать ручного зеркала).

- **NETA2-0a** ✅ `[srv]` Начисление XP на reject-but-advanced: `observeEndIfNeeded` не
  звался, когда счётная/доминирующая победа кончает матч на catch-up-advance игрового
  действия (оно реджектится `E_MATCH_ENDED`) → EC-* не банкил. Добавлен в обе ветки
  (sync+durable, идемпотентно) + тест.
- **NETA2-0b** ✅ `[cli]` Клиент ре-шлёт конверт и на `E_UNAVAILABLE` (сервер откатывает
  seq на транзиентном сбое стора; иначе — вечный `E_OUT_OF_ORDER`) + тест.
- **NETA2-0c** ✅ `[proto]` `playerOrder` в net-матче на реконнекте отклоняет приказ с
  фидбеком, а не применяет локально (blind-order → тихая потеря на resync).
- **NETA2-0d** ✅ `[srv]` `ping.clientTime` требует `Number.isFinite` (как desync/perf).
- **NETA2-1** ✅ `[srv]` `[cli]` **Прозрачные причины отказа хендшейка** (это «connection
  trouble»). Все отказы шли через `rejectUpgrade` (сырой HTTP + `destroy()` до открытия
  сокета), браузер прячет статус → клиент видел только «сервер недоступен». Для НЕ-security
  отказов, которые публичны через `GET /matches` (**матч полон** `E_MATCH_FULL` / **вход
  закрыт** `E_ENTRY_CLOSED`) — `refuseWithReason`: завершить upgrade, послать `{type:error,
  code}`, закрыть (зеркалит путь `E_SLOT_TAKEN` в MatchRoom; CloseEvent+сообщение читаемы).
  Security-отказы (токен/Origin/тикет) остаются `rejectUpgrade` — сокет не открывается для
  неаутентифицированного/cross-origin пира, ничего не течёт. Клиент показывает причину
  (main.ts `onError`). Тесты: wsServer (полный матч → `{type:error,E_MATCH_FULL}`), seatLock
  (3 контракта переведены с сырого 409/403 на читаемый код).
- **NETA2-2** ✅ `[proto]` **Бюджет реконнекта > окна reap'а сокета**. При ungraceful-дропе
  реконнект реджектился `E_SLOT_TAKEN`, пока хартбит (~2×15с ≈ 30с) не подберёт старый сокет,
  а старые 6 попыток укладывались в ~31с → сдавался ровно на освобождении места. Бэкофф вынесен
  в чистый модуль `prototype/src/reconnect.ts` (`reconnectDelayMs`): 8 попыток (1,2,4,8,8,8,8,8с,
  бюджет ~47с) — несколько попыток теперь ФАЙРЯТСЯ ПОСЛЕ закрытия окна reap (31/39/47с), а не
  одна ровно на границе. `main.ts` зовёт чистую функцию вместо inline-формулы. Тест
  `reconnect.test.ts`: ≥2 попытки после `REAP_WINDOW_MS` + бюджет клирит окно с запасом.
- **NETA2-3** ✅ `[srv]` **netserver не дублирует запись квитанций**. На durable-пути квитанцию
  писали ОБА: `persist` (commit-before-broadcast) и `observe`-хендлер. Теперь наблюдение
  `action` несёт флаг **`durable`** (ставится на всех 4 ветках `commitApply`; sync-путь
  `recordReceipt` его не ставит), а netserver сохраняет квитанцию только когда `!durable` —
  durable-путь пишет один раз (`persist`), sync-путь (серверные драйверы / ungated) — по-прежнему
  durable через `observe`. Тест: durable-действие → наблюдение помечено `durable:true`, sync — нет.
- **NETA2-4** 🔶 `[srv]` `[cli]` **Единый источник wire-протокола** (частично; переоценён по
  факту). L3 ✅: **мёртвое поле `events` на `state`-сообщениях снято** — полный snapshot это
  РЕСИНК (join / desync-recovery / дедуп-ретрай), клиент перестраивает базлайн целиком, а
  события едет `delta` (единственное место, где клиент их читает). **Про «одну декларацию»:**
  предпосылка «продублированы и уже разошлись» устарела — дрейф УЖЕ ловится компилятором
  через `wireParity.test.ts` (type-only Mutual-ассерты client↔server; клиент СОЗНАТЕЛЬНО без
  runtime-зависимости от сервера, чтоб серверный код не тёк в бандл). Т.е. «готово, когда
  компилятор ловит дрейф» уже выполнено. Настоящее слияние в одну декларацию = вынести ВЕСЬ
  wire-контракт в общий пакет `@void/protocol` + размиррорить клиентские копии — это
  структурный рефактор с новым пакетом, не полировка; отдельным кирпичом, не в этом заходе
  (тем более на разошедшейся ветке). **Готово, когда:** (если делать) `@void/protocol`, оба
  импортируют, `wireParity` схлопывается в тавтологию/снимается.
- **NETA2-5** 🔒(NETA2-4) `[proto]` `[cli]` **Прототип использует outbox транспорта на
  реконнекте**. Переиспользовать ОДИН `MultiplayerClient` + `connectionLost()` вместо
  hand-rolled пересоздания — приказ в момент дропа очередится и досылается на welcome
  (правильный корень NETA2-0c: очередь вместо отказа). Убирает второй транспорт-путь.
  **Готово, когда:** приказ на реконнекте не теряется и не подделывается; тест очереди.
- **NETA2-6** ✅ `[srv]` `[proto]` **Один оффлайн-шедулер**. `clockDriver.ts` и inline-драйвер
  netserver'а разошлись (кэп таймера + хартбит): боевой хост `packages/server` морозил
  часы у подключённых (нет хартбита). Хартбит внесён в общий `startClockDriver` (`heartbeatMs`
  + `maxDelayMs`; гейт `room.isClockRunning && peerCount > 0` — новый геттер `isClockRunning`,
  т.к. plain dev-матч крутит часы, но никогда не «started», так что `isStarted` заморозил бы
  боевой хост); `onTick` теперь отдаёт `{progressed}`, чтоб хост скипал свои драйверы на
  стойле. Боевой хост (`serverWiring`) получил `heartbeatMs` → больше не морозит; netserver
  перешёл на `startClockDriver` и удалил свой inline-драйвер (~110 строк) — один шедулер на
  оба хоста. Тесты: 12 (арм-логика на stub-комнате — бьётся/идлит/кэп/`progressed`/стоп по
  уходу последнего пира; сквозной — свежий watched dev-матч крутит часы без действий игрока).
- **NETA2-7** ✅ `[srv]` **Один джойн-хендшейк**. netserver переиспользует `registerMatchApi`
  вместо hand-rolled `GET /matches/:id/join` — теперь **per-IP rate-limit** (30/мин, которого
  inline-роут НЕ имел), identity-гейт и error→status-маппинг в одном месте с боевым хостом.
  `createMatch` сделан **опциональным** (netserver сеет матчи вне API → `POST /matches` не
  монтируется). Окно входа (SES-2.3) осталось в netserver-`join`-депе как `E_ENTRY_CLOSED`,
  чтобы сохранить порядок `E_NO_MATCH`→`E_ENTRY_CLOSED`→`E_MATCH_FULL` (боевой `join` AvA-осведомлён,
  своего окна входа не имеет). Тесты: matchApi (`E_ENTRY_CLOSED`/`E_NOT_ROSTERED`→403,
  createMatch-опционален → `POST /matches` 404 + join жив); живой прогон netserver — session-join
  200, ghost 404, no-auth 401, `POST /matches` 404, реконнект 200.
- **NETA2-8** 🔒(тесты) `[srv]` **Единое apply-ядро**. `applyAndBroadcast` (sync) и
  `commitApply` (durable) — две реализации одной оркестрации (advance→apply→reject-advanced→
  success→observeEnd→timing); dedup-фронт строен вчетверо. Извлечь общее ядро,
  параметризованное commit-стратегией (структурная причина дрейфа NETA2-0a и seq-расхождения).
  Крупный/рискованный — много тестов. **Готово, когда:** одна оркестрация, ветки не дрейфуют.
- **NETA2-9** ✅ `[srv]` `[cli]` **Полировка протокола** (LOW-пачка) — сделаны 3 из 5:
  (L1) комментарий `envelopeActionId` больше не врёт про несуществующий clientSeq-fallback;
  (L2) `ping.clear` с не-строковым `pingId` → `E_BAD_MESSAGE`, а не тихая эскалация в
  «стереть все» (тест `protocol.test.ts`); (L5) **обсёрвер обёрнут один раз при присвоении** —
  бросающий метрик-консьюмер НЕ роняет коммит/рассылку (инвариант «метрики — только
  наблюдение» теперь ЭНФОРСИТСЯ, а не только задекларирован; тест на бросающий observer).
  Остаток осознанно перенесён: **L3** (мёртвое поле `events` на `state`-сообщениях) →
  ✅ сделано в **NETA2-4** (поле снято; snapshot это ресинк, события едут только `delta`);
  **L4** (seq reject-advanced sync vs durable) — клиент терпит
  (дропает только `seq < lastSeq`), переупорядочивание курсора в sync-пути = косметика с
  риском, ценность ~0 → отложено (не «чинить» рабочее).
- **NETA2-10** ✅ `[srv]` **Восстановление seat-ticket под `SEAT_LOCK`**. Развилка: «переиздание
  владельцу» отпало — nick+ticket остаётся ЕДИНСТВЕННОЙ личностью на этом безаккаунтном пути,
  так что самообслуживание («это моя ставка, дай новый билет») неотличимо от кражи чужого
  места и подрывает саму цель замка. Сделан «мягкий путь»: `AccountStore.resetSeatTicket(room,
  nick)` (Memory + Postgres, `store/types.ts`) чистит `ticket_hash` — следующий вход того же
  ника минтит билет заново тем же кодовым путём, что «место занято до появления замка»
  (`wsServer.ts`, ветка `stored === null`). Сеть НЕ трогает — вызывается только оператором
  (ops-раннбук `deploy/README.md`, был undocumented raw SQL, теперь бьёт по протестированному
  методу). Тесты: store-контракт (`store.test.ts` — bind→reset→seatTicket снова `null`, сама
  посадка (`nick → playerId`) не трогается, следующий bind выигрывает заново как первый, сброс
  несуществующей/непривязанной пары — no-op). `[cli]` из зоны кирпича не понадобился: клиент уже
  корректно ловит 401 и не требует правок для оператор-инициированного пути.

- **NETA2-mon** ✅ `[srv]` **Сигналы сбоев наружу + durable-логи**. `MetricsAggregator` уже
  собирал десинки / реджекты-по-кодам / dead-letters / overflow / worst-fps, но наружу
  торчали только gauge'ы (`/metrics`) — детали были видны лишь в JSONL после выхода. Новый
  `GET /metrics/summary` отдаёт сводку живьём (process-wide, без match-id/PII — публично, как
  `/metrics`/`/health`), так «ловить сбои» = один `curl`. Плюс JSONL `playtest-logs/` на
  named-volume (`void-logs`) в compose — переживает рестарт/редеплой. Баннер печатает URL.
  Тест: смешанный поток → все сигналы сбоев в одной сводке. **Готово:** ✅.

> **Порядок исполнения** (ценность×простота): NETA2-1 → 9 → 3 → 6 → 4 → 5 → 7 → 2 → 10 → 8.

---

## Блок REFP · Рефактор `prototype/src/game.ts` — вынос секций в отдельные файлы `[proto]`

> `game.ts` = 5289 строк, 167 экспортов, 34 секции — god-file. Блок дробит его на
> каталог файлов по зонам (данные/модули/оркестрация/AI), сохраняя public API через
> re-export из `game.ts` (потребители `main.ts`/`netserver.ts`/тесты не меняются).
> Финальный кирпич (REFP-28) убирает re-export'ы и переводит импорты прямо.
> Порядок фаз — от листьев (чистые функции/данные) к корню (сборка/оркестрация).
> Принцип: 1 кирпич = 1 PR = 1 сессия, `pnpm run check` зелёный, ✅ + `state.md` в том же PR.

- **REFP-1** ✅ `[proto]` **`prototypeData.ts`** — инлайн `parseGameData({...})` каталог
  контента (технологии/учёные/юниты/модули/фракции/здания/события/секторы/типы планет/
  герои/способности/пассивки/деревья/фиттинги) вынесен в `prototype/src/prototypeData.ts`.
  Блок не имел внутренних зависимостей от rest-of-game.ts; `game.ts` импортирует `data`
  и реэкспортирует. `game.ts`: −1070 строк. Гейт зелёный (1830 тестов).
- **REFP-2** ✅ `[proto]` **`map.ts`** — `SectorType`, `SECTOR_TYPES`, `MapNode`,
  `START_CANDIDATES`, `MAP` (сектор-типы + генерация 11×11 карты) вынесены в
  `prototype/src/map.ts`. `game.ts` импортирует и реэкспортирует. Гейт зелёный.
- **REFP-3** ✅ `[proto]` **`fleetStacks.ts`** — `loadoutKey`, `takeFromStacks`,
  `mergeStacks` (чистые хелперы над `UnitStack[]`) вынесены в
  `prototype/src/fleetStacks.ts`. `game.ts` импортирует. Гейт зелёный.
- **REFP-4** ✅ `[proto]` **`tax.ts`** — `TAX_*`, `isInhabited`, `civicTax`,
  `inhabitedWorldCount`, `taxModule` вынесены в `prototype/src/tax.ts`.
  `game.ts` импортирует `taxModule` для `MODULES` и реэкспортирует. Гейт зелёный.
- **REFP-5** ✅ `[proto]` **`formations.ts`** — `FORMATION_*`, `FormationTemplate`,
  `OfficerTemplate`, `DEFAULT_TEMPLATES`, `OFFICER_TEMPLATES`, `formationStats`
  вынесены в `prototype/src/formations.ts`. `game.ts` импортирует и реэкспортирует.
  Гейт зелёный.
- **REFP-6** ✅ `[proto]` **`botFavour.ts`** — `FAVOUR_*`, `botFavour`,
  `botEmbargoes` вынесены в `prototype/src/botFavour.ts`. `game.ts` импортирует и
  реэкспортирует. Гейт зелёный.
- **REFP-7** ✅ `[proto]` **`squadron.ts`** — `squadronTake`, `SortieState`,
  `sortieSpec`, `freshSortie`, `canSortie`, `spendSortie`, `tickRearm`,
  `fleetHasSquadron`, `squadronStrikeRange`, `withinRange`, `squadronReaches`
  вынесены в `prototype/src/squadron.ts`. `game.ts` импортирует и реэкспортирует.
  Гейт зелёный.
- **REFP-8** ✅ `[proto]` **`chain.ts`** — `fleetIdle`, `ChainStep`, `FleetChain`,
  `MAX_CHAIN_*`, `validateChainSteps` вынесены в `prototype/src/chain.ts`.
  `game.ts` импортирует и реэкспортирует. Гейт зелёный. **Фаза 1 завершена.**
- **REFP-9** ✅ `[proto]` **`hunger.ts`** — `HUNGER_MULT`, `hungerModule` вынесены
  в `prototype/src/hunger.ts`. `game.ts` импортирует и реэкспортирует. Гейт зелёный.
- **REFP-10** ✅ `[proto]` **`fleetLaunch.ts`** — `fleetLaunchModule` (`fleet.launch`/
  `unit.built` auto-rally/`fleet.merge`/`fleet.split`/`fleet.engage`) + monotonic
  fleet-id counter вынесены в `prototype/src/fleetLaunch.ts`. `divisionsOf` остаётся
  импортом ИЗ `game.ts` (division-состояние ещё не вынесено, REFP-13) — обратное ребро
  безопасно: читается только внутри тела хендлера, не на модульной инициализации.
  `game.ts` импортирует `fleetLaunchModule` для `MODULES` и реэкспортирует. Гейт зелёный
  (175 файлов, 1894 теста — число не изменилось, чистый перенос без нового поведения).
- **REFP-11** ✅ `[proto]` **`botDiplomacy.ts`** — `botDiplomacyModule` вынесен в
  `prototype/src/botDiplomacy.ts` (зависит от `botFavour` + core diplomacy helpers).
  `game.ts` импортирует для `MODULES`. Гейт зелёный.
- **REFP-12** ✅ `[proto]` **`sessionMarket.ts`** — `MARKET_*`, `MarketLot`,
  `marketLots`, `marketModule` вынесены в `prototype/src/sessionMarket.ts`.
  `game.ts` импортирует и реэкспортирует. Гейт зелёный.
- **REFP-13** 🗑 `[proto]` **`division.ts`** — `Division`, `divisionsOf`, `templatesOf`,
  🗑 **СНЯТО (H4-REVERT, 2026-07-31).** Игра вернулась к системе отдельных наземных
  юнитов: они строятся как обычные юниты, стоят в гарнизоне и ездят в трюме флота
  (`armyModule`), десант разрешает `combatModule`. Текст кирпича ниже оставлен как
  запись о проделанной работе — но описанной механики в игре БОЛЬШЕ НЕТ.
  `regenDivision`, `divisionCargo`, `fleetCargoFree`, `GROUND_TICK_HOURS`,
  `REGEN_PER_UNIT_PER_DAY`, тиковый наземный бой и `divisionModule` вынесены в
  `prototype/src/division.ts` (чистый перенос; часовые константы отзеркалены локально
  по образцу `serverDrivers.ts` — без обратного ребра). Узкая проекция состояния
  `DivisionState` (divisions/divisionSeq/templates/groundBattles) живёт в самом файле;
  `DivState` в `game.ts` ужат до полей, которые тот ещё читает (heroRoster + patrols).
  Обратное ребро `fleetLaunch.ts → game.ts` (`divisionsOf`) переключено на
  `./division` — из трёх рёбер осталось два (`economy.ts` HOUR, `groundcombat.ts`
  тип FormationUnit). `game.ts` импортирует `divisionModule` для MODULES и
  реэкспортирует публичные имена; `game.ts` 1851→1326 строк. `heroRosterOf` остался
  в `game.ts` (домен героев, соседство было текстовым). Гейт зелёный (1993 теста —
  число не изменилось, чистый перенос). `capitalOf` из исходного текста кирпича давно
  живёт в `capital.ts` (REFP-14).
- **REFP-14** ✅ `[proto]` **`capital.ts`** — `capitalsOf`, `capitalOf`,
  `capitalModule` вынесены в `prototype/src/capital.ts`. `game.ts` импортирует и
  реэкспортирует. Гейт зелёный.
- **REFP-15** ✅ `[proto]` **`standingOrders.ts`** — `standingOrdersModule` вынесен в
  `prototype/src/standingOrders.ts`. `game.ts` импортирует и реэкспортирует. Гейт
  зелёный (PR #368, `standingorders.test.ts`).
- **REFP-16** ✅ `[proto]` **`forcedMarch.ts`** — `FORCED_MARCH_*`, `forcedMarchModule`
  вынесены в `prototype/src/forcedMarch.ts`. `game.ts` импортирует и реэкспортирует.
  Гейт зелёный (PR #368, `forcedmarch.test.ts`).
- **REFP-17** ✅ `[proto]` **`instantRepair.ts`** — `INSTANT_REPAIR_*`, `missingHull`,
  `instantRepairCost`, `instantRepairModule` вынесены в `prototype/src/instantRepair.ts`.
  `game.ts` импортирует и реэкспортирует. Гейт зелёный (PR #368, `instantrepair.test.ts`).
- **REFP-18** ✅ `[proto]` **`econScrews.ts`** — `REPAIR_HP_PER_METAL`, `dockRepairCost`,
  `fleetAtOwnDock`, `econScrewsModule` вынесены в `prototype/src/econScrews.ts`.
  `game.ts` импортирует и реэкспортирует. Гейт зелёный (PR #368, `econscrews.test.ts`).
- **REFP-19** ✅ `[proto]` **`economy.ts`** — `economySnapshot`/`netIncome`/`hpOfLevel`
  вынесены в `prototype/src/economy.ts` (зависели только от `data` + shared-core утилит +
  `tax.ts`, REFP-4). `HOUR` остаётся импортом ИЗ `game.ts` (плейн реэкспортнутая
  константа, читается только внутри тела функции — обратное ребро безопасно). `game.ts`
  импортирует все три для `newGame`/AI и реэкспортирует для `main.ts`/`netserver.ts`/
  тестов. Гейт зелёный (175 файлов, 1894 теста — число не изменилось, чистый перенос).
- **REFP-20** ✅ `[proto]` **`matchSetup.ts`** — `SeatConfig`/`SetupConfig`/
  `DEFAULT_SETUP`/`NetworkMatchMode`/`parseNetworkMatchMode`/`networkSeats`/`newGame`
  вынесены в `prototype/src/matchSetup.ts` вместе с приватными хелперами `player`/
  `fleet`/`ARCHETYPE_OF_GRADE` (использовались только внутри `newGame`, экспортировать
  из `game.ts` смысла не было — уехали с вызывающим кодом). Зависел только от уже
  вынесенных файлов (map/formations/botFavour/heroes/ships/economy) + prototypeData +
  shared-core. `game.ts` импортирует для внутреннего использования и реэкспортирует
  для `main.ts`/`netserver.ts`/тестов. Заодно исправлен недочёт REFP-19: `buildProgress`/
  `thresholdRamp` остались неиспользуемым импортом после переноса `netIncome` в
  `economy.ts` — убраны. Гейт зелёный (175 файлов, 1899 тестов — прирост от параллельно
  смерженной LOC-2, не от этого кирпича).
- **REFP-21** ✅ `[proto]` **`protoKernel.ts`** — `MODULES` (порядок = контракт
  детерминизма, инвариант #6), `kernel`, `SCORE_LIMIT`, `ctx`, `advance`, `order`,
  `StepOut` вынесены в `prototype/src/protoKernel.ts` чистым переносом. `game.ts`
  импортирует `ctx` для `stewardGuardOrders` и реэкспортирует всё; 24 осиротевших
  импорта модулей ядра сняты с `game.ts` (их собирает protoKernel). `game.ts`
  1326→1223 строк. Гейт зелёный (1993 теста — число не изменилось).
- **REFP-22** ✅ `[proto]` **`actions.ts`** — оба кластера экшен-билдеров вынесены.
  Первый заход: самодостаточный кластер `moveFleet`…`spyOn` (28 функций) + приватный
  `act`/`seqCounter` + `canTraverse` (REFP-27). Второй, «разбросанный» кластер
  (patrol/chain-штампы, форс-марш/ремонты, market/division/capital/hero-заказы)
  доехал после того, как состояние, с которым он был переплетён, вынесли
  (REFP-13/23/25/26) — остались чистые лист-билдеры; с ними едут только типы
  `SortieState`/`ChainStep`/`MarketSide`. `game.ts` реэкспортирует всё (до REFP-28).
  Гейт зелёный (1993 теста — число не изменилось).
- **REFP-23** ✅ `[proto]` **`patrol.ts`** — `Patrol`, `patrolTarget`, `scrambleOrder`
  вынесены в `prototype/src/patrol.ts` (depends on `squadron.ts`'s `SortieState`/
  `canSortie`/`spendSortie`/`withinRange`, REFP-7, and `actions.ts`'s `engageFleet`/
  `moveFleet`, REFP-22). `game.ts` импортирует для `serverAutoAssaultActions`/
  `serverChainActions`/`serverPatrolActions` (ещё не вынесены, REFP-24) и
  реэкспортирует для `main.ts`/тестов; `squadron.test.ts` продолжает импортировать
  их из `./game`-фасада без правок. Гейт зелёный (182 файла, 1974 теста).
- **REFP-24** ✅ `[proto]` **`serverDrivers.ts`** — `serverAutoAssaultActions`/
  `serverChainActions`/`serverPatrolActions` (CC-2/CC-1/CC-4 server-side standing-order
  drivers) вынесены в `prototype/src/serverDrivers.ts`, вместе с приватными хелперами
  `abilityCooldownKey`/`abilityOnCooldown`/`heroCommandingFleet` (использовались только
  `serverChainActions`'s `ability`-шагом). Зависит от уже вынесенных `chain.ts` (REFP-8),
  `patrol.ts` (REFP-23), `squadron.ts` (REFP-7), `actions.ts` (REFP-22). Попутно
  `castHeroAbility` (единственный ещё-не-вынесенный action-builder, нужный
  `serverChainActions`'s `ability`-шагу) перенесён из `game.ts` в `actions.ts` — leaf-билдер
  без своей завязки на состояние, в отличие от остатка "hero engine"-кластера
  (`spawnHero`/`unlockHeroSkill`/`fitHero`), который остаётся отложенным (REFP-22's
  заметка про market/division/hero-заказы всё ещё в силе для НИХ). Локальный `DriverState`
  (узкое расширение `GameState` под `autoAssault`/`orders`/`patrols`) вместо импорта
  кухонного `DivState` из `game.ts` — тот же паттерн, что `capital.ts`/`standingOrders.ts`.
  `game.ts` импортирует и реэкспортирует. Гейт зелёный (182 файла, 1974 теста — число не
  изменилось, чистый перенос; `game.ts` 2098→1851 строка).
- **REFP-25** ✅ `[proto]` **`stewardGuard.ts`** — `stewardGuardOrders` + его
  эвакуационные хелперы (`liftable`, анти-челночный `EVAC_RETURN_COOLDOWN_H`) вынесены
  в `prototype/src/stewardGuard.ts` чистым переносом; узкая проекция `GuardState`
  (patrols-peek) — по паттерну division/serverDrivers. Заодно `orderAuto`/
  `orderScramble` вытащены вперёд в `actions.ts` (лист-билдеры; гвардия и `aiOrders`
  зовут их без обратного ребра — прецедент `castHeroAbility`). `game.ts` 1223→862
  строки. Тест: `stewardGuard.test.ts` (25.5k, не менялся). Гейт зелёный (1993).
- **REFP-26** ✅ `[proto]` **`ai.ts`** — `SeatAiKind`/`SeatAiDecision`/`seatAiDecision`
  (SES-2.2: делегирование ⊳ заместитель-после-грейса ⊳ никто) + `aiOrders` (полный
  бот-экспандер / драйвер делегированной позы) вынесены в `prototype/src/ai.ts`
  чистым переносом. Заодно `marketList` вытащен вперёд в `actions.ts` (лист-билдер;
  бот выставляет лоты без обратного ребра — тот же прецедент, что orderAuto/
  orderScramble в REFP-25). После выноса с `game.ts` сняты 12 осиротевших импортов.
  `game.ts` 862→554 строки. Гейт зелёный (1993).
- **REFP-27** ✅ `[proto]` **`canTraverse`** — вынесен вместе с REFP-22 в
  `prototype/src/actions.ts` (соседний, тоже чисто shared-core+`getStance`
  код — отдельный файл под 4 строки не оправдан). `game.ts` импортирует и
  реэкспортирует.
- **REFP-28** ✅ `[proto]` **Финальная очистка** — `game.ts` схлопнут в **чистый
  index-фасад** (207 строк одних `export … from`, ни логики, ни констант, ни
  состояния; поверхность экспорта сохранена 1:1 — 165 имён). Последняя логика
  разъехалась по домам: `HOUR`/`DAY` → новый `time.ts`, `heroRosterOf` → `heroes.ts`
  (домен героев). **Обратных рёбер на фасад — ноль**: `economy.ts` берёт `HOUR` из
  `time.ts`, `groundcombat.ts` — тип `FormationUnit` из `formations.ts`. Потребители
  (`main.ts`/`netserver.ts`/тесты) не менялись — уходят с барреля на прямые импорты
  оппортунистически, пофайлово. Гейт зелёный (1993 теста — число не изменилось).

> **Блок REFP закрыт целиком: 28/28.** `game.ts`: 5289 строк на пике → 207-строчный
> баррель. Итог: 24 файла-модуля, ни одного цикла импортов, публичный API не ломался
> ни на одном кирпиче.

> **Порядок исполнения** (ценность×простота): фаза 1 (REFP-1..8) → фаза 2 (9..18) →
> REFP-21 → фаза 3 (19,20,22..27) → REFP-28. Фаза 1 — низкий риск, можно параллельно с
> другими зонами.

---

## Блок REFM · Рефактор `prototype/src/main.ts` — страховка, потом вынос `[proto]`

> `main.ts` = ~15.4k строк UI/DOM/рендера — второй god-file, крупнее `game.ts` на пике
> втрое. Отличие от REFP: у `main.ts` исторически НЕ было ни одного автоматического
> сторожа (ESLint игнорирует `prototype/**`, tsc не запускался, 0 тестов его
> импортируют, 0 экспортов) — ломался он молча (TDZ `httpBase`/`nfmt`/`authMode`,
> `cost()`-разметка в проде, ключи вместо текста). Поэтому нулевой кирпич — сетка,
> и только потом выносы тем же ритуалом, что REFP: чистый перенос, public API через
> реэкспорт, тесты на месте, ✅ + `state.md` в том же PR.

- **REFM-0** ✅ `[proto]` **Страховка: typecheck прототипа в гейте.**
  `prototype/tsconfig.json` (полный `strict` + `noUncheckedIndexedAccess` от
  `tsconfig.base`, DOM-lib; paths: `@void/shared-core` → исходники, `pg` → типы из
  `packages/server`, куда netserver-бандл и складывается) накрывает `src/**` +
  `netserver.ts` + `/localization`; корневой `pnpm run typecheck` гоняет `tsc -p
  prototype` после workspace-пакетов → CI получает его бесплатно. 91 накопившаяся
  ошибка починена по-настоящему, без ослабления строгости: TDZ-класс (`authMode`/
  `pending*AfterAuth` — boot-блок читал их синхронно до объявления), расхождение
  словаря `ChainStep` (нашло живой баг — закрыт отдельным PR в `[core]`), битые
  l10n-фолбэки юнитов (`?.name ?? id` строил несуществующий ключ — заменён на
  `displayUnit()`), хелпер `sectorTypeOf()` вместо 17 небезопасных двойных индексаций,
  честные гварды в `fleetLaunch`/`spotlight*`/`pinch`-жестах и тестах. ESLint для
  прототипа — отдельным кирпичом (REFM-0.1), чтобы не смешивать шум правил с типами.
- **REFM-0.1** ✅ `[proto]` **ESLint для `prototype/`** — `prototype/**` снят из
  `ignores`: играбельный клиент линтуется наравне с пакетами, без послаблений и без
  единого нового `eslint-disable`. Зона получила ровно одну настройку — её Node-скрипты
  (`build`/`netserver`/`host`/`doctor`/`perf`/`selfplay`/`browsertest`…) дописаны в
  существующий блок глобалов рядом с `scripts/**` (+`performance`); TypeScript правил
  не потребовал: typescript-eslint сам гасит `no-undef` для `.ts` (это работа tsc).
  Из 71 замечания 55 были именно недостающими Node-глобалами, остальные 16 починены
  по существу: мёртвые импорты (`GameData`/`Fleet` в forcedMarch, `PlayerId` в
  botDiplomacy, `data` в stewardGuard.test, `ownedDefIds`/`SCORE_LIMIT`, а также мой
  собственный `botEmbargoes`, затесавшийся при REFP-26 — в оригинале он жил лишь в
  комментарии), два бесполезных инициализатора, `prefer-const`, `no-regex-spaces`,
  протухшая `eslint-disable`-директива в smoke.ts. **Две осиротевшие UI-функции
  удалены** (`unitTilesHtml` — ничей ряд плиток состава; `conSoonPane` — заглушка
  «переезжает в конструктор», конструктор давно приехал CON-1) вместе с осиротевшим
  ключом `yard.soon` в обеих локалях — иначе i18n-гейт справедливо ловит сироту.
  Намеренные zero-width пробелы в doc-шапках sandbox/testmode (чтобы вложенный `*/`
  не закрывал комментарий) переписаны словами — невидимый символ ушёл, disable не
  понадобился. Осталась мёртвой CSS-заготовка `.ptiles` в build.mjs — помечено, не
  трогал (CSS вне линта, отдельный кирпич).
- **REFM-1** ✅ `[proto]` **Инвентаризация `main.ts`** — карта файла снята (данные ниже
  посчитаны по коду, не на глаз). **15 420 строк, 64 секции-баннера, 407 функций верхнего
  уровня, 234 модульных `const`, 198 модульных `let`, 0 экспортов, ~80 строк побочных
  эффектов на модульном уровне (навешивание слушателей).**

  > **Метод распила у REFM ДРУГОЙ, чем у REFP — и это главный вывод инвентаризации.**
  > `game.ts` имел чистую экспортную поверхность, поэтому кирпич = «перенести текст +
  > реэкспортировать». У `main.ts` экспортов НЕТ вообще: это DOM-точка входа, где всё
  > висит на общем модульном состоянии (198 `let`). Значит перенос текста невозможен —
  > каждая секция должна уехать модулем, принимающим свои зависимости ЯВНО. Прецедент
  > в зоне уже есть и работает: `testmode.ts` (`initTestMode(hooks)`), `sandbox.ts`
  > (`initSandbox(hooks)` + чистые `sb*`-функции над `GameState`), `spotlightDom.ts`
  > (`startTour(steps, onEnd)`), `updater.ts`. Кирпич REFM = «вынести секцию в модуль с
  > явным `init*(hooks)` / чистыми функциями», а не «вырезать и реэкспортировать».

  **Порядок кирпичей — по СЦЕПКЕ, а не по размеру.** Мера сцепки: сколько ЧУЖИХ
  модульных `let` читает секция (её собственные не мешают переезду). Ниже — все
  секции ≥100 строк, отсортированы по сцепке (легче → тяжелее); «свои/чужие» = число
  модульных `let`.

  _Диапазоны строк — снимок на момент REFM-1; каждый вынесенный кирпич их сдвигает,
  так что ищите секцию по баннеру-комментарию, а не по номеру. Мера сцепки (и, значит,
  порядок) от сдвигов не зависит — она и есть смысл таблицы._

  | секция | строки | размер | свои | чужие |
  |---|---|---|---|---|
  | build/unit codex (плитка → попап + «Строить») ✅ REFM-4 | 6331–6445 | 115 | 0 | 1 |
  | in-app APK auto-update | 14725–14831 | 107 | 0 | 2 |
  | multiplayer (net mode) | 717–837 | 121 | 42 | 2 |
  | «Арсенал» (вкладка хаба, ARS-5) ✅ REFM-5 | 10550–10675 | 126 | 2 | 2 |
  | session market (двусторонний стакан) ✅ REFM-6 | 9791–9926 | 136 | 2 | 2 |
  | steward («Хранитель») ✅ REFM-7 | 9257–9415 | 159 | 4 | 2 |
  | object dossiers (описания при наведении) ✅ REFM-4 | 5978–6330 | 353 | 0 | 2 |
  | viewport, galaxy backdrop & map projection | 900–1005 | 106 | 10 | 3 |
  | division template designer (H4) ✅ REFM-8 | 8775–8916 | 142 | 1 | 3 |
  | side-colour SCHEMES | 332–569 | 238 | 4 | 3 |
  | TT-3.1: экран-дерево технологий ✅ REFM-9 | 8917–9256 | 340 | 2 | 3 |
  | corp: live state (corpFetch) ✅ REFM-11 | 14889–15421 | 533 | 12 | 3 |
  | password reset (`?reset=<token>`) | 11138–11262 | 125 | 1 | 5 |
  | heroes («штаб героев») ✅ REFM-14 | 9416–9790 | 375 | 3 | 5 |
  | scientist council picker | 11697–11808 | 112 | 0 | 7 |
  | Профиль командира (main-menu §4.2) ✅ REFM-10 | 10676–11005 | 330 | 3 | 7 |
  | end screen (итоги матча) | 8625–8769 | 145 | 1 | 8 |
  | graphics preferences | 1006–1197 | 192 | 6 | 8 |
  | conversations (вкладка сообщений) | 6858–7157 | 300 | 0 | 8 |
  | constructor («Верфь») ✅ REFM-13 | 9927–10352 | 426 | 6 | 9 |
  | helpers | 1425–2042 | 618 | 3 | 9 |
  | settings overlay | 11263–11498 | 236 | 4 | 10 |
  | red AI | 3251–3527 | 277 | 0 | 10 |
  | rendering | 3528–3933 | 406 | 2 | 10 |
  | province ping composer | 14061–14182 | 122 | 0 | 11 |
  | side panel | 5132–5977 | 846 | 0 | 11 |
  | single-player setup overlay | 11499–11696 | 198 | 1 | 12 |
  | espionage (SPY-1) | 2122–2372 | 251 | 1 | 12 |
  | session diplomacy & comms menu | 6540–6857 | 318 | 1 | 12 |
  | threat alert (THREAT-HUD) | 1198–1424 | 227 | 5 | 13 |
  | accounts (SES-2.5) | 12326–13007 | 682 | 6 | 13 |
  | camera control (pan/pinch/wheel/tap) | 8371–8582 | 212 | 7 | 14 |
  | diplomacy gate (клиентский слой приказов) | 2634–3250 | 617 | 0 | 17 |
  | holographic static layer (территории+лейны) | 3934–5131 | 1198 | 2 | 18 |
  | canvas input | 8140–8370 | 231 | 0 | 19 |
  | TGT-1: target-order composer | 14183–14724 | 542 | 1 | 20 |
  | ONB-5 return digest | 7265–8139 | 875 | 7 | 29 |
  | meta-progression (прокачка) | 11809–12275 | 467 | 1 | 52 |
  | Android Back / Escape (закрытие слоёв) | 13107–14060 | 954 | 2 | 56 |

  _Секции <100 строк (25 шт., ~1.3k строк суммарно) в таблицу не вошли — они уезжают
  попутно с соседями по домену._

  **Читается так.** Верх таблицы — почти чистые «экранные» модули: рисуют HTML по
  состоянию и зовут пару колбэков, переезжают дёшево. Низ — то, что по определению
  сшивает весь файл: `Android Back` знает про КАЖДЫЙ закрываемый слой (56 чужих `let` —
  это его работа, а не грязь), прокачка и ONB-5-дайджест дёргают полкарты состояния,
  `canvas input`/`camera` живут на камере и выделении. Их трогать последними — или,
  для Back-слоя, не выносить вовсе, а свести к реестру «слой → как закрыть», который
  сами модули пополняют при регистрации.

  **Предлагаемый порядок:** codex → арсенал → рынок → досье объектов → дерево
  технологий → steward → division designer → героя-штаб → профиль → корп-состояние →
  верфь → экраны хаба (настройки/сетап/учёные) → helpers → рендер-слои → и лишь
  затем сцепленное ядро (панель/ввод/камера/Back/прокачка).
- **REFM-2** ✅ `[proto]` **`format.ts`** — чистые презентационные форматтеры
  (`esc`, `kfmt`/`nfmt`/`round1`, `hl`, `TECH_CUR`/`curIc`, `cost`/`costText`,
  `displayUnit`/`buildingName`, `fmtEta`) вынесены из `main.ts` ПЕРВЫМИ намеренно: у них
  нет ни состояния, ни DOM, поэтому каждый следующий экранный кирпич импортирует их
  напрямую, а не тащит через `deps`-объект. Единственная правка сигнатуры —
  `buildingName(name, id)` вместо чтения `data` внутри (модуль остаётся без зависимости
  от каталога); 12 call site'ов обновлены. **Первый тест над бывшим кодом `main.ts`**:
  `format.test.ts`, 15 проверок — экранирование (включая порядок `&` перед `<`),
  сокращения чисел, HTML-vs-текст у `cost`/`costText` (тот самый разъезд, что уехал в
  прод), fail-soft на незнакомом ресурсе, имена и ETA; локаль в тесте пинится RU (под
  Node язык не определяется и «Крейсер» читался бы как «cruiser»). `main.ts`
  15 420→15 382 строки. Гейт: 2008 тестов (+15), 184 файла.
- **REFM-3** ✅ `[proto]` **`icons.ts`** — словарь иконок (`BUILD_ICON`/`UNIT_ICON`/
  `KIND_ICON` + `unitIcon`/`unitIconHtml`/`archPath2d`) вынесен следом за форматтерами и
  по той же причине: иконки нужны каждому экрану. Лист-модуль не тянется к состоянию
  хоста — `unitIconHtml` берёт каталог и цвет стороны аргументами (в `main.ts` цвет
  подставлялся из модульного `youColor`), `unitIcon` — каталог; call site'ы обновлены.
  `main.ts` 15 382→15 337 строк. Гейт зелёный (2008 тестов).
- **REFM-4** ✅ `[proto]` **`dossiers.ts`** — досье объектов (наведение/тап по `data-desc`)
  и кодекс (полная карточка) уехали одним кирпичом: кодекс переиспользует тело досье как
  описание, врозь их не разнести. **Первый кирпич по НАСТОЯЩЕМУ методу REFM** — форма
  выноса не «текст + реэкспорт», а явные зависимости: чистые (`buildingDossier`,
  `unitDossier(id, pcUi)`, `producesLine`, `taskDossier(state, …)`, `cxRow`) экспортируются
  как есть, а четыре рендера, которым нужно живое состояние, выдаёт
  `createDossiers(host)` — `host` = `{ state, pcUi, youColor, queueOf, activeConstruction,
  progressPct }`, шесть хуков вместо чтения модульных `let`. Call site'ы в `main.ts` не
  изменились (деструктуризация возвращает те же имена). Попутно: словарь очереди стройки
  (`BuildLane`/`BuildKind`/`QueuedBuild`/`PlanetBuildQueue`/`ConstructionPayload`/
  `ActiveBuild`) вынесен в `buildQueue.ts` — он понадобился обоим файлам, а две
  структурно одинаковых декларации разъехались бы; снят и осиротевший после REFM-2
  jsdoc от `esc()`. **25 тестов** (`dossiers.test.ts`): порог 50% (новая стройка до
  половины не даёт ничего, апгрейд считает от ТЕКУЩЕГО уровня, а не от нуля), разбор
  ключа `c:<планета>:<полоса>:<состояние>:<ref>` (чужой `seq` и выход за край очереди →
  `null`), маршрутизация `objDossier`, fail-soft кодекса на незнакомом id, цвет стороны
  приходит из хоста. Проверено в браузере: тултип здания, карточка здания и силуэт
  юнита в цвете стороны. `main.ts` 15 337→14 862 строки. Гейт: 2033 теста (+25), 185 файлов.
- **REFM-5** ✅ `[proto]` **`arsenalScreen.ts`** — вкладка «Арсенал» (ARS-5) уехала из
  `main.ts`; `arsenal.ts` рядом остался ЧИСТОЙ моделью (фильтр/грейды/парсинг), новый
  модуль — экран: разметка (`arsenalPanelHtml`/`arsenalCardHtml`, чистые) + собственный
  живой кэш (`initArsenal(hooks)`). Пять хостовых зависимостей свёрнуты в пять хуков, и
  две из них по дороге стали ЧЕСТНЕЕ, чем были: вместо пяти сетевых кусков
  (`resolveServer`/`probeAuthMode`/`authMode`/`sessionToken`/`httpBase`) хост отдаёт один
  `authorizedBase()` — в нём же и живёт правило «за паролем ради ПОСМОТРЕТЬ коллекцию не
  ходим» (нет сервера / нет аккаунтов / нет отложенной сессии читаются одинаково); вместо
  прямого `localStorage` — `readCache`/`writeCache`, так что ключ по позывному остался
  рядом с остальными ключами `main.ts`, а модуль о хранилище не знает вовсе. Верфь
  (`conOriginTag`, LARS-4) читает коллекцию через `arsenal.items()` и подписывает
  источник через `originLabel()` — таблица ключей наружу больше не торчит. Попутно
  идиома `unitDossier(id)?.name ?? displayUnit(id)` (5 повторов) свёрнута в
  `unitTitle(id)` в `dossiers.ts`, а осиротевший после этого `unitDossier` из
  `createDossiers()` убран. **16 тестов** (`arsenalScreen.test.ts`): cache-first (кэш
  красится ДО сети, без авторизации в сеть вообще не идём), битый кэш → пустая витрина,
  отказ сервера и обрыв сети НЕ затирают кэш, экранирование `defId` (коллекция приходит
  с сервера), бейджи заточки/износа, чипы фильтров и снятие фильтра повторным кликом,
  делегирование кликов. Проверено в браузере: витрина с четырьмя карточками, фильтр
  «Modules», переход в кодекс по карточке и тег «drop» на карточке Верфи. `main.ts`
  14 862→14 762 строки. Гейт: 2049 тестов (+16), 186 файлов.
- **REFM-6** ✅ `[proto]` **`marketScreen.ts`** — окно внутрисессионного рынка (ECON-4)
  уехало из `main.ts`; `sessionMarket.ts` рядом остался МОДЕЛЬЮ (модуль ядра, эскроу,
  комиссия), `actions.ts` — строителем приказов, а новый модуль это окно: чистая
  разметка (`marketBoxHtml`, `netNoteText`) плюс два кусочка вью-состояния, которые
  книга и правда держит сама (открытый товар, сторона формы). Пять хуков хоста
  (`root`/`state`/`me`/`order`/`onOpen`), и по дороге снята одна настоящая шероховатость:
  окно искало свои поля через ГЛОБАЛЬНЫЙ `document.getElementById` — теперь все
  запросы скоупятся на само окно (`root().querySelector`), так что модуль перестал
  зависеть от глобального документа и заодно стал тестируемым без DOM. Ручка `#market`
  осталась в `main.ts` — её держит реестр слоёв Android-Back, и это его работа, а не
  грязь. **17 тестов** (`marketScreen.test.ts`): математика комиссии в строке «к
  получению» (продавцу вниз, эскроу вверх), сортировка книги (аски от дешёвых, биды от
  дорогих), свой лот даёт отмену / чужой — исполнение, гашение кнопки при пустой казне,
  net исполнителя в биде, экранирование ника владельца, открытие/закрытие окна,
  переключение вкладки и стороны, приказы уходят от текущего игрока. Проверено в
  браузере: живой пересчёт строки при вводе, выставление лота, отмена, переключение
  вкладок с сохранением лота и закрытие по Escape (реестр Back цел). `main.ts`
  14 762→14 639 строк. Гейт: 2066 тестов (+17), 187 файлов.
- **REFM-7** ✅ `[proto]` **`stewardScreen.ts`** — окно «Хранителя» (ST-2/ST-3) уехало из
  `main.ts`. Движок остался на месте (`stewardModule` в ядре ведёт вахту, `stewardGuard.ts`
  решает ходы, `actions.ts` строит приказы); модуль — окно: три состояния (заперто
  технологией / готово к делегированию / на вахте), журнал SITREP и ЕДИНСТВЕННЫЙ кусочек
  вью-состояния, который окно правда держит само — поза следующей вахты. Хуков семь
  (`root`/`body`/`state`/`me`/`order`/`onOpen`/`openTech`), приказы модуль строит сам
  (`delegateSteward`/`recallSteward` импортируются напрямую). **Важнее формы — то, что
  читающие хелперы стали общими:** `stewFmtDur` (его же считает тревога THREAT-HUD),
  `stewardTechDone` (по нему боковая панель гейтит hold point), `stewMetrics` (утренний
  отчёт снимает и диффит) больше не сидят внутри экрана, а импортируются как чистые
  функции от `(state, me)`. `#steward`, `lastStewAt` и `stewSnapshot` остались в
  `main.ts` — их держат кадровый цикл и обработчик событий вахты. **18 тестов**
  (`stewardScreen.test.ts`): метрики считают только мои миры и округляют казну, формат
  длительности (включая просроченный дедлайн → «0м»), все семь видов записей журнала
  дают локализованную строку (ключ наружу не течёт), НЕИЗВЕСТНЫЙ вид не исчезает, а
  показывается сырым (вахта, которая что-то сделала, не должна читаться как спящая),
  журнал режется до 12 свежими сверху и доживает до утреннего отчёта, три состояния
  окна, делегирование шлёт дедлайн в игровом времени с выбранной позой, битая
  длительность падает на 8ч, а не на NaN. Проверено в браузере: запертая карточка,
  кнопка «в дерево технологий» закрывает окно и открывает дерево. `main.ts`
  14 639→14 513 строк. Гейт: 2084 теста (+18), 188 файлов.
- **REFM-8** 🗑 `[proto]` **`divisionDesigner.ts`** — конструктор шаблонов дивизий (H4)
  🗑 **СНЯТО (H4-REVERT, 2026-07-31).** Игра вернулась к системе отдельных наземных
  юнитов: они строятся как обычные юниты, стоят в гарнизоне и ездят в трюме флота
  (`armyModule`), десант разрешает `combatModule`. Текст кирпича ниже оставлен как
  запись о проделанной работе — но описанной механики в игре БОЛЬШЕ НЕТ.
  уехал из `main.ts`: чистая разметка (`divDesignHtml`, `designList`, `nextSlotUnit`) +
  `initDivDesign(host)` с семью хуками; `open(idx)` принимает шаблон, который показывал
  сборщик, чтобы «⚙ Конструктор» открывал ТОТ дизайн, а не сбрасывался на первый.
  Попутно разъехались по своим домам две вещи, которые нужны не только этому экрану:
  `formIcon` + обе таблицы глифов → `icons.ts` (лист-модуль берёт раскладку аргументом,
  как `unitIconHtml` — за `pcUi()` хоста он не тянется), `FORM_RU` → `formations.ts`
  (это словарь имён ростера, а не иконки); 5 call site'ов `formIcon` обновлены. Сняты
  два осиротевших после REFM-2 комментария (от уехавших `TECH_CUR`/`curIc`).
  **16 тестов** (`divisionDesigner.test.ts`) — и один из них поймал ДЕФЕКТ, внесённый
  самим выносом: кламп индекса применялся только к выбранному дизайну, а подсветка
  вкладки шла по сырому индексу, так что при индексе за краем рисовался последний
  дизайн, но не подсвечивалась ни одна вкладка. Починено клампом один раз наверху.
  Остальное: цикл слота проходит весь ростер и возвращается в пустой (незнакомый юнит
  не застревает), премейд заперт (нет переименования, все слоты `disabled`, приказ не
  уходит даже в обход разметки), имя шаблона экранируется (его вводит игрок), раскладка
  меняет глифы, но не структуру. Проверено в браузере: открытие на выбранном шаблоне,
  цикл слота, замок премейда, переименование, закрытие по Escape. `main.ts`
  14 513→14 401 строк. Гейт: 2100 тестов (+16), 189 файлов.
- **REFM-9** ✅ `[proto]` **`techTree.ts`** — экран дерева технологий (TT-3.1) вынесен:
  чистая разметка (`techTreeHtml`) + `initTechTree(host)` с шестью хуками. Вью-состояние,
  которое окно правда держит само — открытая вкладка-ветка и id раскрытого досье — уехало
  внутрь модуля из двух модульных `let`. Сохранение прокрутки при живой перерисовке (окно
  перекрашивается раз в 500 мс кадровым циклом, и `innerHTML` иначе сбрасывал бы скролл)
  тоже переехало внутрь `repaint()` — раньше это был хвост `renderTech`. `branchLabel`
  экспортируется отдельно: его берёт ещё и совет учёных. Ручка `#tech` осталась в
  `main.ts` — её держат реестр слоёв Android-Back и троттлинг кадрового цикла; «Хранитель»
  теперь открывает окно через `techTree.open()` вместо ручного `classList.add` +
  `renderTech()`. **26 тестов** (`techTree.test.ts`): подписи веток (ключ наружу не течёт),
  **карта раскладки `TECH_COLS` сверяется с живыми данными** — опечатка в id тихо
  выкидывала бы узел в автоколонку, теперь это падает тестом; клиентская проверка условий
  **fail-secure** (неизвестный тип читается как ЗАКРЫТО — правду говорит ядро); рельса дней
  одинаковой высоты на всех вкладках (строки не прыгают при переключении); узлы только
  своей ветки; мета-узлы аккаунта в дерево не попадают; идущее исследование даёт полосу
  ровно на половине и ETA; кнопка гаснет без ресурсов; исчезнувший из данных узел не
  оставляет висящее досье; смена вкладки закрывает досье, а повторный тык по той же — нет;
  прокрутка переживает `repaint()`. Проверено в браузере: пять вкладок, 7 строк рельсы,
  досье с ценой и эффектом, закрытие досье, сохранение прокрутки через живой ререндер,
  Escape, и путь «Хранитель → в дерево». `main.ts` 14 401→14 061 строк. Гейт: 2126 тестов
  (+26), 190 файлов.
- **REFM-10** ✅ `[proto]` **`profileScreen.ts`** — «Профиль командира» (карьерное досье,
  main-menu.md §4.2) вынесен: чистая разметка (`profileHtml` берёт готовый `ProfileView`,
  поэтому ей не нужны ни хранилище, ни часы) + `initProfile(host)` с пятью хуками. Хост
  отдаёт `view()` (позывной, XP, счётчики, корпорация, суверены), `readCache`/`writeCache`
  и `authorizedBase()` — та же политика, что у «Арсенала»: за паролем ради ПОСМОТРЕТЬ
  витрину не ходим. Разбор кэша медалей стал двумя чистыми функциями
  (`parseMedalCache`/`parseMedalCatalog`), и вторая переиспользуется на серверном ответе —
  раньше это была копипаста двух одинаковых `flatMap`. `SOV_SVG` уехал в `icons.ts`: его
  делят профиль и верхний бар.

  **Баннер секции врал.** В карте REFM-1 «Профиль командира» числился как 330 строк, но
  профиля там ровно половина — под тем же баннером жили `syncCommanderFromServer`,
  `openHub` и обработчик регистрации `#cnew`. Вынесено только досье (163 строки), остаток
  получил свой честный баннер «вход в хаб и зеркало аккаунтного XP».

  **19 тестов** (`profileScreen.test.ts`), и главный из них про правило экрана: **«—»
  вместо правдоподобного нуля**. Без сыгранных матчей винрейт, среднее место и серия —
  прочерки, а `Матчей: 0` — честный ноль; без корпорации влияние — прочерк, потому что
  голый `0` читался бы как «ты ничего не заработал». Плюс: экранирование позывного и
  имени корпорации (их вводят люди), экранирование имён медалей (они приходят с сервера
  ТЕКСТОМ, без i18n-ключей), витрина рисует каталог целиком с пометкой неполученных,
  битый кэш вырождается в пустую витрину, отказ сервера и обрыв сети не затирают кэш,
  закрытие крестиком и по фону — но не по тапу внутри листа. Проверено в браузере: обе
  двери (полоска личности в хабе и «досье» на карточке игрока в матче), все шесть плиток,
  оба способа закрытия. `main.ts` 14 061→13 937 строк. Гейт: 2145 тестов (+19), 191 файл.
- **REFM-11** ✅ `[proto]` **`corpScreen.ts`** — корпоративный кабинет (AVA-C1/C2) вынесен
  целиком: 587 строк, **12 модульных `let`** и весь HTTP-слой (`corpFetch`/`refreshCorp`/
  `corpIntent` + шесть вкладок) уехали в свой файл. Самый большой кирпич блока — и при
  этом почти чистый вынос: наружу торчала ровно ОДНА вещь, `corpMine`, которую читает
  карточка профиля; она стала `corp.mine()`. Пять сетевых кусков свёрнуты в один
  `authorizedBase()` (как у «Арсенала» и профиля), `note`/`errText`/`maybeIntro` — три
  явных хука, четыре DOM-слота (`#corp`/`#corphd`/`#corptabs`/`#corpbody`) тоже. Обе
  двери (кнопка хаба `#ccorp` и рельса матча `#railcorp`) остались в `main.ts` — это его
  навигация. Снят осиротевший после REFM-2 комментарий про «Declaration, not const» (он
  описывал уехавший в `format.ts` `nfmt`), и вместе с ним из `main.ts` ушёл весь импорт
  из `./corp` — 22 имени. **11 тестов** (`corpScreen.test.ts`): у всех четырёх ролей есть
  РАЗЛИЧИМЫЕ подписи и свой цвет точки, у каждой вкладки и **каждого вида записи аудита**
  есть перевод (сырой ключ игроку не покажут), порядок вкладок соответствует §7
  corporation-ui.md (живые первыми, заглушки в конце), `mine()` без корпорации пуст —
  именно по нему профиль рисует прочерк, без авторизации кабинет **в сеть не ходит
  вовсе**, серверный `E_*` доезжает тостом через `errText`, а не сырым кодом, обрыв сети
  не роняет кабинет и не сыплет тостами. Проверено в браузере: обе двери, все шесть
  вкладок (включая честные «скоро» на Владениях), закрытие, и сквозная связь
  `corp.mine()` → прочерк влияния в профиле. `main.ts` 13 937→13 354 строки.
  Гейт: 2156 тестов (+11), 192 файла.
- **REFM-12** ✅ `[proto]` **`chatWindow.ts`** — плавающее окно чата (десктоп) вынесено
  целиком: 371 строка реализации плюс 27 строк состояния (`chatOpen`/`chatMin`/
  `chatPinned`/`chatGeom`/`chatCfg`/`chatDrag`…) уехали в свой файл. Наружу торчали ровно
  пять вещей, и публичный интерфейс сведён к ним: `toggle` (рельса), `isOpen`+`close`
  (реестр слоёв Back/Escape), `refreshIfVisible` (четыре точки прихода сообщения — они
  же прятали условие `open && !min`) и `onViewportResize`.
  **Сборка стоит НЕ там, где остальной UI, а на месте старого состояния** — `resize()`
  вызывается синхронно при загрузке и спрашивает у окна `onViewportResize`, так что
  привязка внизу файла попала бы в temporal dead zone и уронила бы загрузку целиком
  (esbuild лоуерит `const`→`var`, так что это молчаливый `undefined`, а не понятная
  ошибка). Поймано typecheck'ом на этапе разводки, до сборки.
  **Три бага, найденные и починенные по дороге:** (1) `censorText` матчил ПОДСТРОКУ,
  хотя комментарий над ним обещал «whole-word» — «hello» превращалось в «****o»,
  «scrapbook» в «s****book», «дураками» в «*****ами»; границы заданы через
  `(?<![\p{L}\p{N}])` с флагом `u`, потому что `\b` в JS считает словом только ASCII и
  вокруг кириллицы срабатывает где попало; словарь заодно скомпилирован один раз, а не
  на каждое сообщение в каждой перерисовке, и метасимволы экранируются. (2) `loadChat`
  делал `Object.assign(chatGeom, v.geom)` БЕЗ ВАЛИДАЦИИ, а localStorage — внешние данные
  (A05/A08): запись `{"geom":{"w":"abc"}}` давала `Math.min('abc', maxW)` → NaN →
  `style.width='NaNpx'`, и окно схлопывалось при открытии; теперь `parseChatCache`
  проверяет каждое поле по типу и зажимает шрифт/прозрачность. (3) Жест перетаскивания
  снимался только по `pointerup`, а `pointercancel` (системный жест, потеря фокуса,
  прерванное касание) не приходит парой — окно оставалось приклеенным к курсору
  навсегда. (4) Строки-пинги в окне были МЕРТВЫ: `convoLineHtml` рисует их одинаково в
  обеих поверхностях и глобальный CSS делает курсор `pointer`, но обработчик
  `jumpToPing` висел только на `#diplo`, а `#chatwin` — независимый узел, события до
  него не всплывают; коалиционная вкладка — как раз тот канал, куда пинги и падают.
  **53 теста** (`chatWindow.test.ts`): арифметика жеста (якорение противоположных краёв
  и упор в минимум — до выноса не проверялось ничем, кроме мышки), зажим геометрии
  (включая случай, когда половина экрана мельче минимума), разбор порченого кэша,
  цензура с границами слова по обеим алфавитам, обнаружение вкладок личек,
  экранирование ника (CWE-79), делегированные клики. Проверено в браузере на СВЕЖЕЙ
  сборке: парковка, отправка, цензура, перетаскивание (+60/−50 точно), `pointercancel`
  останавливает жест, порченый кэш не даёт ни одного `NaN`.
  `main.ts` 13 354→12 985 строк. Гейт: 2264 теста (+53), 199 файлов.
  _Побочно найдено, не чинилось:_ `prototype/browsertest.mjs` пересобирает бандл ТОЛЬКО
  если `dist` отсутствует, поэтому при устаревшем `dist` `smoke:browser` молча проверяет
  старый код и зеленеет — на этом я и попался, первый прогон проверял вчерашнюю сборку.
- **UI-RES** ✅ `[proto]` Единая семья иконок ресурсов + «сколько не хватает».
  Иконки жили двумя поколениями: SVG из макета — только в верхнем баре (локальный
  `RES_SVG` в `main.ts`), все остальные поверхности — текстовые глифы `⛁❒⚘ϟ▣`, а в
  рынке `⛁` вообще был захардкожен в трёх местах. Теперь словарь ОДИН: `RES_SVG`
  переехал в `icons.ts` (рядом с `SOV_SVG`), `curIc()`/`cost()` в `format.ts` рисуют
  те же линии с той же подкраской `--rc-*` — бар, ценники карточек, стакан рынка,
  дерево технологий и строки выработки читаются одним взглядом.
  **Нехватка:** `cost(bag, have?)` — второй аргумент (казна) красит недостающий ресурс
  красным и пишет точный дефицит «−N» (подсказка — `cost.short`). Казна передана во
  все точки решения о стройке: конвейер «ждёт ресурсов», кнопки «построить здесь»,
  модалка технологии (`techCost` делегирует в `cost()`), дерево навыков и обвесы
  героя, карточки кодекса (у `DossierHost` появился хук `me()`).
  Попутно вычищено: `cost()`/`costText()` отдавали игроку сырое `free` — теперь ключ
  `cost.free`; `techCost` больше не собирает цену сам; дубль `myRes()` у «Верфи» снят.
  Тесты: `format.test.ts` (чипы, дефицит, «бесплатно» ключом), `techTree.test.ts`
  (делегация + нехватка в модалке). Проверено в браузере на свежей сборке: карточка
  танка с иконками бара, модалка «Saturation Barrage» с −251/−74/−40 по трём
  ресурсам. Гейт: 2268 тестов (+3), 199 файлов.
- **REFM-13** ✅ `[proto]` **«Верфь»** (`constructor`, 426 строк, 9 чужих `let`) →
  `shipyard.ts`. Форма прежняя: разметка чистая (`yardBoxHtml`, `loadoutPaneHtml`,
  `statBarHtml`, `bagText`, `originTagHtml`, `ownedHullsOf`), DOM трогает только
  `initShipyard(host)`. Главное решение — **черновик заказа стал ЗНАЧЕНИЕМ**: четыре
  модульных `let` (`conHull`/`conModules`/`conCount`/`conPlanet`) свернулись в `YardDraft`
  с чистой `normalizeDraft(state, me, draft, hullList)`. До выноса нормализация («корпус
  не из этой вкладки → первый свой, обвес сбросить»; «мир потерян → первый свой
  строительный») сидела ПОБОЧНЫМ ЭФФЕКТОМ внутри функции разметки и не проверялась ничем,
  кроме мышки; теперь это отдельная чистая функция с тестами, а разметка не пишет в
  состояние. Панель «Герои» осталась за `main.ts` (её кирпич следующий): модуль просит у
  хозяина `heroPaneHtml()` и отдаёт клики обратно `heroClick(tg) → 'repaint'|'close'|null`
  — перенос поведения нулевой, а штабу останется переехать в свой файл, не трогая Верфь.
  **38 тестов** (`shipyard.test.ts`): нормализация черновика (включая «не правит вход»),
  фильтр по снимку арсенала (ARS-5: нет снимка → без ограничений, пустые корпуса →
  честная заглушка), метка «откуда» (LARS-4), цена и полоса характеристики (дельта не
  выталкивает трек за 100%, подпись экранируется — CWE-79), гашение заказа без казны и
  без своих миров, окно целиком (вкладки, смена корпуса сбрасывает обвес, отказ ядра
  доезжает игроку текстом кода, границы счётчика, `unit.build` с корпусом/числом/обвесом/
  миром). `main.ts` 12 934→12 660 строк. Гейт: 2243 теста (+38), 194 файла. Проверено в
  браузере на СВЕЖЕЙ сборке (соло-матч): цена 60→105 металла при постановке `cargo_bay`,
  снятие модуля, счётчик, смена корпуса чистит обвес, вкладка эскадрилий, штаб героев на
  месте, интро Верфи и героев по разу, заказ дошёл в журнал («⚒ ordered: 3× cruiser»),
  консоль чистая.
- **REFM-14** ✅ `[proto]` **«Штаб героев»** (`heroes`, 374 строки, 5 чужих `let`) →
  `heroStaff.ts`. Три модульных `let` (`heroSel`/`heroTab`/`heroDossier`) свернулись в
  значение `HeroView` с чистой `normalizeHeroView`. Болезнь та же, что вскрыл REFM-13:
  нормализация сидела ПОБОЧНЫМ ЭФФЕКТОМ разметки — `heroBodyHtml` писал в `heroSel`
  посреди рендера (`if (!hero) { hero = mine[0]; heroSel = hero.id }`). Теперь рендер не
  мутирует ничего (закреплено тестом «отрисовка не трогает состояние матча»), а выбор
  героя — отдельная функция.
  Своего окна у панели нет — она живёт во вкладке «Герои» Верфи, поэтому модуль отдаёт
  ровно то, что Верфь запросила прошлым кирпичом: `paneHtml()` и `click(target)`. Блок
  роутинга кликов в проводке Верфи схлопнулся до двух строк делегирования.
  **Дальний каст и развёртывание** разрешиться в панели не могут (их цель — тап по
  КАРТЕ): они взводят хост через `armCast`/`armSpawn` и отвечают `'close'`, а
  `heroAim`/`heroSpawnAim` остаются в `main.ts`, где их читает обработчик тапа по миру.
  Попутно: `fmtHrs` переехал в `format.ts` (второй потребитель — командное меню карты),
  `HERO_CASTABLE` и `heroCdKey` экспортированы — их читает то же меню.
  **23 теста** (`heroStaff.test.ts`): свой ростер и его устойчивый порядок, нормализация
  вида (чужой/пропавший герой, пустой ростер, «не правит вход»), словарь кастуемости и
  ключ кулдауна (зеркало ядра), разметка панели, роутинг кликов — дальний `corridor`
  взводит карту и НЕ шлёт приказ, ближний `recall` уходит приказом сразу, покупка узла и
  установка фиттинга гасят досье. `main.ts` 13 060→12 628 строк. Гейт: 2310 тестов (+23),
  198 файлов. Проверено в браузере на СВЕЖЕЙ сборке (соло-матч): ростер из 4 героев, все
  четыре вкладки, смена фокуса, досье узла и фиттинга (с предупреждением о
  необратимости), кнопка развёртывания, консоль чистая.
  _Побочно найдено, не чинилось (зона локализации):_ шапка героя печатает `hero.name`
  БЕЗ `t()`, поэтому у неглавных героев в ней видно сырой ключ (`hero.arch.destroyer`).
  Строка перенесена дословно — баг предсуществующий, живёт и в `main.ts` до выноса.
- **REFM-15+** ⏳ `[proto]` — выносы по карте REFM-1, один кирпич = одна секция = один
  PR, в порядке роста сцепки. Форма выноса — `init*(hooks)` / чистые функции (см.
  вывод о методе выше), а НЕ «перенос + реэкспорт»: экспортной поверхности у `main.ts`
  нет. Приёмка кирпича: гейт зелёный, число тестов не падает, сборка + `smoke:browser`
  чистые, ✅ + `state.md` в том же PR.

> **Порядок:** REFM-0 ✅ → REFM-0.1 ✅ → REFM-1 ✅ → REFM-2 ✅ → REFM-3 ✅ → REFM-4 ✅ →
> REFM-5 ✅ → REFM-6 ✅ → REFM-7 ✅ → REFM-8 ✅ → REFM-9 ✅ → REFM-10 ✅ → REFM-11 ✅ →
> REFM-12 ✅ → REFM-13 ✅ → REFM-14 ✅ → REFM-15+ (по таблице сцепки; следующий —
> конверсации, затем экраны хаба).
> Блок REFP закрыт, так что зона `prototype/` теперь целиком за REFM.

---

> Документ живой: добавляйте и дробите кирпичики обычным PR'ом.
