/**
 * Инструменты основной игры, которых в забеге Sector Zero НЕТ.
 *
 * Правило владельца (§0.1 `docs/sector-zero-roadmap.md`, 2026-09-17): «Sector Zero не тянет
 * из основной игры то, что ей по сути не нужно». Забег одиночный, живых игроков в нём нет,
 * поэтому всё, что говорит с другими людьми или держит мир без тебя, — мёртвые кнопки.
 * Список утверждён владельцем 2026-09-23 (PVR-6.1): чат, почта, маркеры коалиции, корпорация,
 * рынок и «Сон» (Хранитель). Дипломатию владелец оставил.
 *
 * Значение — id кнопки в рельсе «☰ Ещё» (`prototype/build.mjs`). Сторож в тесте сверяет
 * их с разметкой: переименованная кнопка иначе молча вернулась бы в забег.
 */
export const SECTOR_ZERO_ABSENT_TOOLS = {
  chat: 'rail-chat',
  mail: 'rail-msgs',
  pings: 'rail-pings',
  corp: 'railcorp',
  market: 'rail-market',
  steward: 'rail-steward',
} as const;

export type SessionTool = keyof typeof SECTOR_ZERO_ABSENT_TOOLS;

/** Есть ли инструмент у игрока прямо сейчас: в забеге Sector Zero — нет, в остальной игре — да. */
export function toolShown(_tool: SessionTool, sectorZeroRun: boolean): boolean {
  return !sectorZeroRun;
}

/**
 * Поля шапки основной игры, которых в забеге Sector Zero тоже НЕТ (решение владельца
 * 2026-09-24): эмблема с названием и местом под ним («Лазурный пакт · 2-е из 3»), очки
 * победы («✦ 82/1100») и игровой день с отсчётом до следующего. Победа в забеге — выстоять
 * волны (PVR-2.5), а не набрать счёт; место среди ИИ-соседей ничего не значит; ритм забега
 * задают волны, а не сутки мира. Эмблема открывала досье сессии — счёт, фракцию и карьеру
 * основной игры, — и в забеге ему показывать нечего.
 *
 * Значение — id узла шапки (`prototype/build.mjs`); сторож в тесте сверяет их с разметкой.
 */
export const SECTOR_ZERO_ABSENT_HUD = {
  crest: 'tbcrest',
  score: 'tbscore',
  day: 'daycard',
} as const;

/**
 * Шпионажа в забеге Sector Zero тоже НЕТ (заказ владельца 2026-09-25). Кража окна разведки
 * — инструмент против соперника-державы: подсмотреть казну, флоты или мир другого игрока.
 * Забег одиночный, и мёртвая кнопка ему не нужна (тот же довод, что у списка выше).
 * Уходят все двери сразу: кнопка разведки на карточке чужого мира,
 * кнопки 🕵 в ростере дипломатии и вкладка «Шпионаж» окна дипломатии.
 */
export function espionageShown(sectorZeroRun: boolean): boolean {
  return !sectorZeroRun;
}
