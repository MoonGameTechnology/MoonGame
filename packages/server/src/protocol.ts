/**
 * Фасад над `@void/protocol` (NETA2-4). Сам контракт переехал в отдельный пакет, который
 * импортируют ОБА конца — сервер и клиент; здесь остаётся только реэкспорт, чтобы
 * шестнадцать внутренних импортов сервера (`./protocol`) продолжали работать и правка не
 * растекалась по файлам, которые к протоколу отношения не имеют. Тот же приём, что у
 * `prototype/src/game.ts` — чистый index-фасад без логики.
 *
 * Новый код лучше импортировать прямо из `@void/protocol`: фасад существует ради
 * совместимости путей, а не как второй адрес контракта.
 */
export * from '@void/protocol';
