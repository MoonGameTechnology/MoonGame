# Крепости: космическая крепость и форт на планете — технический roadmap

> **Две РАЗНЫЕ сущности, которые легко спутать по имени.** **Космическая крепость** — это
> УЗЕЛ карты: пустой узел без планеты превращается во владеемый и застраиваемый
> (`kind: empty` → `void_station`), после чего его застраивают по нужде, как планету.
> **Форт** (`fort`) — это ЗДАНИЕ на планете: снижение получаемого урона + гарнизон.
> Парные доки: `state.md` §5 (station) / §6 (данные), `map-roadmap.md` (виды секторов),
> `modulesystem.md` (шина), `shuttles-roadmap.md` (SQ-5.1 — «звёздная крепость» как база
> эскадрилий, другая ось), `gdd.md` §7 (бой и захват). Формат — кирпичики:
> `id · зона · статус · «Готово, когда»`. Зоны: `[core]` · `[data]` · `[proto]`.

---

## 0. Модель (read first)

### 0.1 Космическая крепость

> ⚠️ **Раздел переписан 2026-09-15 — см. §0.6.** Здесь стоял путь через технический
> юнит-конвертер: «изучить технологию → построить техюнит → привести в пустой узел →
> он конвертируется в крепость». **Владелец передумал**, техюнит отменён, и требование
> пустого узла снято вместе с ним. Ниже — действующая модель.

**Путь игрока:** захватить узел → построить на нём космическую крепость → крепость
застраивается своим ростером.

**Почему шаг «построить крепость» всё-таки нужен, а не просто «стройте что хотите».**
Местность сегодня почти вся `buildable: false` — на туманности, кладбище, ионном шторме,
плотной туманности и вспышке нельзя возвести НИЧЕГО. Это 90 узлов из 121 на карте
прототипа: декорация с бонусами к скорости и живучести. Крепость — тот шаг, который
превращает захваченную декорацию в развиваемое владение с орбитой и своим ростером
построек. Захват делает узел вашим, крепость делает его полезным.

### 0.2 Что из этого УЖЕ написано

Механика существует в ядре и **при этом недостижима в игре**. Модуль
`packages/shared-core/src/modules/station.ts` (57 строк, 7 тестов, в `DEV_MODULES`)
реализует `station.deploy {planetId}`:

```
E_BAD_PAYLOAD → E_NO_PLANET → E_NOT_EMPTY → E_FORBIDDEN → E_NO_ANCHOR → E_INSUFFICIENT
                           ↓
        node.kind = 'void_station' · node.owner = игрок · payCost({metal: 120})
                           ↓
                 emit 'station.deployed' {planetId, owner}
```

`void_station` в `data/sectorKinds.json` уже владеем, застраиваем
(`shipyard, spaceport, radar, fort, power_plant, fabricator`), без орбиты — и **уже
подписан `"label": "void fortress"`**. Тест `station.test.ts` пинует и полезную нагрузку:
радар, построенный на станции, возвращает зрение в тумане.

### 0.3 Чего не хватает — и почему это не «доделки», а первый кирпич

| Разрыв | Факт | После §0.6 |
| --- | --- | --- |
| **Узлов `empty` нет нигде** | все 4 карты `data/maps/*.json` содержат только `planet`/`asteroid`/`nebula`; `createDevMatch` (`scenario.ts`) тоже их не создаёт | ❌ **не разрыв**: крепость больше не требует пустоты |
| **Генератор карт намеренно без пустот** | `prototype/src/map.ts:378`: «only real provinces (**no "empty" void waypoints**)» | ❌ **не разрыв** по той же причине — карту трогать не надо |
| **Прототип не знает модуля** | `stationModule` отсутствует в `MODULES` (`prototype/src/protoKernel.ts`) — на хосте, где играют, действия просто нет | ✅ разрыв, чинит FORT-0.2 |
| **Прототип не нарисует крепость** | `SECTOR_TYPE_UI` (`prototype/src/map.ts:49-66`) знает `empty`, но записи `void_station` в нём нет | ✅ разрыв, чинит FORT-0.2 |
| **Правило места устарело** | `station.ts:45` требует `kind === 'empty'` — по §0.6 должно быть «любой ЗАХВАЧЕННЫЙ узел, кроме планеты и уже стоящей крепости» | ✅ разрыв, чинит FORT-0.2 |
| **Якорем годится любой флот** | `anchorFleetPresent` (`station.ts:28-32`) — по §0.6 заменяется на «узел уже твой» | ✅ разрыв, чинит FORT-0.2 |
| ~~Нет технологии и техюнита~~ | — | ❌ **отменено**: техюнит снят решением владельца |
| ~~Юнит не расходуется~~ | — | ❌ **отменено** вместе с техюнитом |
| **Нет UI и локализации** | ни одной кнопки; ключей отказов нет в обеих локалях | ✅ разрыв, чинит FORT-0.2 |

**Вывод, определяющий порядок работ:** сначала механику надо сделать ДОСТИЖИМОЙ
(FORT-0.2), и только потом доводить до формы из §0.6. Обратный порядок даёт код,
который невозможно проверить руками.

### 0.4 Решение: расширяем `station`, второй модуль не заводим

Сторож манифеста (`packages/server/src/moduleManifest.test.ts`) пинует **членство и
порядок** `DEV_MODULES`, а `version` отдельного модуля намеренно не закрепляет. Поэтому:

- правка поведения ВНУТРИ `station` не требует бампа `MODULE_MANIFEST_VERSION` — только
  подъёма `version` самого модуля (инвариант #6 не задет, реплеи живых матчей целы);
- новый модуль рядом потребовал бы вставки в `DEV_MODULES` → бамп версии манифеста →
  инвалидация реплеев, и это ровно то дублирование, против которого заведён блок CONV.

**Id модуля (`station`) не переименовывать** — он в закреплённом списке манифеста.
«Крепость» — это имя механики для игрока (ключ локализации), а не идентификатор в коде.

### 0.5 Форт на планете (решение владельца, 2026-09-07)

Форт выдаёт **настоящие наземные юниты**, а не виртуальную величину. Юнит так и называется
— «Гарнизон»; его **нельзя ни грузить, ни перемещать**. Чем выше уровень форта, тем их
больше и тем они сильнее. Потолок — **3**, и фракция этот потолок двигает.

**Почти всё для этого в движке уже есть, и это меняет цену механики.** Трейт `immobile`
означает ровно «никуда не денется»: такой юнит не собирается в флот
(`autoRally.ts:52`), не участвует в слиянии и разделении флотов (`fleetOps.ts:71,101`) и
не грузится в трюм (`army.ts:76`). Отдельного механизма «неподвижности» изобретать не надо
— достаточно трейта.

**Следствие, которое снимает самый рискованный кусок работы.** Раз это НАСТОЯЩИЕ стеки в
`planet.garrison`, их уже видит проверка `planet.garrison.some((s) => s.count > 0)` в
`captureOnArrival.ts:44`. Значит дыра «планета с фортом, но пустым гарнизоном берётся
прилётом пустого флота» закрывается **без единой правки в модуле захвата** — и без общего
хелпера «есть ли гарнизон», который пришлось бы заводить под виртуальный вариант (иначе
форт держал бы захват, но не дрался). Правило остаётся одно, а не превращается в два.

Цена, которую надо заплатить осознанно: настоящий юнит по умолчанию стоит содержания
(`economy.ts` списывает upkeep за гарнизон), поэтому «бесплатен ли гарнизон форта» —
решение владельца, а не деталь реализации. См. FORT-2.2.

### 0.6 РЕШЕНИЯ ВЛАДЕЛЬЦА — 2026-09-15

Разговор начался с «какие пустые узлы добавить на карту» и кончился перестройкой замысла.
Записываю итог, а не ход обсуждения; отменённое помечено явно, потому что через месяц оно
будет выглядеть свежей идеей.

| # | Вопрос | Решение владельца |
| --- | --- | --- |
| 1 | Технический юнит-конвертер | **УДАЛИТЬ — «я передумал»** |
| 2 | Где ставится космическая крепость | **На ЗАХВАЧЕННОЙ территории, на всех видах КРОМЕ планеты** (и кроме той, где крепость уже стоит) |
| 3 | Где строится добывающая станция | **Только мёртвые миры и астероидные поля** |
| 4 | Чем именно добывают | **Той же станцией, что на мёртвом мире** (`metal_station`) |
| 5 | Что такое форт | **Здание на планетах**: снижает получаемый урон СВОИМ И СОЮЗНЫМ наземным войскам |
| 6 | Что даёт прокачка форта | **Больше процента + небольшой гарнизон** (юнит «Гарнизон форта») |
| 7 | Как форт прикрывает от челноков и ракет | **Своей зенитки у него нет — он даёт небольшой дополнительный урон зонального ПВО**, складываясь с батареей |

**Три сущности со словом «крепость» наконец разведены.** Это была главная путаница файла,
и теперь у каждой своё место:

| Сущность | Что это | Где |
| --- | --- | --- |
| **Космическая крепость** (`void_station`) | ВИД узла: захваченная местность становится владением с орбитой и своим ростером | любой захваченный узел, кроме планеты |
| **Форт** (`fort`) | ЗДАНИЕ: снижение наземного урона, с прокачкой — гарнизон и ПВО | только планеты |
| **Добывающая станция** (`metal_station`) | ЗДАНИЕ: добыча металла (30/60/100 по уровням) | мёртвые миры и астероиды |

#### Что эти решения ОТМЕНЯЮТ

- **FORT-0.1 (узлы `empty` в картах канона) — не нужен.** Крепость больше не требует
  пустоты, значит и заводить её незачем. Кирпич снят, а не «отложен».
- **FORT-1.1 / 1.2 / 1.3 (техюнит, конверсия им, вкладка техюнитов) — отменены** решением 1.
- **Требование `kind === 'empty'`** в `station.ts` — заменяется правилом решения 2.
- **Требование флота-якоря** (`anchorFleetPresent`) — заменяется на «узел уже захвачен»:
  владение и есть доказательство, что вы там были.

#### Что проверено по коду и расходится с решением 5

~~**Форт сегодня помогает ТОЛЬКО владельцу мира.**~~ **Починено 2026-09-15.** В хуке
`combat.damage` стояло `planet.owner !== a.defender` → выход: союзник, обороняющий вашу
планету, от вашего форта не получал ничего. Расхождение было тихим — все тесты проверяли
владельца, и спросить про ВТОРОГО обороняющегося было некому, хотя после MSB-4 их на одном
мире может быть несколько.

Правило теперь «владелец ИЛИ его союзник», и проверка именно такая, а не «не враг»: снять
её целиком значило бы прикрыть и ШТУРМУЮЩЕГО, стоящего на вашей же земле, то есть заставить
форт работать на захватчика. Союзник — ровно `alliance` (`isAllied`): перемирие и пакт
войсками не делятся, значит и прикрытием не делятся тоже.

⚠️ **По дороге нашлось, что хуков наземной защиты ДВА**, и правка одного дала бы игроку
необъяснимо частичный эффект: союзник начал получать бонус форта, но не однопроцентную
скидку за каждое стоящее здание. Предикат вынесен в `fortificationCovers` — один дом на оба
хука, иначе они разойдутся снова.

Что уже работает и переделывать не надо: **прокачка форта уже даёт больше процента** —
0.3 → 0.45 → 0.6 по уровням в `data/buildings.json`.

Чего нет: **юнита-гарнизона** (FORT-2.1).

**ПВО форта — решение 7, и оно оказалось правкой ОДНИХ ДАННЫХ.** Зональное ПВО мира
считается так: `planetPointDefense` (`shuttle.ts:81-89`) суммирует поле `pointDefense`
по ВСЕМ стоящим зданиям, беря значение уровня. Значит форту достаточно объявить это поле —
и он начнёт вносить свою долю, растущую с прокачкой, без единой строки кода. Схема уже
держит `pointDefense` с дефолтом 0, поэтому здание без поля ведёт себя ровно как раньше.

Числа: зенитка `zonal_aa` даёт **40** одним уровнем за 130 металла + 40 кредитов. Форт
получает **8 / 12 / 16** по трём уровням — от пятой части батареи до двух пятых. Мир с
фортом прикрыт, но настоящая батарея остаётся настоящей батареей, иначе решение 7 тихо
обесценило бы отдельное здание.

**Ракет в игре нет вовсе**: весь блок MS-* заперт. Зональное ПВО по замыслу бьёт и по ним
(`pointDefense` в схеме: «fires on incoming shuttle/missile»), так что «от ракет» включится
само, когда ракеты появятся — отдельной работы не требует.

#### ⚠️ Открытая развилка: куда девать `starfort`

Здание `starfort` называется **«Void Fortress»** (+40% обороны) и сегодня оно —
ЕДИНСТВЕННАЯ постройка, разрешённая на астероидах. Решение 3 оставляет на астероидах только
добывающую станцию, то есть `starfort` остаётся без места на карте.

Вариантов три, и я НЕ выбираю за владельца: (а) перенести в ростер космической крепости —
имя и эффект туда ложатся, и крепость станет можно укреплять; (б) оставить на астероидах
вторым зданием рядом с добывающей; (в) удалить из игры — оборону в космосе держат `fort` и
`orbital_aa` из ростера крепости. Вопрос задан, ответа пока нет; до ответа здание не
трогаю.

---

## Фаза 0 · Сделать существующее достижимым `[data][proto]`

### FORT-0.1 · 🗑 Узлы `empty` в картах канона `[data]` — снято 2026-09-15

**Снято решением владельца 2 (§0.6): крепость больше не требует пустоты.** Она ставится на
любой ЗАХВАЧЕННОЙ местности, а такой на картах и так вдоволь — 90 узлов из 121 только на
карте прототипа. Заводить пустые узлы стало незачем; текст ниже оставлен записью о том,
что предполагалось, и почему это отменилось.

~~Исходная формулировка:~~
**Подзадачи:** добавить узлы `kind: "empty"` в `data/maps/*.json` (как минимум в
`skirmish-1.json`) — путевые узлы между провинциями, по которым проходят лейны; проверить,
что `MatchMapSchema` их принимает и `buildStateFromMap` доносит `kind` до `Planet.kind`;
не ломать связность (узел без линков недостижим).
**Готово, когда:** в матче со `skirmish-1` есть хотя бы один узел, на котором
`station.deploy` возвращает `ok`, а не `E_NOT_EMPTY`; покрыто тестом на карте.

### FORT-0.2 · ✅ 2026-09-15 Крепость в прототипе: модуль, правило, кнопка `[core]` `[proto]` — M
**Подзадачи:** внести `stationModule` в `MODULES` (`prototype/src/protoKernel.ts`);
завести запись `void_station` в `SECTOR_TYPE_UI` (`prototype/src/map.ts`), иначе узел
не нарисуется; **решить с владельцем**, как пустые узлы попадают на карту прототипа — его
генератор намеренно строит карту без пустот («only real provinces», `map.ts:379`), поэтому
это дизайн-решение, а не правка; обновить счётчик `MODULES` прототипа в `state.md` §9
(35 → 36) — он машинно сверяется `docs-check`.
**Сделано, и объём вышел ШИРЕ исходного** — решения §0.6 превратили кирпич из «довезти
модуль» в «переписать правило и довезти модуль»:

- **правило места переписано** (`station.ts`): вместо `kind === 'empty'` + флот-якорь —
  «свой узел» + `sectorKinds.stationable`. Код отказа `E_NOT_EMPTY` → `E_NOT_STATIONABLE`,
  `E_NO_ANCHOR` снят вместе с якорем (их тексты в локалях тоже — иначе осиротели бы);
- **правило живёт В ДАННЫХ**, а не строкой `'planet'` в коде: новый флаг вида
  `stationable` с дефолтом `true`, исключения проставлены у `planet`, `void_station`,
  `pirate_base`, `neutral_base`. Незахватываемые виды флага не требуют — их закрывает само
  «свой узел», и на это есть сторож в `data/fortPointDefense.test.ts`;
- **модуль доехал до прототипа** (`MODULES` 35 → 36, счётчик §9 сверяется машинно);
- **вид `void_station` рисуется** — тёплый янтарь на холодной палитре: крепость
  единственная появляется по воле игрока, и её надо отличать от ландшафта с одного взгляда;
- **кнопка в панели своего узла**, рядом с «Постройками», а не вместо: на астероидах и
  мёртвом мире осмысленно и то и другое. Правило кнопки — `decisions/fortressRaise.ts`,
  и оно не переписано, а взято у ядра: тест гоняет ВСЕ 30 раскладов и требует, чтобы
  «кнопка нажимается» совпадало с «редьюсер принял». Это защита от ORB-4, где своя копия
  правила развела клиент с данными и кнопка звала строить там, где сервер отказывал;
- **цена экспортирована** из модуля (`STATION_COST`), чтобы у клиента не завелась вторая
  копия числа.

Проверено порчей: убрать модуль из `MODULES` прототипа — и приёмочный тест краснеет с
`E_UNKNOWN_ACTION`, то есть ровно тем симптомом, ради которого кирпич и заводился.

---

## Фаза 1 · Форма из видения владельца `[core][data]`

### FORT-1.1 · 🗑 Технический юнит + технология — снято 2026-09-15

**Отменено решением владельца 1 (§0.6): «удалить юнита-конвертера, я передумал».** Крепость
строится на своей территории без техюнита. Текст ниже — запись о том, что предполагалось.

~~Исходная формулировка:~~
**Подзадачи:** юнит-строитель в `data/units.json` с новым трейтом (напр. `constructor`) —
схему менять НЕ надо, `traits: string[]` уже принимает любую строку, прецедент — трейт
`shuttle` (`construction.ts:426`); технология в `data/technologies.json` с
`unlocks: { units: ['<id>'] }` — гейт включается **только данными**, хук
`construction.requirement` (`technology.ts:367-383`) отдаст `E_TECH_LOCKED` сам; выбрать
`domain` осознанно (`space` — авто-ралли поднимет юнит в флот; `ground` — поедет грузом).
**Готово, когда:** `unit.build` техюнита без технологии отвечает `E_TECH_LOCKED`, с
исследованной — строится; бамп `data/manifest.json` + `schemas.test.ts` + `state.md` §6.

### FORT-1.2 · 🗑 Конверсия расходует техюнит — снято 2026-09-15

**Отменено решением владельца 1 (§0.6): «удалить юнита-конвертера, я передумал».** Крепость
строится на своей территории без техюнита. Текст ниже — запись о том, что предполагалось.

~~Исходная формулировка:~~
**Подзадачи:** в `station.ts` заменить `anchorFleetPresent` (любой непустой флот) на
проверку «в флоте на узле есть юнит с трейтом строителя»; **списать один такой юнит** при
успехе (это и есть «конвертируется»); новый код отказа для «привёл не тот флот» (напр.
`E_NO_CONSTRUCTOR`) — старый `E_NO_ANCHOR` оставить или заменить осознанно, тесты пинуют
оба; поднять `version` модуля; обновить `station.test.ts` (7 существующих тестов — сетка).
**Готово, когда:** обычный флот получает отказ, флот со строителем конвертируется и теряет
один юнит; вход не мутирован (`deepFreeze`); `MODULE_MANIFEST_VERSION` НЕ тронут.

### FORT-1.3 · 🗑 Вкладка технических юнитов в «Верфи» — снято 2026-09-15

**Отменено решением владельца 1 (§0.6): «удалить юнита-конвертера, я передумал».** Крепость
строится на своей территории без техюнита. Текст ниже — запись о том, что предполагалось.

~~Исходная формулировка:~~
**Подзадачи:** добавить `'tech'` в union `YardTab` и запись в `YARD_TABS`
(`prototype/src/shipyard.ts:36-42`) — чрома вкладок и делегат клика уже общие, **CSS не
нужен** (`.cn-tab` живёт в `build.mjs`); список корпусов `YARD_TECH_HULLS` + одна строка в
`hullsOf()`; ключи `yard.tab.tech` в `ru.ts` И `en.ts`.
**Готово, когда:** вкладка видна, техюнит из неё строится, гейт `pnpm test i18n` зелёный.

### FORT-1.4 · Недостающие ключи отказов `[proto]` ✅ — S
**Сделано.** `err.not-empty` и `err.no-anchor` заведены в `ru.ts` И `en.ts` — оба кода
отдаёт только `station.deploy`, поэтому текст назван по делу («здесь не пустота — станцию
ставят только в пустом узле», «нужен свой флот на месте — станцию ставят с корабля»).
Третий названный ключ, `err.tech-locked`, **уже существовал** — описание кирпича здесь
отстало от кода. Сверка остальных кодов `station.deploy` (`E_BAD_PAYLOAD`, `E_NO_PLANET`,
`E_FORBIDDEN`, `E_INSUFFICIENT`) пропусков не нашла.
**Гейт вместо разовой сверки.** Список кодов не переписан в тест руками, а ЧИТАЕТСЯ из
`packages/shared-core/src/modules/station.ts`: новый тест `prototype/src/i18n.test.ts`
(«каждый отказ КРЕПОСТИ назван словами в обеих локалях») разбирает `h.reject('E_*')`
модуля и требует ключ в обеих локалях, плюс `E_TECH_LOCKED` — гейт форта живёт не в
модуле крепости, а в хуке `construction.requirement` (`technology.ts`). Следующий
`h.reject()` без текста уронит гейт, а не доедет до игрока сырым кодом.
**Сверх кирпича замечено (не чинится здесь, зона другая):** из 116 кодов `h.reject()`
всех модулей ядра текста в локали нет у 67 — вне крепости это остаётся открытым. Заведено
кирпичом `LOC-7` в `docs/backlog.md`.
**Готово, когда:** ни один отказ крепости не доезжает до игрока непереведённым.

---

## Фаза 2 · Форт на планете `[core][data]`

### FORT-2.1 · Юнит «Гарнизон» `[data]` ⏳ — S
**Подзадачи:** юнит `garrison` в `data/units.json` — `domain: 'ground'`,
`traits: ['immobile']`, боевые статы под роль «сидит и держит»; **имя в данных пишется
по-английски** (`"name": "Garrison"`), русское «Гарнизон» живёт ключом `data.garrison` в
`/localization/ru.ts` + `en.ts` — `tData()` строит слаг из имени, и русское имя в JSON
схлопнуло бы ключ до недостижимого; ступени силы под уровни форта (либо один юнит с
растущим количеством, либо отдельные юниты на уровень — решить здесь и записать).
**Готово, когда:** юнит существует, `army.load` его не берёт, `fleet.split`/`merge` не
трогают, авто-ралли не поднимает; всё это уже обеспечено трейтом — тест это фиксирует,
а не реализует.

### FORT-2.2 · Форт выдаёт и забирает гарнизон `[core][data]` 🔒(FORT-2.1) — M
**Подзадачи:** поле «что выдаёт этот уровень» в `BuildingLevelSchema` + `BuildingDefSchema`
(рядом с `defenseBonus`, растёт с уровнем); на завершении постройки/апгрейда форта —
`addUnits(planet.garrison, …)`, тем же хелпером (`util/stacks.ts`), которым уже
пользуется `construction.complete`; **на разрушении форта гарнизон снимается** (иначе он
переживёт здание, которое его породило); `captureOnArrival` **не трогаем** — он видит эти
стеки как обычные (§0.5).
**Решение владельца, нужное до кода:** стоит ли гарнизон форта содержания. Настоящий юнит
по умолчанию его стоит; «бесплатен» — это либо нулевой `upkeep` у юнита, либо исключение
в `economy.ts` (первое честнее и не заводит спецслучая).
**Готово, когда:** планета с живым фортом и пустым `garrison` НЕ берётся прилётом, а
штурмуется; апгрейд форта увеличивает гарнизон; разрушение форта его уносит; вход не
мутирован.

### FORT-2.3 · Потолок гарнизона и фракционный модификатор `[data][core]` 🔒(FORT-2.2) — S
**Подзадачи:** потолок 3 (решение владельца) — считать по планете, а не по зданию, иначе
два форта обойдут его сложением; фракция потолок двигает, а `FactionPassivesSchema` —
ЗАКРЫТЫЙ объект из четырёх числовых ключей (`productionBonus`, `fleetSpeedBonus`,
`combatDamageBonus`, `radarRangeBonus`), поэтому нужен пятый ключ и его чтение в
`faction.ts` (модуль уже читает пассивки единообразно — `def?.passives[key] ?? 0`);
без модуля фракций — база, как требует инвариант #3.
**Готово, когда:** гарнизон не растёт выше потолка ни апгрейдом, ни вторым фортом;
у разных фракций потолок разный; без модуля фракций работает базовый.

---

## Фаза 3 · ПВО `[data][core]`

### FORT-3.1 · Уровни `orbital_aa` `[data]` ⏳ — S
**Подзадачи:** у `orbital_aa` в `data/buildings.json` сейчас **нет `upgrades` вообще** —
добавить уровни с растущим `aaDamage` и `upkeep`, как у `radar`/`fort`.
**Готово, когда:** батарея прокачивается; тесты, прибитые к числам, обновлены осознанно.

### FORT-3.2 · ПВО бьёт авиацию, а не только корабли `[core][data]` 🔒(FORT-3.1) — M
**Подзадачи:** шов уже прорезан — `BuildingLevelSchema.pointDefense` описан как
«intercepts shuttle/missile strikes, not regular fleets», но **ни одно здание его не
заполняет**, а `shuttle.ts` читает PD только с флотов (`fleetPointDefense`); научить
эскадрильи видеть планетарный PD и заполнить поле у `orbital_aa`; решить с владельцем
порядок целей — сейчас батарея с 1-го уровня бьёт по КОРАБЛЯМ, а в видении по кораблям
она должна бить только на максимальном уровне.
**Готово, когда:** эскадрилья, влетевшая в зону планетарного ПВО, получает урон; порядок
приоритета целей закреплён тестом.

---

## Фаза 4 · Расширения (по решению владельца) `[core][data]`

### FORT-4.1 · ✅ 2026-09-15 Какие ещё узлы конвертируются `[core]` `[data]` — S

**Закрыт ТЕМ ЖЕ кирпичом FORT-0.2, и это не совпадение:** здесь было записано «вынести
список конвертируемых видов в ДАННЫЕ (поле у `sectorKinds`), а не хардкодить второй
константой», а решение владельца 2 потребовало ровно этого. Разделять их значило бы
сперва написать хардкод, чтобы через кирпич его снять.

Записанные тут же слова владельца — «в узлах, где нет планет» — совпали с решением 2
дословно, то есть замысел не менялся, менялся только путь к нему (техюнит отменён).

**Сделано:** поле `sectorKinds.stationable` (дефолт `true`, исключения у `planet`,
`void_station`, `pirate_base`, `neutral_base`), хелпер `isStationable`, и ни одной
константы вида в модуле — `EMPTY_KIND` снят. Неизвестный вид падает на пермиссивный
дефолт `sectorKindDef`, как все прочие свойства вида, а не роняет действие: fail-secure
здесь обеспечивает второе правило («только свой узел»), потому что незахватываемое своим
не станет.

---

## Что этот роадмап НЕ покрывает

- **«Звёздная крепость» как база эскадрилий** — это `shuttles-roadmap.md` SQ-5.1
  (апгрейд `fort` vs новое здание, лимит базирования). Другая ось: там крепость — здание
  на планете, здесь — узел карты.
- **Форт прототипа `starfort`** (`prototype/src/prototypeData.ts`, только на астероидах,
  даёт орбитальное кольцо) — прототипный аналог канонного `fort`. Сведение двух каталогов
  — предмет блока CONV в `backlog.md`, а не этого документа.
- **Мета-экономика** (крепость за донат, чертежи техюнитов в арсенале) — `economy-roadmap.md`
  и `arsenal-roadmap.md`.
