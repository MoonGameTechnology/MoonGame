// Общая преамбула тестов: подключить тексты ВСЕХ локалей.
//
// В проде так делает только прототип — клиент качает одну запечённую локаль (LOC-6),
// поэтому его модули импортируют `core.ts`, у которого таблицы пусты до первого
// `registerMessages`. В тестах же ожидание обратное: `t('welcome.title')` обязан
// вернуть текст, а не ключ, в любом файле и без ритуала на каждый импорт.
import './runtime';
